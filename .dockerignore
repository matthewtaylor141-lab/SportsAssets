# THE BUILD CONTEXT IS NOW THE REPOSITORY ROOT, so it has to be trimmed.
#
# render.yaml builds the API and workers with `rootDir: .` because the
# image must contain research/beta48/shadow -- the bridge imports the
# tested EV modules rather than carrying a copy. Without this file the
# context is ~271 MB (135 MB of it .git, 195 MB of it research/), all
# uploaded on every deploy, to ship ~7 MB that matters.
#
# WHAT MUST SURVIVE, and what proves it: backend/ and
# research/beta48/shadow/ are the only trees the Dockerfile COPYs.
# backend-image-check.yml builds this exact context and then imports
# every required module INSIDE the container, so an over-broad rule
# here fails CI rather than silently shipping an image whose bridge
# can never load.

.git
.github
**/.pytest_cache
**/__pycache__
**/*.pyc
**/.DS_Store
**/node_modules
*.egg-info

frontend/
docs/
ops/
edge-engine/
pmus-broker/
rn1-collector/

# research/ is 195 MB and the image needs one 7 MB subtree of it.
# Everything is excluded, then that subtree is re-admitted -- each
# parent has to be re-admitted too, because Docker will not descend
# into a directory it has already excluded.
research/*
!research/beta48
research/beta48/*
!research/beta48/shadow
# The frozen whale priors sit beside shadow/, not inside it, and carry
# the completion-hazard table the pair engine reads.
!research/beta48/*.json

# THE CAPTURED INCENTIVE MANIFEST. Narrow on purpose: `acceptance/` is
# 4.9 MB of patches, suite XML and result JSON that the runtime has no
# use for, and the rule above excluded the whole directory.
#
# THIS IS THE OTHER HALF OF THE MANIFEST DELIVERY DEFECT, and it is why
# adding the COPY alone was not enough. The Dockerfile can only copy
# what the CONTEXT contains; `research/beta48/*` removed `acceptance/`
# from the context, so the COPY failed at build time with "not found"
# even though the file was committed and present in the working tree.
# A repository-file check passes here. A build does not.
!research/beta48/acceptance
research/beta48/acceptance/*
!research/beta48/acceptance/incentive_manifest.json

# THE INCENTIVE SCORER, re-admitted for the same reason and by the same
# rule -- and I walked straight into the trap documented above.
#
# `research/beta48/*` excludes CODE as well as directories, and only
# `shadow/` and `*.json` were re-admitted. Adding
# `COPY research/beta48/bettor_incentive_score.py` to the Dockerfile
# therefore failed the build in 24 seconds with "not found", exactly as
# the manifest paragraph predicts: the file was committed, present in the
# working tree, and absent from the CONTEXT. Render reported
# `build_failed 29b25a5` and the previous image kept serving, so the
# production read that followed measured the OLD build -- which is how
# the two new endpoints answered 404 while their tests passed locally.
#
# Two files, named literally, because the command centre scores the
# programme's observed ladders through this engine rather than carrying a
# second copy of the reward formula.
!research/beta48/bettor_incentive_score.py
!research/beta48/bettor_incentive_opportunity.py
research/beta48/shadow/**/__pycache__
