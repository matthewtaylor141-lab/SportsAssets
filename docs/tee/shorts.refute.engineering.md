# REFUTATION of the 'shorts' lens, angle: engineering (read-only; HEAD c2249ce; 2026-09-02 ~23:30Z)

Lens read at cedbae0; HEAD c2249ce shifts mirror_live_rules.py lines by +5 after :124, +11 after :759, +18 after :1335 (git diff cedbae0 c2249ce). Probes run here: tee/refute_probe1.py, tee/refute_probe2.py, tee/refute_parse.py (outputs quoted below).

## Verified as the lens stated
- realized_pnl(0.51,0.40,10,BUY_SHORT)=1.1 | long -1.1; wire_limit(0.49,BS)=0.51; rest_tick=0.51; fill_cash(10,0.51,BS)=4.9 | long 5.1 (refute_probe1).
- sell_wire(1-q) == rest_tick(wire_limit(q,BUY_SHORT)) over 70,099 q on the ladder [0.01,0.99]: 0 mismatches (refute_probe2). Off the ladder (q<0.01) sell_wire caps at 0.99 while wire_limit gives 1.00 — not a money issue.
- target_shares(1.0,-10038.5,0.49,allow_short=True) -> -490 capped; +100000@0.9 -> 277, -100000 -> -2500.
- plan(-510 from 0) -> SELL_LONG 510@0.51 'reduce toward target'; (-510 from -75) -> SELL_LONG 435; (-30 from -75) -> BUY_LONG 45@0.48 'increase'; (0 from -68) -> BUY_LONG 68@0.3 'increase toward target'.
- _state_nums(BookState(ledger_net=-5)) = None; book_sell(BookState(),10,0.49) -> booked 0, overfill=True; book_buy on ledger -5 -> 'bad_state'; select_flatten(-5,...) = None; mirror_target(1.0,-10038.5,0.49,50) -> target 0 'short_side_refused' intent BUY_LONG.
- keep_or_replace(OpenOrder side 'SELL_SHORT' vs SELL_LONG plan) = 'replace'.
- _us_game_key: bur / mid / draw slugs of atc-eflch-bur-mid-2026-09-02 all -> 'eflch-bur-mid-2026-09-02'.
- market_type_of('atc-...-bur') = 'moneyline'; tsc- = 'total'.
- _norm_order of a resting BUY_SHORT -> side 'BUY', intent BUY_SHORT; _bid_matches(that order with order_id, {0.53}, 10) = True; _lost_fill_is_ours(SELL fill @0.53, his 0.47, BUY_SHORT) True | as BUY_LONG False; _fingerprint_cents(0.47,BS)={0.53}.
- MIRRORMKT parse over the lens's 8 files: 60 lines / 42 distinct / 33 mapped / 18 his_net<0 / 14 negative ledger (refute_parse, files[:-1]) — EXACT match. Over all 9 files (probe_33690960366, 22:35-22:56Z, not in the lens): 72 / 48 / 38 / 20 / 15.
- grep -c 'short side not admitted': 0,0,2,0,3,7,3,4,(3).
- probe_33686724064.txt:1320 GATES ok 373 mismatch 0; :1950-1951 SHORTTRUTH 50/50 SELL; :1956,1959-1961 atc- SHORTROW venue_side=ORDER_SIDE_SELL; :2041-2044 the 517sh @0.97 BUY_SHORT fill; :1988 SIDES buys 856392 sells 64.

## Refutations / corrections
R1. per-side premise (finding 4, and 1d of the report). The venue's OWN sides for an atc- team contract share ONE identifier with an explicit long bool: probe_33686724064.txt:1866 `sides=[{"long":true,...,"identifier":"atc-ecu1-cue-gua-2026-09-02-cue"},{"long":false,...,"identifier":"atc-ecu1-cue-gua-2026-09-02-cue"}]`. premap.side_intent (premap.py:2052-2068) reads the `long` bool FIRST, so the No-side of a per-side team contract is BUY_SHORT on the SAME slug — not "unique identifier -> BUY_LONG" (:2077-2080), and not the contract-kind hard BUY_LONG (:2183-2192, which never runs for a market that HAS sides: "a market that HAS sides never falls through to the contract branch"). The copy lane does exactly that: :1429 `MAPA 10.53 rn1 pick=No slug=atc-scp-dfc-joh-2026-09-02-dfc` -> the SHORTROW at :1956 (qty 49, ORDER_SIDE_SELL) -> the shadow's ledger -49 on that slug. Under that shape map_market's cands are {Yes:(slug,BUY_LONG), No:(slug,BUY_SHORT)} and _choose_long takes the LONG branch (mirror_shadow.py:238-241) — probe: `choose long+short same slug -> {'us_slug':'s','long_asset':'Y','other_asset':'N','source':'ledger'}` with NO per_side key; short-only rows -> the same without per_side. per_side=True needs two BUY_LONG rows on two different slugs (:232-237), which the venue's side shape does not produce for atc- team contracts. So (a) "per-side = soccer moneylines" and "only per_side_unsupported refuses atc-" are not supported: the refusal on his No side of an atc- slug is short_side_refused, the same as aec-; (b) the "re-picked long slug every tick" defect is confined to the 2-longs shape, which the logs give no instance of (the lens itself: per_side unreadable from logs, grep -c per_side = 0 in all 9 probes).
R2. Three-way games. The venue lists a DRAW contract per soccer game: probe_33686724064.txt:1831-1832 NAMEDML-Q "...end in a draw?" on atc-eflch-bur-mid-2026-09-02-draw, plus the -bur and -mid slugs (:1825-1830); the slug census over all probes has 8 x '...-draw' slugs. So (i) "B-long is economically A-short" (report section 1d) is false — P(not A) = P(B) + P(draw); (ii) his condition on a soccer game is one of THREE binary markets (Bur win / Mid win / Draw), each its own slug, all three sharing _us_game_key (probe) — the lens's "one book per game with a signed target in side-A shares" would silently drop his Mid and Draw positions, and P1's one-per-game referee (le:8645-8660, widened OR lane='mirror') already does.
R3. finding 2's proposed change (flip mirror_shadow to allow_short=True now) breaks a P2-gate invariant: spec 1e "shadow_live_disagree: a tick where the shadow's target != the book's target ... is named" and gate (4) "shadow_live_disagree == 0"; p2_verdict(_p2_numbers(shadow_live_disagree=1)) = (False, ['shadow_live_disagree']) (probe). Every live book whose his_net turns negative would read live target 0 (flatten) vs shadow target < 0 -> counter > 0 -> P2 refused by construction. It also changes P0's would-fill reading mid-measurement (SELL-to-open plans from a flat ledger judged against the bid). Amend: carry a SECOND column (target_short / would_side_short / would_fill_short) computed with allow_short=True beside the long-only target the live worker is compared against.
R4. finding 3 item 6 ("episode_close_reason uses abs(st.ledger_net) — already sign-safe"): false. It calls _state_nums first (rules :1182), which returns None on ledger_net < 0 (:950-951): probe `episode_close_reason(BookState(ledger_net=-5, avg_cost=0.5, gross_buy_usd=10), True, None, None, 0) = 'bad_state'`. A short book could never close.
R5. finding 3 item 1 ("a live long book whose target turns negative flattens to 0 first, then opens a SHORT episode"): 047:72-73 mirror_books_one_open_per_market (whale, us_market_slug) WHERE state <> 'closed' forbids a second open book, and the only path to 'closed' on a live market is episode_close_reason's flat-for-MIRROR_FLAT_CLOSE_S (3600 s, rules :246; probe) — so RN1's tennis sign flips (the Nakashima shape the mirror exists for) would wait an hour flat before the short episode could open, or never if he flips back inside it. Needs an explicit sign-flip close (or a leg switch inside one book), not a 'flatten then open'.
R6. finding 3 item 7 ("keep_or_replace compares OpenOrder.intent"): OpenOrder has no intent field (rules :641-647: side, wire, qty, leaves, placed_at). Fine as an amendment, wrong as a description.
R7. finding 1's "no new venue arithmetic" hides one env switch: every short branch in le is gated by _use_short_math = is_short_intent and short_model_confirmed() (le:2965-2966, :2959-2962 LIVE_SHORT_COST_MODEL). Probe with LIVE_SHORT_COST_MODEL=off: wire_limit(0.49,BS)=0.49, realized_pnl(0.51,0.40,10,BS)=-1.1, fill_cash(10,0.51,BS)=5.1 — the wire denomination and the P&L sign both flip silently. A mirror short path must refuse (fail closed) when short_model_confirmed() is False rather than inherit the copy lane's arithmetic.
R8. finding 1's "reuse submit_fok verbatim": the preview guard (pmus.py:2233-2260) compares venue prev_cost against expected_cost = limit_price x quantity; for a BUY_SHORT the limit on the wire is the SELL cent (0.97) so expected = 0.97q while the venue's cashOrderQty is the collateral 0.03q — the guard passes by ~30x and bounds nothing on a short (the per-fill lane already runs under this). Not a refutation of the wire, a missed fact.
R9. room_scale(qty, wire, ...) divides the room by the WIRE (rules :632): probe room_scale(1000, 0.97, 50, 1250, 5000, 1250) = 51 shares vs 1000 at wire 0.03 — a short increase sized by the sell cent is under-sized ~30x relative to its collateral; the day cap and clip would need (1 - wire) for a short.
R10. his_level(reducing=False) reads only his BUY of the long token (ms:398-400): for a short-book reduce his move can be a SELL of the other token; probe: fills [SELL other @0.4796 (newer), BUY long @0.6 (older)] -> his_level(not reducing) = 0.6, the stale entry. Rare for RN1 (64 sells) but the lens's "reducing already picks the right family on both signs" is not exact.

## What the lens missed (scope: shorts and per-side)
M1. Three-way soccer (draw slug) — R2.
M2. The venue's per-side team contract shares its identifier between long/short sides — R1 — so atc- soccer moneylines are admissible to P1 as LONG books today (family moneyline, per_side not set) and the lens's per-side chapter addresses a shape the logs never show.
M3. shadow_live_disagree / P2 gate coupling of the allow_short flip — R3.
M4. The sign-flip episode under the one-open-per-market index and the 3600 s flat close — R5.
M5. LIVE_SHORT_COST_MODEL disarm inverting the short arithmetic the mirror would reuse — R7.
M6. The preview guard is inert on shorts — R8; room_scale sizing by the sell cent — R9.
M7. episode_close_reason bad_state on a negative ledger — R4.
M8. The take path: spec T "ONE IOC at the SAME wire via submit_fok(..., IOC)" — for a short INCREASE the IOC must go out as intent=BUY_SHORT, sell=False; a worker that maps every SELL_LONG plan to sell=True (P1's shape) would send SELL_LONG on a side we do not hold.
M9. Timing: P2 (shorts) is behind D3 — 30 closed books, 20+ games, ci95 lower bound > 0 (spec gate (1)-(2)); the lens's 'dollars_at_stake' framing ("18 markets x $250 in 6 h") is not reachable before that proof, and the 9th probe shows the same proportion (20/38), not growth.
