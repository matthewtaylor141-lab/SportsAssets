# COVERAGE LENS — why 81% of RN1's markets read "unmapped" in the P0 shadow, what they are, what maps them

Read-only review of commit cedbae0 (HEAD) against probe logs 33670859137 (19:23Z) … 33686724064 (22:32Z, latest).
All paths relative to /home/user/SportsAssets/backend/sportsassets unless noted. Probe = scratchpad/probe_33686724064.txt unless noted.
Sandbox facts that bound this report: the venue gateway is NOT reachable from here (`curl https://api.polymarket.us` → `CONNECT tunnel failed, response 403`), the DB is not reachable, and the WBLOB_rn1 activity blobs in every probe file are masker-corrupted (gunzip yields 0.6–23 KB of a 4.8 MB file; the workflow says so itself at .github/workflows/engine-diagnostic.yml:3568 "unmasked — the log blobs are not"); the engine-export artifact (id 9869152566, 6.17 MB) exists on GitHub but its Azure blob host is denied by the egress proxy. So DOLLAR numbers below are denominators from the logs; the numerator needs the MIRRORCOVER step in §5.

## 0. Headline numbers (all from logs)

| reading | value | source |
|---|---|---|
| P0 window 24h: markets read / mapped | 309 / 56 = 18.1% mapped, 81.9% unmapped | probe:1713 `MIRROR rows=5577 markets=309 mapped=56` |
| Series of the same reading across runs | 28/144 (19.4%) 19:23Z; 44/284 (15.5%) 20:34Z; 51/303 (16.8%) 21:16Z; 54/305 (17.7%) 21:44Z; 56/309 (18.1%) 22:32Z | `grep "MIRROR rows=" probe_*.txt` |
| Latest tick | markets=20 unmapped=1 skipped_unmapped=7 (TTL cache working) | probe:1728 MIRRORHB |
| Per-tick cap | MAX_MARKETS_PER_TICK=20, `skipped_markets` 381→430 per tick until the abandon tick | workers/mirror_shadow.py:59; MIRRORHB lines |
| RN1 new positions/day, and how many carry a slug date inside yesterday..tomorrow | med_new=1263.5, med_playable=1206.5 (95.5%) | probe:1544 WHALERATE; api/app.py:7132-7135,7173-7178 |
| RN1 notional 2026-09-02 (UTC day, RN1 only) | $2,400,134.97 on 9,236 BUY rows, 1,255 distinct assets | probe:1548 WRDAY (endpoint `/api/admin/whale-rate?whale=rn1`, yml:916) |
| Where his dollars sit | lots ≥ $250 = 86.8% of his stake (30d) | probe:1686 SIZEEDGE |
| Copy-sweep candidate pool by game date | past=971 future=0 undated=0 today_tomorrow=1249 | probe:1590 SWEEPMIX; workers/copy_sweep.py:302-319 |
| Premap sweep completeness | full: events=1694 rows=82670 pages=17/120 window=[12,96] complete; fast: 554 ev, 12558 rows, 6/25 pages complete | probe:1393-1394 PREMAPSWEEP |
| Copy-lane refusals 30d attributed to `unmapped` | refused=45715, 65.15% of all refusals | probe:1759 GATEEDGE |
| Copy-lane unmapped census (48h, 400 sampled, ALL whales) | type_prefix_filter_emptied 175 (43.8%), no_side_match 83 (20.8%), resolves 77 (19.3%), no_key_intersection 65 (16.3%) | probe:1797-1800 UNMAPSTEP |
| League-alias probe | 0 of 400 would be found by dropping the league token | probe:1795 UNMAPALIAS |
| Named-tennis bridge (dark) | would_resolve=0 (not_named 33, wrong_type 21, outcome_thin 1) | probe:1805 NAMEDML |
| RN1 refused rows by league, 7d (proxy for his mix) | itf 2453, mlb 2066, atp 1820, epl 1228, lal 800, wta 603, nfl 556, sea 550 | probe:2080-2094 WL |
| Venue-listed vs not, per league (all whales, lifetime rows) | atp listed=54 0ev=814 x404=25; itf 195/1476/606; wta 11/451/12; mlb 3707/100/50; epl 161/149/76 | probe:2118-2135 LGE |

Reading: tennis (itf+atp+wta = 4,876 of RN1's 10,076 league-attributed refused rows in 7d = 48%) is the biggest slice and the copy lane's own diagnosis says the venue mostly does NOT list it (0ev ≫ listed); MLB is the opposite (listed ≫ 0ev → our mapper's miss, and LGE says mlb rows are moneyline 5419 / total 4879 / spread 1861, so a large part is derivatives P1 would refuse anyway). Soccer per-team yes/no (epl, lal, sea, eflch…) is listed by the venue and refused by premap for wording (§2.f).

## 1. The exact mapping path and every way it returns None

### 1.0 Before mapping: which markets are even considered
- `active_conditions` (mirror_shadow.py:155-167): `WHERE lower(w.username)=$1 AND t.condition_id IS NOT NULL AND t.ts >= now() - 6h`, newest first. A trade whose `condition_id` is NULL (chain leg not yet enriched, or a token Gamma never knows: ingestion/pipeline.py:314-318 `no metadata for token … will enrich on refresh`) is INVISIBLE to the shadow — not counted unmapped, not counted at all. Size today: probe:2104 `NOSLUG rows=17215 catalog_has_token=2 token_unknown=17213` lifetime, but `LGE (no slug): 17215 (7d 1)` → negligible in the last week.
- `tick_once` (mirror_shadow.py:644-651): the per-tick cap (20) takes the NEWEST markets; a market cached in `_unmapped_until` (15 min, :73-74, set at :660-662) is skipped and counted `skipped_unmapped`. The 309 markets in the window are therefore the newest ~309 of his 6h activity, not a random sample; `skipped_markets` (381-430 per tick) never got read in the window at all.

### 1.1 `map_market(pool, fills)` (mirror_shadow.py:249-298) — returns None at
| # | line | condition | class |
|---|---|---|---|
| N1 | :256-258 | no `asset` on any fill | impossible in practice |
| N2 | :264-277 | live_orders rows on his tokens exist (`us_market_slug IS NOT NULL`, intent LONG/SHORT, newest 20) but `_choose_long` returns None | ledger-ambiguous |
| N3 | :280-282 | `from . import premap` raises | deploy fault |
| N4 | :288-297 | every per-token `premap.resolve(...)` returned None or lacked `market_slug`/`intent` | **premap miss (the 81%)** |
| N5 | :298 | premap answered but `_choose_long` returns None | premap-ambiguous |

The ledger query (:264-271) is the copy sleeve's own rows: `us_market_slug` is written only after a mapping succeeded (live_executor.py:8378-8381), on every path — premap, exact, yes/no exact, AND fuzzy. It does not read `mapping_src`; the copy lane itself trades only `QUARANTINE_RESUME_SRC = {"premap","exact"}` under quarantine (live_executor.py:2087). So the mirror's ledger source can hand it a fuzzy mapping the copy lane refused to trade (risk, not coverage).

Note the context passed to premap: `by_asset` takes the FIRST fill per token (:284-287) and passes `market_title, event_title, outcome, market_slug` — `outcome` is `t.outcome` only (no `COALESCE(t.outcome, mt.outcome)` as api_unmapped_census does at api/app.py:4913); `_norm(None)` → '' → `match_side` returns None (premap.py:1827-1829).

### 1.2 `_choose_long` (mirror_shadow.py:217-246) — None at
- :232-234 two BUY_LONG candidates on the SAME slug (ambiguous); :246 no LONG and no SHORT candidate.
- per_side=True (:235-237) only when both tokens resolve BUY_LONG on DIFFERENT slugs. Evidence that the venue's soccer per-team yes/no markets are NOT this shape: probe:1809-1838 NAMEDML-Q rows show side "yes" and side "no" both with identifier `atc-spl-neo-kha-2026-09-03-neo`; premap.side_intent (premap.py:2066-2073) names them LONG/SHORT from `long`/`marketSideType`. So a mapped soccer "Will X win?" condition yields {long=Yes token, other=No token}, per_side False — P1's `per_side_unsupported` (analytics/mirror_live_rules.py:436-437) does not bite there.

### 1.3 `his_fills` join (mirror_shadow.py:170-189)
`COALESCE(t.market_title, m.title)`, `m.event_title` (markets table only), `COALESCE(t.market_slug, m.slug)`, `t.outcome`. `trades.market_slug` is Gamma's market slug written at enrichment (ingestion/pipeline.py:323-334 `market_slug=$6` ← `meta["slug"]`; gamma.py:84-86). Whale-side shapes seen in the data-api rows salvaged from the corrupted blob (tee/w_rn1_probe_33670859137.txt.json, 26 rows): `elc-qpr-car-2026-09-02-car` title "Will Cardiff City FC win on 2026-09-02?" outcomes Yes/No; `mlb-sd-cin-2026-09-02-total-9pt5` "San Diego Padres vs. Cincinnati Reds: O/U 9.5"; tennis per tests/test_mirror_shadow.py:25-27 `atp-nakashi-michels-2026-09-02` title "US Open ATP: Brandon Nakashima vs Alex Michelsen", outcomes = player names.

### 1.4 `premap.resolve` (premap.py:2906-3026) — None at, in order
| step | line | trigger | RN1 shapes that hit it |
|---|---|---|---|
| R1 no date on his slug | :2938-2940 | `date_of(global_slug)==''` | slugless trades; undated event-level markets ("us-open-2026-winner" → probe run below: date='' keys=[]); TYP unknown 19428 lifetime (probe:2112) |
| R2 no keys | :2949-2950 | titles/slug yield nothing | rare |
| R3 no key intersection | :2960-2961 | `event_keys && $1` empty | venue does not list the game inside now-12h..now+96h, or key grammar: slug keys can NEVER meet on tennis (whale `atp-nakashi-michels-2026-09-02` vs venue `aec-atp-branak-alemic-2026-09-02`, live_executor.py:3471-3479), so tennis rides on TITLE keys only (`brandon nakashima vs alex michelsen@2026-09-02`, run below) which need the venue event title to carry both full names AND the same date. League aliasing measured 0/400 (probe:1795). |
| R4 unknown market type | :2969-2971 | `PREFIX_FOR_TYPE.get(market_type_of(slug))` None | exact_score (`elc-mot-dun-2026-09-02-es-3-0` → exact_score, want=None, run below), prop, unknown |
| R5 type prefix emptied | :2979-2981 | event captured, no row of that family | 43.8% of the copy-lane sample; e.g. `itc-udi-ven-2026-09-02-first-half-total-0pt5` → typed `total` (copy_sports.py:322-324 returns total before the >4-char guard) → "6 rows on this event, none with a ['tsc'] prefix" (probe:1801) |
| R6 no side match | :2982,:3009-3010 | `match_side` None (named lane dark: PREMAP_NAMED_LANE off, :2984-2985) | §2.f: soccer per-team yes/no wording + the phantom-line bug; tennis name spelling/ambiguity |
| R7 side has no intent | :3016-3021 | sweep could not name LONG/SHORT | GATEEDGE no_side_intent 480 rows/30d (copy lane) |
| R0 query failed | :2958-2959 | table absent/degraded | deploy fault |

### 1.5 The unmapped TTL cache
`_unmapped_until[(whale, cid)] = now + 900` (mirror_shadow.py:660-662) whenever `reason` startswith "unmapped"; checked at :649-651. Never invalidated by a premap fast-lane write (180 s cadence, premap.py:50-55) or a new ledger row — worst-case 15 min lag, acceptable. It is process-local (a restart forgets it) and unbounded in size (one entry per (whale, cid); ~1,300 assets/day → fine).

THE TERMINAL MEMO (D1, 2026-09-06, live worker only): `mirror_live._terminal_until[(whale, cid)] = now + ms.UNMAPPED_TTL_S` whenever a CANDIDATE's quote read (`_tick_candidate` → `_read_market` → `_bbo`) carried a state in `ms.STATE_TERMINAL` (`MARKET_STATE_EXPIRED`, `CLOSED`, `TERMINATED`, `MATCH_AND_CLOSE_AUCTION` — a market that has ended, a per-market fact); the candidate walk skips such a market under census `cand_terminal_skipped` until the TTL runs. Why: the walk spends its `MAX_MARKETS_PER_TICK` (20) reads newest-touched first, and his newest-touched mapped markets are matches he trades to settlement — the 19:02Z census read `venue_halted` on 26 of 29 candidate reads, every one `MARKET_STATE_EXPIRED`, so an open market behind them never reached a slot (25 expired candidates ahead of 1 open one: the first tick reads 20 expired as before, the second tick skips them and reads the open one; `tests/test_d1_fills_dedup.py`). NEVER written for `HALTED` / `SUSPENDED` / `PREOPEN` (those reopen, and keep counting toward the miss streak), NEVER from an existing book's read (`_tick_book` does not touch it: a book on an ended market is managed every tick until it closes), and never for a read that failed or named no state. Same TTL, same process-local, unbounded shape as `_unmapped_until`. `cand_terminal_skipped` is appended LAST in `CENSUS_KEYS` (past the served 40-key prefix) and does not ride on `integ`, which is at its 39-key ceiling: read it off the raw heartbeat row. The shadow's own walk keeps re-reading terminal markets (it measures; not changed here).

### 1.6 What the copy lane has that the mirror does not
live_executor.py:8184-8341, in order: premap.resolve → `_tennis_candidates(title, slug) + _us_slug_candidates(slug, outcome)` through `pmus.resolve_market_exact` (moneyline) / `resolve_derivative_exact` (spread,total) → his own slug verbatim (:8294-8301) → `resolve_team_yesno_exact` (:8309-8320) → `resolve_market` fuzzy (:8334-8341). The mirror runs ONLY premap.resolve (Postgres) + the ledger (mirror_shadow.py docstring :12-15 "no venue call"). Consequence: the mirror's coverage on tennis and soccer is bounded by (a) premap title keys and (b) whatever the COPY LANE already mapped into live_orders. Once RN1 is in mirror mode the copy lane stops at dispatch (`if mirror_mode(username): … return _copy_stop("mirror_mode", username)`, scratchpad/p1_panel_synthesis.md:300-302, and :304 "ABOVE every entry gate, sizing, the INSERT and both submit sites") and source (b) dries up for every NEW market — P1's own unmapped share will be HIGHER than the shadow's 81%, not lower, unless §4 lands first.

Sandbox proof of the candidate grammar (PYTHONPATH=backend, no DB, import in 0.2 s):
```
_tennis_candidates('US Open ATP: Brandon Nakashima vs. Alex Michelsen','atp-nakashi-michels-2026-09-02')
 -> ['aec-atp-branak-alemic-2026-09-02', 'aec-atp-alemic-branak-2026-09-02']
_us_slug_candidates('elc-qpr-car-2026-09-02-car','Yes') -> ['aec-elc-qpr-car-2026-09-02', 'elc-qpr-car-2026-09-02-car']
_us_slug_candidates('mlb-sd-cin-2026-09-02','San Diego Padres') -> ['aec-mlb-sd-cin-2026-09-02', 'mlb-sd-cin-2026-09-02']
```
and of premap's key construction on RN1 shapes (same run):
```
atp-nakashi-michels-2026-09-02  type=moneyline want={'aec','atc'} keys=['alex michelsen vs brandon nakashima@2026-09-02','atp-nakashi-michels-2026-09-02','brandon nakashima vs alex michelsen@2026-09-02','michelsen vs nakashima@2026-09-02','nakashima vs michelsen@2026-09-02']
elc-qpr-car-2026-09-02-car      type=moneyline keys=['cardiff city vs qpr@…','elc-qpr-car-2026-09-02','qpr vs cardiff city@…','will cardiff city fc win on 2026 09 02@2026-09-02',…]
mlb-sd-cin-2026-09-02-total-9pt5 type=total want={'tsc'}
itc-udi-ven-2026-09-02-first-half-total-0pt5 type=total (a 1H total typed as the game total)
elc-mot-dun-2026-09-02-es-3-0   type=exact_score want=None  -> R4
us-open-2026-winner             date='' -> R1
```

## 2. What the unmapped markets ARE

The shadow itself cannot say: an unmapped row carries `long_asset/other_asset` only (mirror_shadow.py:432-437) and the MIRRORMKT line prints `condition_id[:16]` (yml:1195); `summarize.latest` (analytics/mirror_report.py:69-74) drops `detail` (map source, per_side) and there is no title/slug/sport/notional column. The `latest 12` sort also under-represents unmapped markets: they are written once per 15 min (TTL) while mapped ones are rewritten every tick, so 11 of the 12 printed rows are mapped tennis/MLB and one is `0x2a43b8656d565a … unmapped` (probe:1715-1726). Classification therefore comes from the copy lane's own funnel on the same whale, same feed:

a. **Tennis singles moneylines the venue does not list** — RN1 7d: itf 2453, atp 1820, wta 603 refused rows; LGE says 0ev (search found no event) dominates listed by 15:1 (atp 814:54), 7.6:1 (itf 1476:195), 41:1 (wta 451:11). Exact-lane 404 trails: probe:1433 `unmapped: exact[aec-wta-taytow-taypre-2026-09-02 6x404,1xyn] slug:NotFoundError; event:20/20; search[…]:0ev`. Game-level, moneyline, plausibly NOT listed (ITF/challenger depth). This is the family P1 admits (MIRROR_FAMILIES={'moneyline'}, mirror_live_rules.py:255) and where RN1's shadow-mapped markets already sit (probe:1715-1726: aec-atp/wta/itfwo).
b. **Tennis the venue DOES list but premap misses** — key grammar (R3: slug keys never meet; title keys need the venue's event title = "First Last vs First Last" on the same date — venue shape evidence tests/test_pmus.py:285 `"Dalma Galfi vs Ella Seidel"`, tests/test_memory_census.py:45 `"Harry Wendelken vs Stefano Travaglia"` with `aec-atp-harwen-stetra-2026-08-24`), name spelling (R6: apostrophes, surname collisions), and DATE DRIFT between the two venues' slug dates for late-night ET matches (unmeasured — open question). The copy lane maps these via `_tennis_candidates` (first3+last3 grammar, live_executor.py:3453-3540) — the exact path the mirror lacks. The named premap lane that would do it network-free is dark and measured would_resolve=0 (probe:1805).
c. **Soccer per-team "Will X win on DATE?" (atc- family)** — RN1 7d: epl 1228, lal 800, sea 550 (+eflch, ecu1, scp, atbl, spl seen in MAPA lines probe:1428-1431, 1450-1456 as MAPPED by the copy lane → via `resolve_team_yesno_exact`/fuzzy, not premap). premap refuses them twice over (§2.f). Game-level, moneyline, venue LISTS them (NAMEDML-Q shows the rows in us_premap). This class becomes P1-relevant the moment the copy lane stops (§1.6).
d. **MLB/soccer totals, spreads, first-half totals, exact scores, "leading at half"** — R4/R5 (43.8% of the copy-lane sample; probe:1435 Motherwell exact score, :1440 Udinese 2-2, :1444 "Burnley FC leading at half" all `search …:0ev`). Outside MIRROR_FAMILIES anyway; some are not listed at all (derivative depth), some are listed but typed wrong (1H total → `total`).
e. **Event-level / undated** (tournament winners, futures) — R1; TYP unknown 19,428 lifetime rows across whales; no window can catch an undated slug. Not measured for RN1's last 24h; the probe classifies them.
f. **The soccer yes/no premap refusal, proved in the sandbox** (`PYTHONPATH=backend python`, premap + pmus only):
   - his title `Will NEOM SC win on 2026-09-03?` → `_lines_of` = ['03','09'] (the `-09`/`-03` of the ISO date match `_LINE_CTX` `[+-]\s*(\d+)`, premap.py:84-86), slug_lines=∅.
   - venue question (probe:1809) → `_question_line` = '' (dates stripped, premap.py:2118), `_questions_agree(norm(his), norm(venue))` = False (premap.py:351-355 needs equality/containment; "Will NEOM SC win on 2026-09-03?" vs "Will NEOM SC win against Al Khaleej Saudi Club in the Saudi Pro League match scheduled for Sep 3, 2026?").
   - `match_side(rows,'Yes',his_title,slug)` = None. With a DATELESS title it resolves; with an AGREEING question but the dated title it STILL returns None because `_yn_line_ok` refuses `bool(rl) != bool(his_lines)` (premap.py:1876-1877) on the phantom lines. Two independent blockers; the second is a bug (the whale side never strips dates the way the venue side does at :2118). probe:1804 UNMAPEG shows it live: `spl-neo-kha-2026-09-03-neo outcome=Yes … his_lines=['03','09']`.

Venue plausibility by class: (a) not listed (copy-lane search 0ev, exact 404) — cannot be mapped by any code; (b),(c) listed — mappable; (d) mostly not listed and out of family; (e) unknown.

Does RN1 trade "5 days out"? No evidence he does: 95.5% of his new assets carry a slug date within yesterday..tomorrow (WHALERATE med_playable/med_new = 1206.5/1263.5), the copy-sweep pool has future=0 (probe:1590), and the premap board walk is COMPLETE at 17 of 120 pages over now-12h..now+96h (probe:1393; premap.py:2241-2245, 2457-2469). Widening the board window is not a lever. The `past=971` bucket (game date before today) is in-play/finished games still in the sweep's retry pool; the sweep's back_h=12 covers in-play (premap.py:2241) and PRUNE_HOURS=26 (premap.py:65) ages the rest.

## 3. Share of his DOLLARS in the unmapped set

Not computable from any artifact this sandbox can read (see header). What exists:
- Denominator: RN1 BUY notional 2026-09-02 = $2,400,134.97 (probe:1548; RN1-only per api/app.py:7110,7180 `sum(t.notional)`), 1,255 distinct assets; 12-day medians 11,360 trades / 1,328.5 assets per day (probe:1544).
- Concentration: 86.8% of his 30d stake is in lots ≥ $250 (probe:1686), i.e. the dollar answer is decided by a few hundred markets/day, not by the count — a count-based 81% can be anything in dollars.
- The mirror knows his_net only for MAPPED markets (mirror_shadow.py:438-441); unmapped rows carry no size. `mirror_shadow` has no notional column (migrations/046_mirror_shadow.sql:33-62).
- The copy lane's gate-edge cohort DOES carry dollars per gate (`roi_with_ci` returns `staked`, analytics/proof.py:114-117; `admin_gate_edge` api/app.py:5635-5646) but the probe prints only refused/share/roi (yml:1215): `GATEEDGE unmapped: refused=45715 share=0.6515` — its `.gates.unmapped.staked` vs `.taken_at_his_price.staked` is a one-line jq away (copy-lane rows scored at HIS price, 30d, whale-filterable with `?whale=rn1`).
- The MIRRORCOVER step (§5) computes the true number from the data-api (`usdcSize` per fill, field confirmed in the salvaged rows) joined to the shadow's map/unmap verdict.

## 4. Per failure class: the code change that maps it, or the proof it cannot be mapped

| class | mappable? | change |
|---|---|---|
| (a) tennis not listed on US | No — venue gap (search 0ev + exact 404 on both player orders and all tour codes) | none; the probe must PROVE the gap per market (§5) so the 81% splits into "cannot" and "did not" |
| (b) tennis listed, premap miss | Yes | 1) `map_market`: after premap, run the copy lane's exact lane — `_tennis_candidates + _us_slug_candidates + [his slug]` through `pmus.resolve_market_exact`, paced via `venue_pace.pace(READ_PACING_S)` and behind `_unmapped_until` so a miss costs ≤ 4-6 reads per 15 min per market; source label "exact" so P1's `mapping_ok` reads the same class the copy lane trades under quarantine (`QUARANTINE_RESUME_SRC`). 2) Move `_abbrev_player/_tennis_candidates/_us_slug_candidates` (live_executor.py:3448-3577) into a pure module (copy_sports.py) so the mirror, the copy lane and the runner import ONE grammar. 3) premap side: at sweep time also emit event keys from the SIDE DESCRIPTIONS ("<a> vs <b>@date" both orders from `marketSides[].description`, premap.py:2149-2165) so a venue event whose title is not "A vs B" still meets the whale's title key; flip the named lane only after its audit reads zero-mismatch (its own gate, premap.py:2984-2999). |
| (c) soccer per-team yes/no | Yes | 1) bug fix: `match_side` computes `his_lines` from `_lines_of(_QDATE_RE.sub(" ", his_title))` (mirror the venue-side stripping at premap.py:2118) — the sandbox run shows an agreeing question then resolves. 2) wording: widen `_questions_agree` for the yes/no branch by the closed template the executor already validates (`resolve_team_yesno_exact` W-gates, pmus.py:1218-1258: his slug `<lg>-<a>-<b>-<date>-<t>` + "Will <team> win" subject) — or, cheaper and already-reviewed, call `pmus.resolve_team_yesno_exact` from `map_market` on the moneyline+`Yes/No` shape exactly as live_executor.py:8309-8320 does, paced and TTL-cached. |
| (d) derivatives | Partly listed, out of family | none for P1 (family gate); fix the typing leak `first-half-total` → `total` (copy_sports.py:322-324 runs before the `>4-char unknown-word` guard at :343) so a 1H total is `prop`, not a game total — a wrong-market guard, not coverage |
| (e) undated / event-level | Unknown | probe classifies; premap cannot (R1 by design, premap.py:2915-2937); if the venue lists futures, a separate dated-key-free lane with its own audit — not P1 |
| N2/N5 ambiguity | n/a | keep refusing; count it (add `detail.map_refusal`) |
| §1.0 NULL condition_id | Yes | none needed now (7d 1 row); the probe counts it |
| §1.6 copy lane stops in mirror mode | Yes | the mirror must own its mapping (rows above) BEFORE `mirror_mode` flips for RN1, or keep the copy lane's MAPPING (not its orders) running for mirrored whales: dispatch after `us_market_slug` is written, i.e. move the `mirror_mode` stop below the mapping block (live_executor.py:8184-8381) so live_orders keeps carrying the mapping the mirror's ledger path reads |
| measurement | Yes | `shadow_market` unmapped branch: write `detail={'his_slug','title','event_title','sport','family','explain':resolve_explain(...)['step'],'notional_6h'}`; `summarize.latest` include `detail`; MIRRORMKT print `\(.detail.family)/\(.detail.explain)`; add per-family unmapped split the P0→P1 gate needs (p1_panel_synthesis.md:441 "unmapped share of his tennis moneyline activity ≤ 20%") |

Risk note on the ledger source: `map_market` trusts any live_orders row with `us_market_slug`, including rows the copy lane mapped by the fuzzy resolver and then refused (`quarantined: … (src=fuzzy, slug=…)`, live_executor.py:7349-7358). Filter `AND COALESCE(error,'') NOT LIKE 'quarantined%'` or persist `mapping_src` on the row and require it in `QUARANTINE_RESUME_SRC ∪ {'yesno_exact'}`.

## 5. MIRRORCOVER — runner-side probe step (own job, like `side-truth`, yml:3760-3767, because the probe job is at its 32-minute budget, yml:49-62)

Inputs: the checkout (yml:67), `pip install polymarket-us` (+ `pip install -e backend` until the grammar lives in a pure module — live_executor imports asyncpg/settings at module level, live_executor.py:36-41), `ADMIN_TOKEN`, `BASE` (yml:70-75).

Step A — server facts, one call: `GET $BASE/api/admin/mirror-cover?whale=rn1&hours=24` (new, read-only; ~40 lines beside `admin_mirror_shadow` api/app.py:5663-5672): for every condition in `active_conditions(pool,'rn1',24)`: `condition_id, his_slug, event_slug, title, event_title, outcomes, sport, family=market_type_of(his_slug), buy_usd_24h=sum(notional) FILTER side='BUY', n_fills, map=map_market(...)→{source,us_slug,per_side} or None, explain=resolve_explain(...)['step'/'detail'] when None, ttl_cached=bool`. Fallback when not deployed: crawl `https://data-api.polymarket.com/activity?user=0x2005d16a84ceefa912d4e380cd32e7ff827875ea&limit=500&offset=N` (the weekly-report crawl, yml:2311-2331; fields conditionId, slug, eventSlug, title, outcome, side, type, usdcSize confirmed) until `timestamp < now-24h`, aggregate per conditionId, and read `us_market_slug` per condition from `$BASE/api/admin/mirror-shadow?hours=24&whale=rn1` (source unknown → print `mapped_?`).

Step B — venue index, no per-slug calls: `pub.events.list({"limit":100,"offset":k*100,"active":True,"closed":False,"startTimeMin":now-12h,"startTimeMax":now+96h})` — the sweep's own rung (premap.py:2280-2283) — paged at 0.35 s until a short page (17 pages today ≈ 6 s): dict `slug→market`, `event_keys_for(title, slug)→event`, and every `marketSides[].description`.

Step C — per condition classify (Python, on the runner):
```
cands = _tennis_candidates(title, his_slug) + _us_slug_candidates(his_slug, outcome) + [his_slug, "aec-"+event_slug, "atc-"+event_slug]
hit = first cand in index  |  else title-key hit (event_keys_for(title,his_slug) ∩ index keys)
if map: line = mapped_ledger | mapped_premap (map.source)
elif hit and not hit.closed: line = listed_on_us_but_unmapped:<explain.step>:<slug_hit|title_hit>
elif hit and hit.closed: listed_closed
else: for cand in cands[:3]: retrieve_by_slug (pace 0.35s; cap 150 markets ranked by buy_usd_24h desc)
      -> found: listed_on_us_but_unmapped:<explain.step>:direct ; all 404 and search.query(title) 0 events: not_listed_on_us
```
Output, one line per market, then totals:
```
MIRRORCOVER <class> rn1 <cid[:10]> his=<his_slug> us=<us_slug|-> sport=<> family=<> usd24h=<0.00> fills=<n> why=<explain.step|-> hit=<slug|title|direct|-> dates=<his_date>/<venue_date>
MIRRORCOVER-TOTAL markets=<n> usd=<sum> mapped_ledger=<n>/$<usd> mapped_premap=<n>/$ listed_unmapped=<n>/$ not_listed=<n>/$ listed_closed=<n>/$ undated=<n>/$ null_condition=<n>
MIRRORCOVER-CELL <sport>|<family> markets= usd= mapped= listed_unmapped= not_listed=
MIRRORCOVER-WHY <explain.step>: markets= usd=      (for listed_unmapped only)
```
Budget: index ≈ 6 s; data-api ≤ 12 pages × 0.4 s; ≤ 150 × 3 direct reads × 0.35 s ≈ 160 s; `timeout-minutes: 10`. Every venue read goes through the same 0.35 s pacing the sweep uses (premap.py:64, the 2026-08-23 429 fix).

Acceptance for the P0→P1 coverage gate (addendum §3): `MIRRORCOVER-CELL tennis|moneyline` listed_unmapped/(mapped+listed_unmapped) ≤ 20% by markets AND by usd; `not_listed` is reported beside it and is not ours to fix.

## 6. Open questions the probe answers
1. Dollar share of (a) vs (b)+(c) — the only number that says whether the 81% matters.
2. Venue tennis event-title shape and slug-date agreement with his slug date (late-ET matches) — decides whether title keys can carry tennis without the exact lane.
3. How many of the 56 mapped came from the ledger vs premap (`detail.map` exists in the row, not in the endpoint) — decides how much coverage disappears when `mirror_mode` stops the copy lane.
4. Whether the venue lists any undated/event-level market RN1 trades (class e).

## 7. C1 (2026-09-06) — the mirror maps what the copy lane already maps

Owner order 2026-09-06 ~14:00Z: "Let's remove those caps so we start copying his actual book." Shadow probe 16:37Z: 1,217 of his markets in 6 h ($2.15M of his flow) unmapped — no_side_match 577/$1.68M, no_key_intersection 376/$329k, type_prefix_filter_emptied 165/$72k, unknown_market_type 99/$68k — while the per-fill copy lane (§1.6) had been mapping the same feed through four more resolvers. Patch C1 (against 78da1a1); tests in `backend/tests/test_mirror_maps_the_copy_lane.py`.

### 7.1 What the mirror maps now, in order

`mirror_shadow.map_market(pool, fills, pmus, whale=, condition_id=, budget=, out=)` — the one mapper both the shadow (`shadow_market`) and the live worker (`mirror_live._tick_candidate`) call; `analytics/mirror_report.mirror_cover_report` still calls it without a venue module and stays table-only.

| # | step | source | where |
|---|---|---|---|
| 1 | our own ledger rows on his tokens (unchanged) | `ledger` | `map_market` |
| 2 | `premap.resolve` per token (unchanged; `PREMAP_YN_IDENTITY=on` still answers here as `premap`) | `premap` | `map_market` |
| 3 | `_tennis_candidates(title, slug) + _us_slug_candidates(slug, outcome)` through `pmus.resolve_market_exact` — moneyline only; one candidate per call; or `pmus.resolve_derivative_exact` + `_dh_sibling_guard` for spread/total | `exact` | `map_lane.exact_lane` |
| 4 | his slug verbatim through `resolve_market_exact` — moneyline only | `exact` | `map_lane.exact_lane` |
| 5 | `pmus.resolve_team_yesno_exact` + `_dh_sibling_guard_ml` — moneyline only | `yesno` | `map_lane.exact_lane` |
| 6 | MIRROR-ONLY: the aec code-order side (`map_lane.aec_code_side`), run only when step 3 reported the `aec-` candidate LISTED and refused its side | `grammar` | `map_lane.exact_lane(grammar=True)` |
| — | the fuzzy `pmus.resolve_market` | never | — |

Steps 3–5 are the copy lane's own (live_executor's copy block, same order, same gates, same 20 s box per call). The copy block itself is byte-identical to 78da1a1: its own tests pin its text (`test_exact_resolver_diagnostics` counts three `_ex_diag.append("timeout")`, `test_identity_slug_is_exact` pins `[src_slug], ctx.get("outcome")`, `test_live_executor_mapping` pins the tennis call within 600 chars of `mtype == "moneyline"`), so it does not call `map_lane`; instead `test_the_copy_lane_and_the_shared_lane_make_the_same_resolver_calls` drives BOTH with the same recording resolvers on tennis, college football, per-team yes/no, total and spread contexts and asserts identical resolver calls in identical order on identical candidates, the copy lane's list being exactly one entry longer (its fuzzy step). Either side moving fails that test.

Per market the lane runs over his tokens larger position first and stops when one token's answer names both sides (an `aec-` identifier — both sides share it, the other token is the other intent — or a yes/no contract); a per-side family (distinct identifiers) reads both so `_choose_long` can pick his directional side. When the two tokens' sources differ the market's source is the LESS certified one (`MAP_SOURCE_RANK`).

### 7.2 The college-football side (`grammar`)

The venue's own payload for `aec-cfb-bayl-aubrn-2026-09-05` (probe 2026-09-03, the account's positions row) has `marketMetadata.title = "Baylor vs. Auburn"` — the SCHOOLS, in slug order — and `outcome = "Bears"`: the sides are the mascots. His feed says "Baylor". Step 3 finds the market by slug and refuses the side by name — correctly: the copy lane's `aec-` side rule is name similarity on `marketSides[].description` plus the venue's long marker (`pmus.resolve_market_exact` :1936-1961), it has NO team-code or order rule, and the 2,012 echoes on `side_echo_last` certify name+marker, not order. `map_lane.aec_code_side` (round-2 rules, review A/B) chooses the side from HIS OWN signal, never from the mascot, and every fact must agree: his outcome names exactly one of his slug's two codes by the WHOLE-WORD / FIRST-WORD rule (`code_hits`: the code is a whole word, or the first word starts with it, or it starts with the first word (≥3 letters); no substring, no subsequence — "Michigan State" hits `mich` by its first word and the index refuses it; `aubrn` never names Auburn); his `outcome_index` is PRESENT and equals the code's position (mandatory: `side_code_no_index` when absent); the venue slug echoes his under `aec-` with exactly two sides sharing it; the intent is the venue's long/short marker on the side at that position. His OTHER token, when in the fills, is judged by the pair rule (`pair_agrees`): complementary `outcome_index` and no claim on the mapped code, no venue read — Auburn (index 1, names neither code) is the other side of Baylor's contract by construction. Refusals, all on the row (`detail.explain`) and in MIRRORUNMAP's `why:`, and ANY of them on either token refuses the whole market (review E): `side_code_unmatched` (names neither code — "Sooners"; pair-resolvable only when the sibling maps), `side_code_ambiguous` (both codes as whole words), `side_code_no_index`, `side_code_conflict` (index ≠ position; his slug's own side token names the other team; a venue side's description names the other position's code and not its own, or names BOTH codes; the venue's matchup title names the codes in the opposite order), `side_code_pair` (the sibling's index is not the complement), `side_code_shape`, `side_code_nointent`. What remains unproven at mapping time — that `marketSides[i]` is the team at slug position i — is exactly what the class's live certification checks against venue-only truth (7.7).

### 7.3 Venue budget, pacing, cache

Every resolver call goes through `venue_pace.pace(READ_PACING_S)` (the `_paced_bbo` gate; one slot per call — `resolve_market_exact` is called one candidate at a time so each slot is one lookup; a `resolve_team_yesno_exact` call's own ≤2 lookups ride one slot) and counts one unit of the tick's `MapBudget`. THE KNOB, exactly: `mirror_shadow.MAP_READS_PER_TICK = int(rules.capped_env("MIRROR_MAP_READS", 10.0, floor=0.0))` — env name `MIRROR_MAP_READS`, default 10 resolver calls per tick, a shell may only LOWER it (`50` reads 10, `3` reads 3, junk reads 10; pinned by `test_the_environment_may_only_lower_the_map_read_budget`). Past it the market gets NO verdict this tick: shadow row reason `map reads capped: no verdict this tick`, `detail.explain = map_reads_capped`, never TTL-skipped, read again next tick; live: `_mirror_stop("map_reads_capped")`, no `_unmapped_until`. Verdicts (hit or miss) are remembered per (whale, condition_id) per token for `MAP_CACHE_TTL_S` = 900 s (`_map_cache`), and a candidate slug the venue answered 404 is remembered under the same TTL (`_slug_404_until`) so a lane the budget cut resumes where it stopped; a market the budget cut gets NO entry, and both memories are pruned on the TTL at every map (`_prune_maps`, review C). The `aec-` payload is read once per market per call for both tokens. Shadow stats: `mapped_by {ledger, premap, exact, grammar, yesno}`, `map_venue_reads`, `map_cache_hit`, `map_reads_capped`; row detail: `map_venue_reads`, `map_cache_hit`, `map_lane` (`unavailable` when the venue module carries no resolvers, or the exception name). Live census (appended to `CENSUS_KEYS`, past the served 40-key prefix): `map_reads_capped`, `map_source_unverified`, `map_venue_read`, `map_cache_hit`, and the grammar class's own `grammar_echo_unreadable`, `grammar_tripped`, `grammar_probation`, `grammar_echo_unverified`, `grammar_echo_ok`, `side_echo_mismatch`; SERVED on `integ` (review D, (4)): `map_source_unverified`, `map_reads_capped`, `side_echo_mismatch` (the block is 39 keys, under the sanitizer's 40).

### 7.4 Which sources the live mirror admits, and why

`mirror_books.map_source` (047, TEXT, no CHECK) now carries `'ledger' | 'premap' | 'exact' | 'yesno' | 'grammar'`. `_tick_candidate` refuses any source outside `mirror_live.MIRROR_LIVE_MAP_SRC = {ledger, premap, exact, yesno}` that is not `grammar` by name (`map_source_unverified`); `grammar` goes through its own certification first (`_grammar_admission`, 7.7) and then, like the four others, through `_admit_source` → `le._mapping_admitted` (open and every increase, `_increase_recheck` reads `book.map_source`; a grammar book's increase also re-reads the class's trip):

| source | while the mapping quarantine holds | quarantine lifted | why |
|---|---|---|---|
| `premap`, `exact` | admitted on the resume lane (`QUARANTINE_RESUME_SRC`, `LIVE_PREMAP_WHALES`, `premap_live`) | admitted | the copy lane's own resume classes; `exact` is the same `resolve_market_exact`/`resolve_derivative_exact` output the copy lane trades |
| `yesno` | refused by name: `quarantined: mapping class unverified … (src=yesno, …)` | admitted | the copy lane's `yesno_exact`: refused-but-recorded there too and certified under `YESNO_CERT_KEY`; the mirror goes live on it the same day the copy lane does (a one-token change to `QUARANTINE_RESUME_SRC`, or the quarantine lifting) |
| `ledger` | refused (not in `QUARANTINE_RESUME_SRC`) | admitted | unchanged |
| `grammar` | admitted on the resume lane's switches (`_mapping_admitted` as for `exact`: circuit, verified set, hold, `LIVE_PREMAP_WHALES` + `premap_live`; the refusal text says `src=grammar`) AND ONLY under its own certification (7.7): venue-truth check ok before the open, one book at a time until its first fill is echoed, refused `grammar_tripped` after any mismatch | the same: the certification is not a quarantine clause | the copy lane has no side rule this class could inherit, so it earns its own — every gate is a census name and the trip is `side_echo_mismatch`, served on `integ` |

`analytics/mirror_report.ADMISSIBLE_SRC` stays `{premap, exact}` (pinned equal to `QUARANTINE_RESUME_SRC`), so MIRRORSRC's `admissible=` is the share P1 can open today; `yesno` and `grammar` appear in `by_source` beside it.

### 7.5 Explain names and the two measurement changes

`explain_unmapped(pool, ctx, refusal)`: a lane refusal (`side_code_*`, `map_reads_capped`) names the row; otherwise premap's own step, unchanged — a market the new steps also fail keeps `no_side_match` etc., and a market they MAP is no longer in MIRRORUNMAP by construction. `unknown_market_type` now carries its split as `unknown_market_type:unparsed` (the slug grammar named no family — a parser gap) or `unknown_market_type:family_not_listed` (prop / exact_score / btts / crypto — a venue-family gap); `premap.resolve_explain` keeps `step == "unknown_market_type"` (the copy lane's census buckets on it) and adds `split`. `his_fills` reads `COALESCE(t.outcome, mt.outcome)` (and `outcome_index`) from `market_tokens`, as `api/app.py`'s unmapped census does, so a chain row not yet enriched is not filed as `no_side_match`.

### 7.6 What this does NOT map (left out, by design)

- Tennis the venue does not list (§2.a): every candidate 404s; `premap`'s name stays. The 404 memo makes the second tick free.
- `bun-wer-lei-2026-09-05-lei` ("Will RB Leipzig win on 2026-09-05?"): the copy lane's yes/no lane refuses it pre-network (`yn:title-code`: "rb leipzig" does not prefix `lei` under `_code_prefix_hit`), so the copy lane only ever mapped it through FUZZY. The mirror never runs fuzzy; the network-free mapper for this row is premap's identity branch (`PREMAP_YN_IDENTITY=on`, scoping lever #1, source `premap`, tested here) — the owner's flip, not a code change.
- LA LIGA, as his feed actually spells it (probe MIRRORUNMAPMKT 2026-09-06: `lal-bet-rea-2026-09-04-bet`, "Will Real Betis Balompié win on 2026-09-04?"; venue SHORTROW: `atc-lal-bet-rma-2026-09-04-bet`): the two feeds spell Real Madrid's code differently (`rea` vs `rma`). The identity branch needs `atc-` + his slug byte for byte; the yes/no lane builds `atc-lal-bet-rea-…-bet` / `atc-lal-rea-bet-…-bet` (both unlisted) and refuses his title first (`yn:title-code`: "real betis" does not prefix `bet`, and "Real Betis Balompié" would not either). NEITHER path maps it; premap's `no_side_match` stands (its title keys do find the venue rows). With the coordinator's spelling (`lal-bet-rma-…`, title "Will Real Betis win …") the identity branch maps it (source `premap`) and the yes/no lane still refuses at `yn:title-code` — the wording that lane needs is a subject whose collapsed distinctive form starts with the code ("Betis Sevilla" would pass; "Real Betis" cannot: `real` is not one of the ten `GENERIC_CLUB_TOKENS`). What would map the feed's row is a code alias read off the venue's own row (his side code + league + date pin the venue slug `atc-lal-<a>-<b>-<date>-bet` uniquely; the opponent's venue code is then READ, never guessed) — a C2 item, not in this patch.
- Derivative families map (`exact`, with the doubleheader guard) but their books still answer to `rules.admission`'s family gate as before.
- `mirror_cover_report` (MIRRORCOVER) is unchanged and table-only.

### 7.7 Round 2 (2026-09-06 18:25Z, "college football and la liga should be very easy to map"): the grammar class goes live under its own certification

**What the copy lane's side rule is, exactly.** `pmus.resolve_market_exact` :1936-1961 on a two-sided market: score each `marketSides[].description` against his outcome (`_outcome_score`, name similarity), take the unique side at or over `MATCH_FLOOR`, intent from `order_intent_for` → `premap.side_intent` (the venue's `long` marker). The post-fill echo (`_side_echo_verify` :5611) re-derives through `premap.match_side` (name-based), then checks the position SIGN against the intent (sole-leg attribution), then `_independent_check` (name-based). No code rule, no order rule: on mascots every one of them abstains or refuses. So `side_echo_last ok=2012` certifies "name + marker", and `grammar` cannot inherit it — this is the branch the coordinator's order names ("if the copy lane has NO side rule for aec-, say so").

**The certification (`mirror_live`), every step a census name:**
1. `_grammar_admission` before the open: the state key `mirror_grammar_echo` readable (else `grammar_echo_unreadable`), not tripped (else `grammar_tripped`), no other grammar book still awaiting its first-fill echo (else `grammar_probation` — one at a time, never TTL-skipped), then VENUE-ONLY TRUTH (`map_lane.grammar_truth`, review (2)): the venue's per-side contract for the chosen code, `atc-<lg>-<a>-<b>-<date>-<code>` (`contract_slug`), must name in its own `outcome`/`title`/`team` fields EXACTLY the `aec` side description the rule chose (`Bears`). Both strings are the venue's; agreement pins side i to the code with no order assumption. Unlisted → `grammar_echo_unverified` (refused, nothing tripped); listed and different → `side_echo_mismatch` (refused, the class TRIPPED). Ok → `grammar_echo_ok`, the chosen side recorded under `pending[slug]`.
2. `_admit_source`: the resume lane's own gates through `le._mapping_admitted(pool, w, "exact", slug)` — circuit, verified set, hold, `LIVE_PREMAP_WHALES` + `premap_live` — with the refusal text renamed `src=grammar`. `grammar` never joins `QUARANTINE_RESUME_SRC`.
3. `_grammar_fill_check` in `_tick_book`, every tick the book holds shares until verified: the venue's positions payload (`_position_echo`, `pmus.position_side`'s walk with `marketMetadata` kept) must name the recorded side (`outcome == "Bears"`) with the book's sign, `|net|` within 5%+1 of our booked fill (the copy lane's sole-leg rule; else `grammar_echo_unverified` and the book waits). Ok → the book joins `verified` (the next grammar book may open); mismatch → `_freeze(book, "side_echo_mismatch")`, open order cancelled, the class tripped; `_increase_recheck` on any grammar book re-reads the trip.
Clearing a trip is an admin write to `mirror_grammar_echo` (`tripped=false`, `mismatch=0`) — no shell switch exists on purpose.

**Live-admitted sources after round 2 and the evidence for each:** `premap` and `exact` — `QUARANTINE_RESUME_SRC`, the copy lane's 691/0 shadow-cert streak and `side_echo_last ok=2012`; `yesno` — the copy lane's `yesno_exact` under `YESNO_CERT_KEY`, refused while the quarantine holds; `ledger` — the copy lane's own traded rows; `grammar` — the venue-truth check per market before the open plus the first-fill echo per book, counted on `mirror_grammar_echo`, both unfalsified in the suite only (no live count yet: `ok=0` until the first CFB book).

**Expected mapping rates (the shadow's unmapped list, last 6 h, probe 1577 — the endpoint prints the 12 largest by dollars, the rest is not readable from here):** `cfb-*`: 0 of the 12 printed rows; the scoping's four cfb samples (`cfb-bayl-aubrn`, `cfb-ucla-cah`, `cfb-boise-ore`, `cfb-unlv-hawaii`) all map by grammar when his outcome names a first-word code and the index agrees (Baylor/`bayl`, UCLA/`ucla`, Boise State/`boise`, UNLV/`unlv`, Hawaii/`hawaii`, Oregon/`ore`) — 4/4 in the shadow; LIVE they open only where the venue lists the per-side `atc-cfb-…-<code>` contract naming the mascot side (the venue does list per-side cfb contracts: `atc-cfb-hawaii-stan-2026-08-29-h` in the probes), so the live rate is the venue's per-side listing rate, unknown from here. `lal-*`: 5 of the 12 printed rows ($189k of $956k) — the two moneylines (`-bet`, `-rea`), two totals and a spread — and 0 of 5 map now: every one carries the feed's `rea` where the venue has `rma` (the totals' `tsc-lal-bet-rea-…` and the spread's `asc-…` candidates are unlisted for the same reason). The C2 code-alias rule above would recover the two moneylines through `yesno`'s admission class and, with the same alias in the derivative candidates, the three derivatives through `exact`.

### 7.8 Round 3 (2026-09-06, conditional review of round 2)

1. **Single-token fills** (`mirror_shadow._catalogue_pair`): nothing in a one-token fill pins the feed's `outcome_index` order to the slug's team order, so a first-word collision ("Michigan State" naming `mich`) would map the wrong side on the index alone. A grammar map with no sibling in his fills now needs the market's own `market_tokens` rows: exactly the condition's two tokens, the mapped token at the code's position, the sibling at the complement with no claim on the mapped code (`pair_agrees` on the catalogue row) — else `side_code_pair`; an unreadable catalogue or no `condition_id` refuses the same way. Both tokens in the fills: the pair rule reads the fills, the catalogue is never read.
2. **`grammar_truth` trips only on the other side's name**: `mismatch` (refuse + trip) only when the per-side contract names `sides[1-i].description` in its outcome/title/team fields; a yes/no-shaped contract, a name that is neither side's, or no name at all is `unverified` (no book, no trip).
3. **The venue's own suffix** (`mirror_live._contract_candidates`): the per-side contract is found in `us_premap` (`identifier LIKE 'atc-<lg>-<a>-<b>-<date>-%'`), single-token suffix only (segment/prop rows never fit), fitting his code exactly, or prefixing his code and not the other's (`…-h` for `hawaii` beside `stan`), or whose question names the aec side description whole AND whose suffix does not fit the other side (§10.7, the certification fix); EXACTLY one distinct slug, else `grammar_echo_unverified`. When the sweep lists no row, his own code's slug (`contract_slug`) is the one candidate the venue must confirm.
4. **A book whose first fill is unverified WAITS**: `_grammar_fill_check` answers `ok | wait | frozen`; an unreadable certification state, an unreadable or unattributable echo, or a missing `pending[slug]` record is `wait` — `_tick_book` writes a `grammar_echo_unverified` no-plan and the book is neither traded further nor frozen; a missing record can never trip.
5. Pinned: "Bayl Aubrn" (index 0) beside "Aubrn" (index 1) refuses `side_code_ambiguous`; a one-letter first word ("A&M Aggies") never names a code.

### 7.9 Round-3 review fold (2026-09-06 19:4xZ, landed with round 3)

Two fixes from the adversarial review of round 3, both fail-closed, both pinned in
`tests/test_c1_round3.py`:

- `mirror_shadow._catalogue_pair`: a catalogue sibling whose `outcome` is NULL/blank is
  `side_code_pair`. The catalogue's indices come from the same feed as his fill's, so only the
  sibling's NAME pins the feed's index order to the slug's team order; with it empty, the pair
  rule would pass on the index complement alone and a swapped feed order opens the wrong side.
- `mirror_live._contract_candidates` takes the OTHER side's description (read off the aec
  market's `marketSides`) and its by-question fit requires the question to name our side and
  NOT the other's: the opponent's per-side row ("Will Tigers win against Bears?") no longer
  fits ours, so a sweep that lists only the opponent's row cannot become a false trip.

Residual, accepted and dated: a grammar book whose first-fill echo stays `wait` plans nothing,
reductions included (only the closing auction and `_maybe_close_episode` run before the hook);
a grammar book that cannot certify cannot follow his exit until the echo reads. To revisit when
the first grammar book has opened (none has; college football is the class, and Sunday lists
three games).


## 8. C2 (2026-09-06) — the per-team yes/no identity branch maps his soccer

Owner 18:25Z: "I need you to map more of his trades ... la liga should be very easy to map"; 18:57Z: "we need more volume". In the two hours to 19:04Z the shadow read 260 markets ($544k) and mapped 3; the bulk was soccer per-team yes/no. The venue's own rows for five of his events (preset `premap-rows`, 19:09Z; 216 `atc-` rows) reproduced every refusal offline. Patch C2 against 2dc3204 (C1 rounds 1-3 + the review fold); tests in `backend/tests/test_c2_yesno_alias.py` (the dump's per-team and draw rows are embedded verbatim). Every admission below is IDENTITY (byte-equal team codes, date and side token, plus name corroboration on both teams) — never fuzzy; `PREMAP_YN_IDENTITY=on` still gates all of it, and the wording arm is byte-identical.

### 8.1 The four gates that refused the seven feed rows, and what each does now

| gate | before | now | where |
|---|---|---|---|
| league slot | `_yn_slot_bad` ran the CLUB scope screen on the `lg` group: the single-letter rule refused `serie a` — every Serie A market | `pmus._yn_league_slot_bad`: non-empty, ≤ 5 tokens, one token of ≥ 3 letters, no scope token/stem (bridge list + `_YN_SCOPE_EXTRA` + `_YN_LEAGUE_SCOPE`: primavera, youth, u21, esoccer, srl, friendly …), adjacent-run joins; a single letter only as `a` or before `league` (`k league 1`); a lone digit is a league number | `pmus.py` (`_YN_LEAGUE_SCOPE`, `_yn_league_slot_bad`), used by `premap._yn_team_row/_yn_draw_row` AND the copy lane's `resolve_team_yesno_exact` (`yn:league-slot`) |
| names with digits | `_YN_Q_PATTERNS` slots were `[a-z ]+?`: `Bologna FC 1909` never matched (Mainz 05, 1899 Hoffenheim …) | the three slots are `[a-z0-9 ]+?`; digits stay in the name and go through `_yn_name_match` as tokens (`1909 ≠ 1919`) | `pmus._YN_Q_PATTERNS` (still one template) |
| diacritics | `_norm` is NFKD→ascii-ignore: `Tromsø` → `troms` (ø, ł, đ, ß have no decomposition) and `_folds_away` refused the title outright | `pmus.fold_latin` maps those letters explicitly (ø→o, ł→l, đ→d, ß→ss, æ→ae …) and deletes apostrophes (`Newell's` → `newells`, not `newell s`); applied before `_norm` on the yes/no name channels (his anchor `_bridge_title_subject`, the venue subject/opponent/draw slots, the copy lane's W2/W4/W7) and in `event_keys_for` on both sides; `_folds_away` reads through it, so a letter OUTSIDE the table still refuses by name. `_norm` itself is untouched (it produces `side_norm`, half of the `us_premap` unique index) | `pmus.py` (`_LATIN_FOLD`, `fold_latin`, `_norm_folded`), `premap.py` (`_folds_away`, `_bridge_title_subject`, `event_keys_for`) |
| league code | `_yn_his_identifiers` = `atc-` + his slug byte for byte; his `nor`/`arg`/`pol`/`fl1`/`por` are the venue's `els`/`lpa`/`ekst`/`lg1`/`lpb` | the alias rule below (`premap._yn_alias_pick`) | `premap.py` |

### 8.2 The alias rule (no table)

`_yn_pick(rows, outcome, his_title, his_slug, his_event_title)`: identity first (unchanged); then, ONLY when no row carries his own identifier, a row `atc-<LG>-<a>-<b>-<date>-<t>` is his when ALL of: (i) his slug parses as `<lg>-<a>-<b>-<date>-<t>` with `t ∈ {a, b}` and the venue row carries exactly that suffix in that order; (ii) exactly ONE venue league code carries the suffix among the rows his keys fetched — two is `yn:league-ambiguous` (a same-day women's/youth/esoccer twin lands here even though its own league slot would refuse it); (iii) the question fullmatches the per-team template on his slug's date, its subject name-matches his title's anchor (`_bridge_title_subject`), its opponent name-matches the OTHER side of HIS event title (`_yn_event_sides`, the witness — no event title is `yn:alias-unwitnessed`, a title naming another game `yn:event-shear` / `yn:opp-witness`), the league slot passes the league rule; (iv) `side_norm`/intent are the venue's own for his Yes/No; the line guard and the identity veto of `match_side` apply as to every row. The alias observed rides the hit (`league_alias: "nor->els"`) and `resolve` labels it `matched_by 'premap_alias'`; source stays `'premap'`. His own identifier on the board and refused is never aliased around. The DRAW (`_yn_draw_pick`): his `<lg>-<a>-<b>-<date>-draw` + Yes/No → the `-draw` row under the same identity, its question the venue's dated draw template naming BOTH his event's teams in either order (`yn:draw-names`), his title a closed veto (`_yn_title_is_his_draw`: says `draw`, never `win`, any date his slug's, every non-furniture token one of his two teams' — the feed's draw wording is unattested, so no grammar is guessed); same league code → `premap_identity`, another → `premap_alias`.

### 8.3 Refusal names (census: `resolve_explain` → `yn_c2.refusal`, and the step's `split`, so `explain_unmapped` prints `no_side_match:yn:…`)

`yn:league-slot`, `yn:league-ambiguous`, `yn:name-digits` (a digit in HIS subject — the bridge's `subject_has_digit` pin, kept), `yn:no-row`, `yn:alias-unwitnessed`, `yn:event-shear`, `yn:opp-witness`, `yn:identity-refused`, `yn:shape`, `yn:qdate`, `yn:scope`, `yn:subj`, `yn:side`, `yn:intent`, `yn:folds`, `yn:title-folds`, `yn:title-<why>`, `yn:draw-unwitnessed`, `yn:draw-title-<why>`, `yn:draw-names`; dark, a would-resolve reads `yn:would-resolve`. `yn_identity`'s pinned dict is unchanged.

### 8.4 The seven feed rows against the 19:09Z dump (flag on, offline)

| his slug | outcome | venue identifier | intent | matched_by | note |
|---|---|---|---|---|---|
| `sea-juv-mil-2026-09-06-juv` | Yes | `atc-sea-juv-mil-2026-09-06-juv` | BUY_LONG | premap_identity | league slot `serie a` |
| `sea-bol-sas-2026-09-06-sas` | Yes | `atc-sea-bol-sas-2026-09-06-sas` | BUY_LONG | premap_identity | + opponent `Bologna FC 1909` |
| `lal-esp-sev-2026-09-06-esp` | No | `atc-lal-esp-sev-2026-09-06-esp` | BUY_SHORT | premap_identity | mapped before too; production's 19:02:02Z `no_side_match` was the memo (8.5) |
| `nor-kbk-tro-2026-09-06-tro` | Yes | `atc-els-kbk-tro-2026-09-06-tro` | BUY_LONG | premap_alias `nor->els` | `Tromsø` → `tromso` |
| `arg-ros-new-2026-09-06-new` | Yes | `atc-lpa-ros-new-2026-09-06-new` | BUY_LONG | premap_alias `arg->lpa` | `Newell's` → `newells` |
| `fl1-olm-pfc-2026-09-06-olm` | Yes | — | — | `no_key_intersection` | the dump carries no `olm-pfc` rows (the query named five events); with the venue's wording under `lg1` (listed tonight, 3 games) it aliases `fl1->lg1` — pinned as an expectation |
| `por-gui-cas-2026-09-06-gui` | Yes | — | — | `no_key_intersection` | same: under `lpb` (3 games tonight) it aliases `por->lpb` |

Also pinned: `pol->ekst` (`Jagiellonia Białystok` / `Śląsk Wrocław`, Polish letters both sides), the draw on all five events (`sea-juv-mil-…-draw` → `atc-sea-juv-mil-…-draw`, `nor-…-draw` → `atc-els-…-draw`), and the refusals: a suffix under two league codes, the NEOM shear (a wrong-team title on the right suffix), a lined row, a swapped intent, another date, the reversed team order, no event title.

### 8.5 The premap poller's cadence and the unmapped memo

`workers/premap.py`: the full sweep runs every `REFRESH_SECONDS` = 1800 s over now-12h..now+96h (120 pages at 0.35 s), the fast lane every `FAST_REFRESH_SECONDS` = 180 s over now-3h..now+14h (25 pages), and the fast lane SKIPS a cycle while the full sweep holds `_SWEEP_LOCK` (~42 s per full sweep). A market the venue lists at 18:59Z for a 19:00Z kickoff is in `us_premap` within one fast cycle, two when one is skipped — and the shadow's `UNMAPPED_TTL_S` = 900 s memoised the 19:02:02Z `no_side_match` for the whole first half. No kickoff time is readable by the tick (`us_premap` stores no start time; his feed carries a date, not a clock), so the rule keys on what the tick has: `mirror_shadow.unmapped_memo_s(explain, seen_before)` — a market's FIRST unmapped verdict, when the verdict is one the poller can change (`no_key_intersection`, `no_side_match`), is remembered `UNMAPPED_FRESH_TTL_S` = 360 s (two fast cycles); every later miss, and every other verdict, keeps 900 s. Cost: at most one extra read per new market, ever. Applied in the shadow's `tick_once` (it carries the explain); `mirror_live._tick_candidate` computes no explain and is unchanged.

### 8.6 What this does NOT do (left out)

- A digit in HIS OWN title subject (`Will Mainz 05 win …`) still refuses at the bridge's `subject_has_digit` (pinned in `test_mapping_identity`); it is now counted as `yn:name-digits`.
- The copy lane's yes/no lane (`resolve_team_yesno_exact`) keeps every gate of its own (`yn:title-code`, `yn:opp-title-code`, the thin/twin floors); it only gained the fold, the digit slots and the league rule — `Kristiansund BK` still refuses there because `kbk` does not prefix it.
- No alias for derivative candidates (`tsc-`/`asc-` under another league code): the exact lane's candidate grammar is untouched.
- `_norm` is not changed globally; `side_norm` and the unique index keep the old fold. Existing `event_keys` rows for ø/ł clubs re-key at the next sweep (30 min, 3 min in the fast window); until then the lookup, not the decision, is the old one.
- The live worker's memo (`mirror_live`) is unchanged: it has no explain in reach at the refusal.

### 8.7 M1 (2026-09-07) — four identity fixes on this arm, proved on the venue's verbatim rows (`gap_soccer.md` §2/§7, `gap_new_families.md` §1.7; tests in `backend/tests/test_m1_identity_fixes.py`)

| gate | before | now |
|---|---|---|
| league slot, Serie B | `pmus._yn_league_slot_bad('serie b')` = True (`b` is a bridge scope token, and a single letter): every per-team and draw row of the six `srb` games refused `yn:league-slot` although his codes equal the venue's under `itsb->srb` ($25.7k) | the single letter AFTER `serie` is the tier — the venue's own phrase ("… in the Serie B match scheduled for Sep 6, 2026?") — symmetric with the `a` exception and the letter before `league`; every scope token, stem and join around it still refuses (`serie b women`, `primavera b`, `serie b u21`, bare `b`). `itsb-ver-are-…-ver` → `atc-srb-ver-are-2026-09-06-ver` `premap_alias itsb->srb`; the draw alike. `L.R. Vicenza` still refuses the CLUB screen (`yn:scope`): a club-name gate, no venue identity extends it. One C2 pin moved with it: a `Serie B` question on a `sea` identifier no longer reads as a scope twin (the venue's own league phrase is not a scope) |
| the numbered club (P6) | `_yn_pick` stopped at the bridge's digit rule (`subject_has_digit` → `yn:name-digits`) before any venue row was read: "Will Bologna FC 1909 win on 2026-09-06?" refused while the venue's own row said "Will Bologna FC 1909 win against US Sassuolo Calcio in the Serie A match …" — Parma Calcio 1913, Stade Rennais FC 1901, FC Basel 1893, US Avellino 1912, 1. FSV Mainz 05 alike ($25k/24 h) | when the bridge answers `subject_has_digit` AND his slug's tail is a team code (`_yn_slug_parts`; a moneyline carries no line, so the number is never a line here), the arm reads the same date-verified subject (`_bridge_title_subject_any`, the bridge's reading before its digit rule) and runs the identity / single-alias arms exactly as today with ONE tightening: the venue's subject must equal his by RAW token-set equality (`pmus._yn_name_match_raw`, `_yn_name_match`'s first branch alone — no `GENERIC_CLUB_TOKENS` removal), so the number is restated verbatim or the row refuses. Not restated ("Bologna FC", "Bologna FC 1919", his "Will Mainz 05 win …" against the venue's "1. FSV Mainz 05"): `yn:name-digits`, unchanged. The record carries `yn_c2.numbered`. The bridge's own rule and the wording arm's veto (`_yn_title_is_his_game`) are untouched (`test_mapping_identity`'s "FC Schalke 04" pin stands); `test_c2_yesno_alias`'s "Will Mainz 05 win …" pin REVERSED by design — its venue row restates "Mainz 05" verbatim. C5's title witness (`_c5_his_club`) still reads the bridge's rule: a numbered club certifies no code translation from its own title |
| the club-slot screen (P7) | `pmus._yn_slot_bad` ran the bridge's single-CHARACTER rule: `_norm` leaves "1. FSV Mainz 05" as `1 fsv mainz 05` and the bare `1` refused every slot it appeared in — `yn:scope` on `bun-hsv-mai-…-hsv`, `yn:draw-unwitnessed` on the draw, `total:title-shape` on the total ($5.2k) | letters-only, as the league slot's rule already is (no lone digit is in any reviewed scope list); the scope tokens, stems, extra list, joins and the 5-token cap are read as before; `premap._has_scope_token` (the bridge's, the wording arm's) is byte-identical. Observation (review F6): a lone DIGIT in a club slot (`arsenal fc 2`, a digit reserve marker) is now identity-held, not scope-held — it maps only when the venue restates `Arsenal FC 2` verbatim and refuses against `Arsenal FC` by the token set. `-hsv` → `premap_identity`, the draw → `premap_identity`, `-total-4pt5` → `total:line-absent` (its true refusal: the venue lists 0.5–3.5) |
| the digit team code (`copy_sports.market_type_of`) | a lone post-date token with a digit (`chi1-cdp-cu1-2026-09-06-cu1`, `bra-cor1-cha-…-cor1`, `arg-riv-riv1-…-riv1`, `ere-her1-az-…-her1`; $14.7k/30 h) failed `isalpha` and typed `unknown` (`unknown_market_type:unparsed`) | the token that EQUALS one of the slug's own two team codes (the two tokens before the date) is a moneyline — identity from his slug, no new grammar; any other alnum token (`cu2`, `game2`) stays `unknown`. `family_of`, `_feed_family`, `slug_lines` unchanged. `chi1-cdp-cu1-…-cu1` then reaches the venue's `atc-pdc-pal-uco` rows and refuses `no_side_match:yn:no-row` — BOTH codes and the league differ, so C5 certifies nothing (no trace); `bra-cor1` / `arg-riv1` are the C5 case |


## 9. C3 (2026-09-06) — the derivative families by identity: spreads, first-half totals, btts, the draw's title witness, the shared stem

Owner 19:33Z: "I need more trades firing in the mirror sleeve! We need to be mirroring a larger percentage of RN1s positions." His last 2 h to 20:35Z: 290 markets, $631k; moneylines and most full-game totals map since C1/C2/D1. What still refused, by his dollars in 3 h (`c3_rows_2039.log` table 2): spreads $20.4k + $3.4k + $2.3k + $1.7k + $1.6k + $1.2k + $1.1k + $1.0k + $0.8k (`no_side_match`: every `asc-` row carried the line digits as its side), the first-half total $23.4k + $1.3k + $0.8k (typed `prop`, no venue prefix), draws $11.5k + $5.5k + $4.9k (`yn:draw-unwitnessed` / `no_key_intersection`), btts $6.0k + $4.1k + $0.8k (`yn:title-title_not_win_shape`), exact scores, the halftime draw, team totals (`unknown_market_type`). Patch C3 against 36e6110; tests in `backend/tests/test_c3_derivatives.py` (the dumps' rows embedded verbatim). Every admission is IDENTITY — the venue identifier BUILT from his slug (league or the C2 alias, team codes, date, segment, line, byte for byte after the pt5 fold) plus name corroboration from the venue's own question and a closed veto on his title — never a similarity score, never a nearest line, never a home/away position.

### 9.1 The venue's grammar, read from the rows (`c3_rows_2038.log`, `c3_rows_2041.log`)

| family | venue identifier | sides | question (verbatim shape) | his slug / title / outcome |
|---|---|---|---|---|
| spread | `asc-<lg>-<a>-<b>-<date>[-<seg>]-(neg\|pos)-<L>` (seg ∈ fh, sh, 1h, 2h, 1q..4q) | one proposition, two sides sharing the identifier, described by the line (`10.50`), marked long/short | `Will the <A> cover -10.5 vs the <B> in <A> vs. <B>?` / `… cover 10.5 …` (no sign = a gets the points); **a, the first slug team, is always the subject**; the trailing matchup is the names (`Bulldogs vs. Rattlers`) or the codes (`JUV vs MIL`); cfb rows name nicknames on most lines and the schools on some (22.5/24.5 for scarst-flam; the 1h/1q… rows for washst-wash) | `<lg>-<a>-<b>-<date>-spread-(home\|away)-<L>` / `Spread: <Team> (-19.5)` / `<his team>` |
| total, fh/sh total | `tsc-<lg>-<a>-<b>-<date>[-fh\|-sh]-<L>` | over / under | `Will the total in ESP vs SEV be more than 0.5?` — **identical across segments**: the segment lives only in the identifier | `…-total-<L>` `<A> vs. <B>: O/U 1.5`; `…-first-half-total-<L>` `<A> vs. <B>: 1st Half O/U 0.5` / Over, Under |
| btts, fh/sh btts | `astatc-<lg>-<a>-<b>-<date>[-fh\|-sh]-btts` | yes / no | `Will both teams score in the match\|first half\|second half between <A> and <B> on <date> <time>?` (some rows carry a stamped line `45` from the clock — not a bar) | `…-btts` / `<A> vs. <B>: Both Teams to Score` / Yes, No |
| draw | `atc-<lg>-<a>-<b>-<date>-draw` (C2) | yes / no | `Will the <league> match <A> vs <B> scheduled for <date> end in a draw?` | `…-draw` / `Will <A> vs. <B> end in a draw?` / Yes, No |
| exact score | — not listed for soccer (`es-X-Y` rows exist for tennis sets only) | | | `…-exact-score-X-Y` — refused `exact:family-absent` |
| halftime result | — not listed | | | `…-halftime-result-draw` — refused `halftime:family-absent` |
| team total | — not listed | | | `…-team-total-(home\|away)-<L>` — refused `team_total:family-absent` |

`copy_sports.family_of` / `segment_of` read his suffix by these attested word forms (`market_type_of` is untouched: its pins keep a first-half total `prop` for the copy lane's block); `premap._want_prefixes` maps a C3 family to its kind; `premap.c3_same_segment` keeps only the rows on HIS segment before any matcher runs (a full-game slug never sees a `-fh-` row, a first-half slug never the full game), and a segmented slug is read by the family pick alone.

### 9.2 The asc side parser (`premap._asc_yes_no`, `_market_rows`)

`us_premap` carried every `asc-` row as side_norm `10 50`, intent BUY_SHORT (both sides normalised to the digits, collided on the `(identifier, side_norm)` index, the short write won). Now an `asc-` market whose two sides share the slug, describe themselves without letters, and carry explicit markers naming exactly one long and one short writes side_norm `yes` (the long side, BUY_LONG) and `no` (the short side, BUY_SHORT) — the venue's own marker on the side itself, as the astatc yes/no rows read; any other shape keeps the old rows. Cadence: the fast lane rewrites now-3h..now+14h every 180 s, the full sweep every 1800 s; the new rows land under new keys at the first sweep after deploy, the stale digit rows are inert (never yes/no, never a candidate) and age out at `PRUNE_HOURS` = 26 h. No migration, no re-parse on read.

**The convention is observed, not inferred** (`venue_asc_2125.log` 21:26:43Z, `venue_asc_2130.log` 21:29:29Z — the gateway's bbo/book on one live game; `GET /v1/markets/<slug>` is 404 for every family, so the side names come only from the events/markets listing the poller parses). South Carolina State (the ~19.5-point favorite) vs Florida A&M, in progress: `asc-cfb-scarst-flam-…-neg-24pt5` "Will the Bulldogs cover -24.5" LONG 0.18 (bid 0.17/ask 0.20, pre-game close 0.24, shortPx 0.82); `…-pos-21pt5` "Will the Bulldogs cover 21.5" LONG 0.99 (bid 0.99 × 859,957, shortPx 0.01). The favorite covering +21.5 is near-certain and its LONG trades at 0.99; covering −24.5 is unlikely and its LONG at 0.18–0.24 — were LONG the complement the prices would be swapped. Corroboration: `asc-cfb-washst-wash-…-neg-24pt5` "Cougars cover -24.5" LONG 0.01; `asc-lg1-olm-pfc-…-neg-1pt5` "Marseille cover -1.5" opened 0.27, settled 0.0000, and `asc-lal-esp-sev-…-neg-1pt5` settled 0.0000 (neither won by two); `astatc-lal-esp-sev-…-btts` settled 1.0 (both scored), `tsc-lal-esp-sev-…-fh-1pt5` settled 0.0 (first-half under 1.5). So LONG = the named (first-slug) team covers the stated line = the parser's `yes` row; pinned with those prices in `TestTheAscConventionIsObserved`.

### 9.3 Spreads (`premap._c3_pick_spread`, matched_by `premap_spread`)

His team is his OUTCOME by name; his signed line is the title's when the title's team name-matches his outcome, else its negation (`Spread: Washington (-23.5)` with outcome `Washington State` is Washington State +23.5). His team resolves to a or b ONLY by `_yn_name_match` against the two names in the venue question on his line. Then, with a the question's subject:

| his team | his line | venue row | side | intent |
|---|---|---|---|---|
| a | −L | `neg-L` | yes | BUY_LONG |
| a | +L | `pos-L` | yes | BUY_LONG |
| b | −L | `pos-L` (a covers +L is the complement) | no | BUY_SHORT |
| b | +L | `neg-L` | no | BUY_SHORT |

Pinned as eight cases (a/b × ±L × the title naming his team or the other). Refusals: `spread:line-absent` (his L is listed under neither sign — 19.5 is not 21.5/22.5/24.5), `spread:names-unreadable` (his outcome matches neither venue name — the nickname rows), `spread:whole-number`, `spread:title-shear` (title line ≠ slug line), `spread:title-unreadable`, `spread:title-shape`, `spread:title-segment`, `spread:outcome`, `spread:question-shape`, `spread:sign-shear` (the identifier's neg/pos disagrees with the question's sign), `spread:intent`, `spread:line-shear`, `spread:league-ambiguous`, `spread:segment-absent`, `spread:ambiguous`; M1 (2026-09-07, reporting only): `spread:sides-unparsed` — the wanted identifier IS on the board but no row of it carries a yes/no side (the pre-C3 sweep's `1 50` / `2 50` BUY_SHORT rows on an ended game the sweep never re-read: every EPL / lal spread residue of 2026-09-06, $39.5k), named apart from `spread:line-absent` so the census stops filing a listed line as a venue gap; a live sweep writes the yes/no rows and the pick proceeds as pinned.

### 9.4 First-half totals (`_c3_pick_total`, `premap_fh_total` / `premap_sh_total` / `premap_total`) and btts (`_c3_pick_btts`, `premap_btts` / `premap_fh_btts`)

Total: identifier `tsc-<code>-<a>-<b>-<date>[-fh]-<L>`, side_norm over/under, the row's line = the question's line = his; his title's qualifier must be exactly `1st Half O/U L` (`first half` also) for fh, `O/U L` for the full game (`total:title-shear`); the question's matchup must name his title's two sides or be the identifier's own codes (`total:names`). Full-game totals now map through the wording arm on the unique unsegmented row (before this the fh/sh siblings made it ambiguous). btts: identifier `astatc-<code>-<a>-<b>-<date>[-fh]-btts`, the question's segment word must be the identifier's (`btts:segment-shear`), its date his (`btts:qdate`), both names his title's two sides (`btts:names`); his title `<A> vs. <B>: Both Teams to Score` (`1st Half …` only on a `first-half-btts` slug, else `btts:title-shear`). League alias as C2: exactly one other code carrying the suffix when his own is absent (`fl1->lg1`), two is `<family>:league-ambiguous`. Corroboration under an alias is WEAKER than C2's yes/no alias for a total: the tsc question names only the codes (`OLM vs PFC`), so the two clubs are agreed only through the event keys his title built (date + both names, the venue's own event title); the btts question names both clubs in full and `btts:names` holds the alias to his title's two sides (pinned: an aliased btts row naming another club refuses).

### 9.5 The draw's title witness (`_yn_draw_title_sides`, matched_by `premap_draw_title`) and the key builder

The feed's draw title is `Will <A> vs. <B> end in a draw?` on every one of the 24 draw rows in the dump. When no event title names two sides, that title — through exactly that template, nothing else — is the witness; the same `_yn_title_is_his_draw` veto and the venue's `yn:draw-names` corroboration apply; the four-argument identity caller (`yn_identity_rows`) maps the draw under his own league code and never aliases. A dated or qualified variant stays `yn:draw-unwitnessed` (the C2 pin). `event_keys_for` reads the same wording down to its matchup before keying (`olympique de marseille vs paris fc@2026-09-06`, where it keyed `draw vs marseille` before) — a lookup, not a decision; `fl1-olm-pfc-…-draw` was `no_key_intersection` for this alone.

### 9.6 The shared stem (`map_lane.shared_stem`, `code_names`)

When one team code is a proper prefix of the other (`wash`/`washst`, `mich`/`michst`, `ohio`/`ohiost`) a first-word hit on the shorter code names either team. `code_names(code, outcome, sibling)`: the longer code is named when the outcome's first word starts with the whole code, or starts with the stem and the outcome's second word starts with the code's residual (`Washington State` → `wash` + `st`); the shorter code when the first word starts with the stem and the longer is not named (`Washington`, `Washington Huskies`). No subsequence, no similarity. Used wherever `code_hits` decided a side (`aec_code_side`'s hits, its venue-side and title-order checks, `pair_agrees`). `pair_agrees(cfb-washst-wash, i=1, "Washington State", 0)` — the row that was `side_code_conflict` — agrees; both tokens map by their own words; a sibling naming the mapped team (`Washington` beside a mapped `wash`) and the no-stem case (`Baylor` beside a mapped `bayl`, `Michigan State` beside a mapped `mich`) still refuse `side_code_conflict`. The whole-first-word case (`tex`/`texas`, `mia`/`miami`): `Texas Tech` and `Miami (OH)` name the LONGER code by their whole first word, so the shorter code's team is a single-side hit that maps only with the pair rule agreeing — beside the mapped token that sibling claims the mapped code and refuses the market (pinned, `TestTheStemAndTheWholeFirstWord`). I did not take the literal rule "stem-shared is neither a hit nor a conflict, the pair agrees by the index alone": it would map `Washington`/`Washington State` on the feed's index with nothing to catch a swapped order (the round-3 collision), and it leaves the mapped token unmatched. The residual rule is name evidence.

### 9.7 Expectation table (his rows of `c3_rows_2039.log` table 2 against the dumps' venue rows, offline, `PREMAP_YN_IDENTITY=on`)

| his slug | outcome | venue identifier / refusal | side / intent | matched_by |
|---|---|---|---|---|
| `sea-juv-mil-…-first-half-total-0pt5` ($23.4k) | Under | `tsc-sea-juv-mil-2026-09-06-fh-0pt5` | under / BUY_SHORT | premap_fh_total |
| `cfb-scarst-flam-…-spread-away-19pt5` ($20.4k) | South Carolina State | `no_side_match:spread:line-absent` — the venue lists 21.5/22.5/24.5, not 19.5 | — | — |
| `lal-mala-lev-…-draw` ($11.5k) | Yes | unverified: no mala-lev venue rows in the dumps; maps as lal-esp-sev does if `atc-lal-mala-lev-…-draw` names both clubs | — | — |
| `fl1-olm-pfc-…-halftime-result-draw` ($6.3k) | Yes | `unknown_market_type:family_not_listed`, refusal `halftime:family-absent` | — | — |
| `sea-juv-mil-…-btts` ($6.0k) | Yes | `astatc-sea-juv-mil-2026-09-06-btts` | yes / BUY_LONG | premap_btts |
| `lal-esp-sev-…-draw` ($5.5k) | Yes | `atc-lal-esp-sev-2026-09-06-draw` — settled 1.0 at the venue (the match was a draw, `venue_asc_2130.log`): the $5.5k long-draw position the title witness recovers | yes / BUY_LONG | premap_draw_title |
| `fl1-olm-pfc-…-draw` ($4.9k) | Yes | `atc-lg1-olm-pfc-2026-09-06-draw` (alias fl1->lg1) | yes / BUY_LONG | premap_draw_title |
| `lal-esp-sev-…-btts` ($4.1k) | Yes | `astatc-lal-esp-sev-2026-09-06-btts` | yes / BUY_LONG | premap_btts |
| `bra-cru-cap-…-spread-home-1pt5` ($3.4k) | Cruzeiro EC | unverified: no bra-cru-cap venue rows in the dumps; maps to `asc-bra-cru-cap-…-neg-1pt5` yes only if that row's question names Cruzeiro EC | — | — |
| `sea-juv-mil-…-exact-score-2-0` ($3.3k) | No | `unknown_market_type:family_not_listed`, refusal `exact:family-absent` | — | — |
| `lal-mala-lev-…-team-total-home-2pt5` ($3.1k) | Under | `unknown_market_type:family_not_listed`, refusal `team_total:family-absent` | — | — |
| `cfb-washst-wash-…-spread-home-23pt5` ($2.3k), -24pt5, -21pt5 | Washington State | `no_side_match:spread:names-unreadable` — the venue's rows on those lines name Cougars/Huskies | — | — |
| `fl1-olm-pfc-…-spread-home-1pt5` ($1.7k) | Paris FC | `asc-lg1-olm-pfc-2026-09-06-neg-1pt5` (alias fl1->lg1; Paris FC +1.5 = not OLM −1.5) | no / BUY_SHORT | premap_spread |
| `cfb-washst-wash-2026-09-06` ML ($2.1k) | Washington State (index 0) | `aec-cfb-washst-wash-2026-09-06` side 0 `Cougars` by `washst`; the sibling `Washington` agrees | BUY_LONG | grammar (aec_code_order) |
| `lal-esp-sev-…-spread-home-1pt5` ($1.0k) | Sevilla FC | `asc-lal-esp-sev-2026-09-06-neg-1pt5` | no / BUY_SHORT | premap_spread |
| `sea-juv-mil-…-spread-away-1pt5` ($0.8k) | AC Milan | `asc-sea-juv-mil-2026-09-06-pos-1pt5` (Milan −1.5 = not Juve +1.5) | no / BUY_SHORT | premap_spread |
| `sea-juv-mil-…-first-half-total-1pt5` ($1.3k) | Under | `tsc-sea-juv-mil-2026-09-06-fh-1pt5` | under / BUY_SHORT | premap_fh_total |
| `lal-esp-sev-…-first-half-total-0pt5` ($0.8k) | Over | `tsc-lal-esp-sev-2026-09-06-fh-0pt5` | over / BUY_LONG | premap_fh_total |
| `fl1-olm-pfc-…-total-4pt5`, `-5pt5` | Under | `no_side_match:total:line-absent` — the venue's full-game lines are 0.5–3.5 (4.5 exists on fh/sh only, never taken) | — | — |
| `lal-mala-lev-…-btts` ($0.8k) | No | unverified (no mala-lev rows in the dumps) | — | — |

What the venue does not list tonight, by fact: soccer exact scores, halftime results, team totals; the spread line 19.5 on scarst-flam; full-game totals 4.5/5.5 on olm-pfc; cfb totals (46.5/51.5/58.5 on washst-wash, 56.5 on scarst-flam): no `tsc-cfb-…` rows in the dumps, unverified — they map through the wording arm only if the venue lists that exact line. Those stay unmapped by fact, not by code.

### 9.8 The switch, the echo, the family label, and what is left out

**The switch.** All of C3 — spreads, first-half totals, btts, the draw's title witness — sits behind `PREMAP_YN_IDENTITY=on`, the switch C2's identity arm answers to (`premap.resolve` / `resolve_explain`: `c3_his(global_slug) if yn_identity_on() else None`, the `c3_pick` call inside the same `if hit is None and yn_identity_on():` block; `_want_prefixes` reads the C3 family only when on). Off, every row reads exactly as before C3: a spread is `no_side_match`, a first-half total `unknown_market_type:family_not_listed` (prop, no prefix), btts `no_side_match:yn:title-title_not_win_shape`, the draw with no event title unmapped; `resolve_explain` carries `c3_on`. Pinned in `TestTheSwitchHoldsAllOfC3`. The asc parser (a row, not a decision) and the stem rule (the grammar class has its own certification) are not behind it.

**The post-fill echo.** `live_executor._side_echo_verify` re-derives a C3 fill through `premap.c3_pick` on the venue's live rows of the market bought (identifier from his slug, the question, his title's veto, the SEGMENT — never `match_side`, which has no segment and would certify a full-game row against a first-half slug) and compares identifier AND intent: the right side is `ok`, the complement of the same asc proposition is `mismatch` (shadow: counted under `side_echo_shadow`, never tripped; live: the requarantine as for every class), a wrong-segment row `unverified`. `_independent_check` never cross-checks a C3 fill against the moneyline candidates (a spread outcome is a club name; `resolve_market_exact` would find the `aec-`/`atc-` moneyline and its different identifier would read as a false mismatch that re-arms the total quarantine): spreads and totals go to the copy lane's own `resolve_derivative_exact` (today it refuses the venue's `… in JUV vs MIL?` wording, so `unverified`), btts has no independent resolver (`unverified`). So the first-fill gate opens for these families on the echo's `ok`, and the wrong-side tripwire is armed. REACH (re-review, 2026-09-06 21:47Z): that echo runs on the COPY LANE's fills only (`live_executor.py:5996` is its one call site; entry is OFF by owner order). The mirror worker runs no post-fill echo on its own premap books — moneylines before C3 and the C3 families now alike — and its first-fill gate reads only the global `side_echo_last.ok >= 1`. Wiring `_side_echo_verify` into the mirror's fill path is a named follow-up, not part of C3. Two more residuals: the asc listing's `marketSides` were never captured verbatim (the per-market GET 404s; the parser is pinned on the synthesized shape and the prices prove the semantics, so any other shape refuses), and `live_rows_for_market` reads through the SDK's `retrieve_by_slug` — if that route 404s on asc as the raw GET does, every C3 echo is `venue unreachable` → `unverified` (safe, inert) until one live fill confirms it.

**The family label.** The mirror files a slug under `copy_sports.mirror_family_of` (`mirror_live` admission facts, `mirror_shadow._family_of`): a first-half total is the `total` family (segment `fh` in its slug), a spread `spread`, btts `btts`; everything else is `market_type_of`'s answer. `MIRROR_FAMILIES` is unchanged; the copy lane's `BLOCKED_TYPES={'prop'}` and `market_type_of` are unchanged (`copy_verdict('rn1', first-half total) == market_type_blocked`, pinned).

### 9.9 M1 (2026-09-07) — the key grammar's event-title head; the empty prefix filter named by the board (`gap_new_families.md` §0.4/§3.2, `gap_prefix_filter.md`; tests in `backend/tests/test_m1_identity_fixes.py`)

**The key (lookup only, `event_keys_for` / `_yn_event_sides`).** His stored `markets.event_title` sometimes carries a trailing family segment after the matchup — `Everton FC vs. Manchester United FC - Exact Score`, `Arsenal FC vs. Chelsea FC - Halftime Result`, `Málaga CF vs. Levante UD - More Markets` (every exact-score / halftime-result row of `nf_his_1350.log`). The key read `everton fc vs manchester united fc exact score@2026-09-06` and met nothing: $84k/24 h of exact scores and halftime results died at `no_key_intersection` before any lane ran, and `_yn_event_sides` read the segment into the second club (`levante ud more markets`), so C5's event witness was dark on the same rows. Now a title matching `^(?P<head>.+? vs\.? .+?) - (?P<seg>[A-Za-z ]+)$` whose segment is one of the feed's three attested family segments — `Exact Score`, `Halftime Result`, `More Markets`, a CLOSED list (`_EVENT_TITLE_SEGMENTS`) — keys the HEAD alone (the same `a vs b@date` / `b vs a@date` keys the plain title yields, byte for byte — the plain title's keys are unchanged) and `_yn_event_sides` reads the same head; the segment builds no key, a title naming one club builds no matchup key, and no bare-date key exists (the `league_alias_probe` rule stands). The list is closed because `_yn_event_sides` is a WITNESS (the alias arm, the draw, C5 decide on it): the review's CRITICAL found that a regex admitting any trailing word let `Arsenal FC vs. Chelsea FC - Women` / `- Reserves` / `- Aggregate` witness the men's full-game rows (`premap_alias wsl->epl`), the wrong-event class the bridge's scope belt exists for; any other suffix now stays in the title, keys nothing and witnesses nothing, exactly as before (`tests/test_m1_review_pins.py` §A). His exact-score market title `Exact Score: <A> X - Y <B>?` (the feed's one shape) keys the two clubs as the matchup, never the score. The exact-score and halftime rows therefore now reach the venue's rows and refuse by the family name (`unknown_market_type:family_not_listed`, `exact:family-absent` / `halftime:family-absent`) — the lanes are separate builds; nothing maps here.

**The empty prefix filter (`resolve_explain` only; `resolve` untouched).** `type_prefix_filter_emptied` was documented as a fast-lane latency symptom; `gap_prefix_filter.md` proved a second class: leagues the venue lists ONLY moneylines for (bra, lpa, ekst, scp, srb, swsl, sld — 6 `atc` rows per game, zero `tsc`/`asc`/`astatc` rows anywhere in the table; ~$82k/24 h of his totals and spreads counted as a mapper gap, recoverable by no rule). When `kept` empties on a C3 family (behind the switch, like every C3 reading), `_kind_absent_probe` names it from the venue's own rows, never a table: the league code(s) the board's identifiers carry for this game (`_C3_IDENT_RE` `lg`), then one read-only probe per code — `SELECT 1 FROM us_premap WHERE identifier LIKE '<kind>-<lg>-%' LIMIT 1` — a row is `<family>:kind-absent:league-listed` (the venue lists the family in that league, not yet on this game: the latency case), none is `<family>:kind-absent:league-unlisted` (structural, unmappable at this venue today). Names: `total:kind-absent:league-unlisted`, `total:kind-absent:league-listed`, `spread:kind-absent:…`, `btts:kind-absent:…`; sibling of `total:segment-absent` (which needs a `tsc` row on another segment — a first-half total on an `atc`-only board is `kind-absent`). The step name stays (the census buckets on it; `explain_unmapped` prints `type_prefix_filter_emptied:total:kind-absent:league-unlisted`); the probe runs only on the refusal, like `league_alias_probe`, and is recorded in `kind_probe` (`kind`, `league_codes`, `asked`, `listed`; a failed probe or an unreadable board leaves the bare `<family>:kind-absent` with `error`). The refusal returns before `c3_same_segment` / `c3_pick` can see the rows, so the probe widens nothing (pinned: `resolve` is None in every case; the counterfactual `tsc-lpa-rac-cat-2026-09-06-2pt5` row makes the same slug `resolves` with no code change).

Every C3 hit is source `premap` (`QUARANTINE_RESUME_SRC`'s own class; the copy lane's premap step answers with the same rows, so the copy lane's spreads/btts/fh totals now map at premap before its exact lane — the copy lane's `prop` block still refuses a first-half total there). `resolve_explain` carries `family`, `c3` (the family trace), `matched_by`, and the family refusal as the step's `split`, so MIRRORUNMAP prints `no_side_match:spread:line-absent` etc.; the not-listed families keep `unknown_market_type:family_not_listed` (pinned) with `refusal` naming the family. Left out: exact score / halftime result / team totals (not on the venue: refused by name, an orientation rule is unwritten until a soccer `es-` row is observed); cfb spreads whose venue row names nicknames (identity cannot read them — the 1h/1q rows of washst-wash name the schools but a full-game slug never crosses a segment); segmented spreads and second-half families are supported by grammar but unattested in his feed; `_norm`, `side_intent`, `match_side`'s branches and `market_type_of` are unchanged; the live worker's family gate reads `market_type_of` as before (a first-half total is admitted as `prop`, which `MIRROR_FAMILIES` carries).


## 10. C4 (2026-09-06 22:2xZ) — college football spreads whose venue rows name mascots; the multi-word school code

Owner 19:33Z: "I need more trades firing in the mirror sleeve!" Read at 22:22Z (`his-recent_2222.log`): in the two hours to 22:21Z RN1 filled 198 markets / $460k; the C3 families map (soccer totals, draws, the cfb totals `tsc-cfb-washst-wash-…-total-34pt5` $42k on a book). Still refused: `cfb-washst-wash-2026-09-06-spread-home-<L>` (outcome "Washington State", title "Spread: Washington (-L)") $7.5k + $3.8k + $1.3k + $0.3k + $0.1k as `no_side_match:spread:names-unreadable`, and the moneyline `cfb-scarst-flam-2026-09-06` (outcome "South Carolina State") $1.0k as `side_code_unmatched`. Patch C4 against 566094a; tests in `backend/tests/test_c4_cfb_spreads.py` (the dumps' rows verbatim). Identity only: no nickname dictionary, no similarity, no home/away, and no positional inference beyond what the venue's own rows certify.

### 10.1 What the venue's rows say (`c3_rows_2038.log`)

The SAME game is named by mascots on some `asc-` rows and by schools on others, per row: `asc-cfb-washst-wash-…-neg-24pt5` "Will the Cougars cover -24.5 vs the Huskies in Cougars vs. Huskies?" beside `asc-cfb-washst-wash-…-1h-pos-3pt5` "Will the Washington State cover 3.5 vs the Washington in Washington State vs. Washington?"; `asc-cfb-scarst-flam-…-neg-21pt5` "Bulldogs … Rattlers" beside `…-neg-22pt5` "South Carolina State … Florida A&M". The moneyline rows list the mascots as sides: `aec-cfb-washst-wash-…` cougars (BUY_LONG) / huskies (BUY_SHORT), `aec-cfb-scarst-flam-…` bulldogs / rattlers. No row on either event names a mascot AND its school; the venue's per-side contracts (`atc-cfb-…-<code>`) list none for these two games (the dumps carry only the `atc-cfb-washst-wash-…-dstd/fg/rec/…` prop rows), and where the probes do list one (`atc-cfb-bayl-aubrn-2026-09-05-winner-2h-bayl` "Will Baylor win the second half?") its question names the school. So a mascot is tied to a slug position ONLY by the aec side order — which is exactly the C1 grammar class's claim (§7.2: "what remains unproven at mapping time — that marketSides[i] is the team at slug position i") and exactly what its certification checks per market (§7.7 step 1: the venue's per-side contract for code i must name the aec side description).

### 10.2 The chain (`premap._c4_subject_by_code`, `_c4_subject_certified`, `map_lane.code_reads`)

Runs inside `_c3_pick_spread` ONLY when C3's name step reads nothing (his outcome name-matches neither venue name) AND neither venue name names a code (a school-named row that does not match his outcome keeps C3's `spread:names-unreadable`). Everything before it (line byte-equal, whole-number, segment, title shape/shear) and after it (the sign/complement table, the intent, the sign shear) is C3 unchanged.

1. **Codes.** `cfb-washst-wash` on both feeds: a = washst, b = wash (the C3 key: league, a, b, date, segment, line).
2. **His team and his title's team, by the code** (`map_lane.code_reads`): the C1 whole-word/first-word rule, the C3 stem rule ("Washington State" → washst by `wash`+`st`, "Washington" → wash), and — only where those name NEITHER code — the C4 split rule (10.4). Exactly one code, else `spread:code-unmatched` (trace `code_hits`); the title's team the same way, else `spread:title-unreadable` (trace `title_hits`). **The pair witness (review fold, 10.7):** the two must be the two DIFFERENT positions — two different names on one code is `spread:code-collision`, a title naming his outcome's own team (one name, read twice) is `spread:pair-unwitnessed`; both refused before any certification is read.
3. **The subject.** C3's grammar has a in the subject slot; on a mascot row the subject's identity is a POSITION, certified only by the venue's own rows on this event: the moneyline row `aec-<lg>-<a>-<b>-<date>` must be on the board with exactly the question's two names as its sides (a set — its ORDER is never read), and the C1 grammar class's state (`ingestion_state.mirror_grammar_echo`, read by `premap.grammar_cert` in `resolve`/`resolve_explain`, spreads only) must be readable, not tripped, and hold a VENUE-TRUTH record for that aec market — `certified[aec slug] = {outcome_desc, side_index, his_slug}`, written by `mirror_live._grammar_admission` when `map_lane.grammar_truth` says ok (the venue's per-side contract for the code named that mascot; kept after the fill echo pops `pending`, bounded at 200). The record's mascot must be one of the question's names, and its slot in the question (0 subject, 1 opponent) must equal its certified code position: a record placing the subject's mascot at b (or the opponent's at a) is `spread:subject-conflict` — the grammar never has b as the subject, so that is a contradiction, never a map. Either side's record certifies the pair (Huskies at wash puts Cougars at washst by elimination: two teams). Anything short of this — aec row absent (`aec-absent`), other names (`aec-names`), state unreadable (`grammar-unreadable`), tripped or `mismatch > 0` (`grammar-tripped`), no record for this event (`grammar-uncertified`), a record of another shape or another event (`record-shape`) — is `spread:subject-uncertified`, the reason in the trace's `subject`.
4. C3's table: his team a with −L → `neg-L` yes BUY_LONG; a with +L → `pos-L` yes; b with −L → `pos-L` no BUY_SHORT; b with +L → `neg-L` no. matched_by `premap_spread_code` (trace `subject_via: code`); source `premap`; behind `PREMAP_YN_IDENTITY` with the rest of C3.

**What the certification says, and whether it is on tonight.** The C1 class does not certify a convention ("side 0 is slug team a for cfb"); it certifies ONE aec market at a time, against venue-only truth, before a grammar book opens (§7.7). At 22:22Z `mirror_grammar_echo` reads ok=0, mismatch=0, `grammar_echo_ok`=0, `grammar_probation`=0: no grammar book has opened, nothing is certified, and for these two games the venue lists no per-side contract in `us_premap` (the live fallback — his own code's `atc-cfb-washst-wash-…-washst` as the one candidate the venue must confirm — is a live read only the worker makes). So tonight every one of the five washst-wash spreads refuses `spread:subject-uncertified` (`grammar-uncertified`); they map the tick after the live worker certifies `aec-cfb-washst-wash-2026-09-06` (venue truth ok on its moneyline), and stay refused while nothing does. That is the fail-closed reading of the brief's step 3: the mascot→position tie is trusted only where the venue's own contract has confirmed it for this very market.

### 10.3 Refusal names (census: `no_side_match:<split>`)

`spread:code-unmatched` (his outcome names neither or both codes), `spread:title-unreadable` (existing name; now also his title's team naming neither/both codes on a mascot row), `spread:code-collision` and `spread:pair-unwitnessed` (10.7, the pair witness), `spread:subject-uncertified` (10.2 step 3, with `c3.subject` naming which leg), `spread:subject-conflict`. Every C3 spread refusal is unchanged; `spread:names-unreadable` now stands only where a venue name names a code (the school-named rows) or his title/outcome name the mascot but not the school (C3's own name path).

### 10.4 The multi-word school code (`map_lane.code_words`, folded into `code_reads`)

`code_hits("scarst", "South Carolina State")` is False (first word "south" neither starts with nor prefixes "scarst"), so the moneyline refused `side_code_unmatched`. The rule: the code is the concatenation of NON-EMPTY PREFIXES of CONSECUTIVE words of the outcome, from its first word, EVERY word used in order, the split UNIQUE, and the outcome at least two words; "&" inside a word is dropped before splitting ("A&M" → "am"). `scarst` = s|car|st, `flam` = fl|am, `washst` = wash|st, `michst` = mich|st, `ohiost` = ohio|st, `tam` = t|am. Refused: a split that skips a word (`tex` over Texas|Tech leaves Tech unused — pinned as the NEGATIVE; `code_hits("tex", "Texas Tech")` keeps its C1 verdict, a first-word hit), two splits (`abab` over Aba|Bab), a single word (the first-word rule owns it: `aubrn` never reaches Auburn), codes under three letters. `code_reads(code, outcome, sibling)` is `code_names` wherever `code_names` names EITHER of the slug's codes, and the split rule only where it names neither — so `mist` on "Michigan State" beside `mich` stays unnamed (mich is named first; C1's pin), every C1/C3 verdict stands, and the split rule turns a "names neither" into a hit and nothing else. `aec_code_side`'s hits, its venue-side and title-order checks, and `pair_agrees` read `code_reads`. `cfb-scarst-flam-2026-09-06` outcome "South Carolina State" (index 0) now maps to `aec-cfb-scarst-flam-2026-09-06` side 0 Bulldogs BUY_LONG as `grammar`; "Florida A&M" (index 1) is Rattlers BUY_SHORT; the pair agrees both ways; live it answers to the grammar class's certification as every grammar book does (the venue lists no per-side contract for this game in the dumps: `grammar_echo_unverified`).

### 10.5 Expectation table (the six rows of `his-recent_2222.log`, offline against the 2038 dump's rows, `PREMAP_YN_IDENTITY=on`)

| his slug | title / outcome | usd | tonight (`mirror_grammar_echo` ok=0, nothing certified) | once `certified["aec-cfb-washst-wash-2026-09-06"]` = {Cougars, side_index 0} (or {Huskies, 1}) | side / intent | matched_by |
|---|---|---|---|---|---|---|
| `cfb-washst-wash-…-spread-home-23pt5` | Spread: Washington (-23.5) / Washington State | 7,541 | `no_side_match:spread:subject-uncertified` (grammar-uncertified) | `asc-cfb-washst-wash-2026-09-06-pos-23pt5` (WSU +23.5 = the Cougars, a, cover +23.5) | yes / BUY_LONG | premap_spread_code |
| `…-spread-home-24pt5` | Spread: Washington (-24.5) / Washington State | 3,848 | same | `…-pos-24pt5` | yes / BUY_LONG | premap_spread_code |
| `…-spread-home-21pt5` | Spread: Washington (-21.5) / Washington State | 1,279 | same | `…-pos-21pt5` | yes / BUY_LONG | premap_spread_code |
| `…-spread-home-7pt5` | Spread: Washington (-7.5) / Washington State | 338 | same | `…-pos-7pt5` | yes / BUY_LONG | premap_spread_code |
| `…-spread-home-1pt5` | Spread: Washington (-1.5) / Washington State | 113 | same | `…-pos-1pt5` | yes / BUY_LONG | premap_spread_code |
| `cfb-scarst-flam-2026-09-06` (moneyline) | South Carolina State, index 0 | 963 | `aec-cfb-scarst-flam-2026-09-06` side 0 Bulldogs — the shadow maps it now (10.4); live: `grammar_echo_unverified` until the venue confirms the per-side contract | same | BUY_LONG | grammar (aec_code_order) |

The two small rows with outcome "Washington" (`…-spread-home-2pt5` $125, `…-1pt5` $3, same title "Spread: Washington (-L)") name ONE team twice: no pair witness, `no_side_match:spread:pair-unwitnessed` under any certification (10.7; before the fold they read b with −L → `…-pos-L` no, which is right on this game and unknowable by the rule). (The log's `his_slug` column is cut at 40 characters; which of the 2x rows is 23.5/24.5/21.5 is read from `c3_rows_2039.log` table 2, and the chain's answer is the same shape on every line the venue lists.) The venue's 1h/1q/2h school-named rows never enter: a full-game slug never crosses a segment (C3), and a `first-half-spread` slug (`fh`) does not match the venue's `1h` token — `spread:segment-absent`, unchanged.

### 10.6 What this does NOT do (left out, by design)

- No dictionary, no fuzzy step, no price evidence in code: `neg-24pt5` "Cougars cover -24.5" trading LONG 0.01 while the title says Washington −23.5 corroborates Cougars = Washington State to a reader, and the code never reads it.
- The same-event school-named rows (the 1h/1q rows put "Washington State" in the subject slot of the same identifier head) are not used to certify the full-game rows' subject: that would be a positional inference across rows (that the venue keeps the same team in the subject slot on every segment), which no venue row certifies.
- No offline read of the per-side contract rows: where `us_premap` lists `atc-cfb-<a>-<b>-<date>-<code>`, its question names the SCHOOL (the probes' `-winner-2h-bayl` "Will Baylor win the second half?"), not the mascot, so it cannot tie a mascot to a code from the table; the mascot field lives only in the live contract payload `grammar_truth` reads. The C1 class's own record is therefore the one certification, and it is per market.
- The class-level counters (`ok >= 1` on some other game) never certify this game: the C1 class opens no grammar book without per-market venue truth, and the spread inherits exactly that bar, not a weaker one.
- `live_executor._side_echo_verify` (the copy lane's post-fill echo; entry OFF by owner order, §9.8) re-derives a spread through `c3_pick` on the bought market's live rows alone — no aec row, no state — so a `premap_spread_code` fill reads `no unique live match` → `unverified`, never a false mismatch. Wiring the board and the record into that echo is the same named follow-up as §9.8's.
- The C3 pin `test_nicknames_on_the_venue_row_are_unreadable` asserted the pre-C4 refusal name on exactly these rows; its `None` result and `venue_names` assertion stand, its expected split is now `spread:subject-uncertified` (`aec-absent` on that board). One C1 round-3 pin moved with the fold (10.7, the certification fix); no other C1/C2/C3 pin moved.

### 10.7 Review fold (2026-09-06 23:3xZ) — the pair witness; the per-side contract's suffix

**The collision rule (HIGH-1, 2026-09-06 23:3xZ).** The chain resolved his outcome and his title's team independently by `code_reads`, and the code rules read each name ALONE: a first-word prefix names one code for two schools — "Ole Miss" (whole word) and "Mississippi State" (first word) both read `miss`, and `msst` is named by nothing; likewise `mich` for "Michigan"/"Michigan State", `ala` for "Alabama"/"Alabama A&M", `tex` for "Texas"/"Texas A&M", `ark` for "Arkansas"/"Arkansas State" (a one-letter residual is never the stem rule's), `flor` for "Florida"/"Florida A&M" (the split rule for `flam` is never reached once the first-word rule names the other code), and the whole-word rule collides too ("Southern Miss" reads `miss` beside `smiss`). Both names landed on position 0, the title's sign was not flipped, and "Spread: Ole Miss (-7.5)" / "Mississippi State" mapped to `neg-7pt5` yes — the Rebels' side of a Mississippi State fill. The fix is the same bar C1 holds on the moneyline (§7.8: an unnamed sibling corroborates nothing): `_c4_subject_by_code` requires a PAIR WITNESS — his outcome and his title's team must resolve to the two DIFFERENT positions — before anything else is read. Two different names on one position is `spread:code-collision` (every case above, pinned in `TestThePairWitness`); a title naming his outcome's own team ("Spread: Washington State (-23.5)" / "Washington State", "Spread: Washington (-2.5)" / "Washington") carries no second name and is `spread:pair-unwitnessed` — no single name can exclude the collision ("Mississippi State" alone reads `miss` and would buy the Rebels' side), so the four same-name rows of C3's sign table and the two small "Washington" rows of 10.5 now refuse, on every game, including washst-wash where the stem rule happens to read one name right. The four rows where his title names the OTHER team (the $7.5k / $3.8k / $1.3k / $0.3k / $0.1k rows are all of that shape) map exactly as 10.5 says; two names on two positions map on every game the rules can read (scarst/flam on the mascot rows, mich/ohiost). Both refusals are by name, before the state (an unreadable state changes nothing), and carry `code_hits` / `title_hits` in the trace. Not folded: a second name from his event title ("Washington State vs. Washington") could witness the same-name rows the way C2's yes/no branch reads `_yn_event_sides`; his event title is not in any spread row this session logged (his-recent prints no such column), so it stays a named follow-up rather than a rule built on an unobserved field.

**The deviation's real coverage cost (v3, 2026-09-07).** A fill on the FAVOURITE's token has outcome == the title's team by construction ("Spread: South Carolina State (-19.5)" / "South Carolina State", $35k at 20:28Z, is that shape), so on a mascot row every favourite-side fill refuses `spread:pair-unwitnessed`; only underdog-side fills (title names the favourite, outcome the dog: the five washst-wash rows) map through this chain. Tonight's direct cost is the two "Washington" rows, $128 (the S.C. State row was `spread:line-absent` regardless: 19.5 is not listed). School-named venue rows are untouched by this (C3's name path). **A known limit (LOW-1, v2 review).** The witness assumes his outcome and his title render a school the same way. Mixed renderings in one row — "Louisiana Tech" / "LA Tech", "UNC" / "North Carolina", "LSU" / "Louisiana State", "UCF" / "Central Florida", "Miami (OH)" / "Miami Ohio" — can read two DIFFERENT positions for one school ("Louisiana Tech" reads the opponent's `lou` by its first word, "LA Tech" reads `latech` by the split rule) and pass the witness with his team at the wrong position. A single feed market renders a team one way, so this is a feed-shape assumption, not an observed path; the event-title witness above (two names of his own, both read against the two codes) is the named follow-up that would close it, and until then the census key to watch is `premap_spread_code` fills whose `code_hits` and `title_hits` name a school the feed also spells another way.

**The certification fix (HIGH-2, 2026-09-06 23:3xZ; inherited from C1 round 3, load-bearing since C4).** `mirror_live._contract_candidates` admitted a per-side contract by its QUESTION alone: with only `atc-cfb-washst-wash-2026-09-06-wash` "Will the Cougars win?" in `us_premap`, `_contract_candidates(…, i=0, "Cougars", "Huskies")` returned that row (suffix `wash`, the OTHER code), `grammar_truth` read the same contract naming "Cougars" and said ok, and the class would have written `certified[aec] = {Cougars, 0}` — every washst-wash spread would then place the Cougars at washst, and the C1 moneyline would buy side 0 for a Washington State fill, on the venue's row for wash. Now `by_question` also requires that the suffix does NOT fit the other side: a suffix that IS the other code, or prefixes it (whether or not it prefixes ours too, `bo` for boise/bost), is never ours by its question. Pinned with exactly that case (`TestTheContractSuffixIsNeverTheOtherSides`): the candidates are `[]`, `_grammar_admission` answers `grammar_echo_unverified` with ok=0 and nothing certified; and for Huskies at wash (i=1) the wash row is the code's own contract by code, its name "Cougars" is the OTHER side's, and the truth check trips — the venue's own contradiction, caught before a dollar moves. The by-code fits (his code, a prefix of his code and of no other, the venue's short code `bu`/`h` by its question) are unchanged. One C1 round-3 pin moved: `test_a_question_naming_both_sides_is_not_our_contract` asserted that, without the other side's description, BOTH `…-bu` "Will the Bears win against the Tigers?" and `…-au` "Will the Tigers win against the Bears?" fit Baylor's side by question (`len == 2`); the `au` row's suffix prefixes `aubrn` — it is the other side's contract, the hole in miniature — so the line now reads `== [base + "bu"]`; every other assertion of that pin, and every other C1/C2/C3 pin, is unchanged. Two smaller items from the same review: the class-level counters never certify a market (mutant c: `_state(ok=5)` with no record is `spread:subject-uncertified` / `grammar-uncertified`, pinned in `test_the_class_counters_certify_nothing`), and `certified` is written through `_grammar_certify`, which pops a re-certified key before re-inserting it so tonight's certification is the newest record and the oldest is evicted (pinned in `test_a_market_certified_again_is_the_newest_record` and the live-worker record test).

**v3 (2026-09-07), the re-review's LOWs.** LOW-2, the wrong-side guard on a re-certification: `_grammar_admission` reads the record it already holds for the aec market against tonight's venue-truth reading (`_grammar_contradiction`): the SAME mascot at the OTHER position, or ANOTHER mascot at THIS position (Huskies at washst after Cougars at washst), is the venue contradicting itself — a wrong side one of the two times — and is a `mismatch` like any other: the class trips, `side_echo_mismatch`, the old record is kept, `last.detail` names both readings; it replaced the record silently before. The same reading is a refresh, and the complementary side (Huskies at wash after Cougars at washst) is the pair itself, consistent — the reviewer's literal rule (any change of `(outcome_desc, side_index)`) would trip on that, so the rule is the contradiction, not the change (pinned both ways in `TestAContradictingRecertificationTrips`). LOW-3, `fits_other` is symmetric: a suffix that EXTENDS the other code (`texas` beside `tex`) is not ours by its question either; beside HIS code the same suffix is the venue's longer rendering and fits by its question (pinned in `test_the_other_codes_contract_never_fits_by_its_question`).


## 11. C5 (2026-09-07) — a differing TEAM code is translated by the venue's own question, per game

Owner standing orders: "map more of his trades", "mirror a larger percentage of RN1's positions"; identity-only mapping, fail closed, never map on a guess. The gap, read from the venue's own rows (render-ops `epl-rows`, 2026-09-07 00:05Z, `scratchpad/hard2/epl_rows_0004.log`; the investigation in `epl_unmapped.md`):

| his feed (`trades`) | the venue (`us_premap`) |
|---|---|
| `epl-eve-mun-2026-09-06-mun` — "Will Manchester United FC win on 2026-09-06?" ($91k in 6 h) | `atc-epl-eve-mnu-2026-09-06-mnu` yes/no, LONG/SHORT, line empty — "Will Manchester United FC win against Everton FC in the Premier League match scheduled for Sep 6, 2026?" |
| `epl-eve-mun-2026-09-06-eve` — "Will Everton FC win on 2026-09-06?" | `atc-epl-eve-mnu-2026-09-06-eve` — "Will Everton FC win against Manchester United FC in the Premier League match scheduled for Sep 6, 2026?" |
| `…-draw`, `…-total-2pt5`, `…-btts`, `…-spread-away-1pt5` | `atc-epl-eve-mnu-…-draw` "Will the Premier League match Everton FC vs Manchester United FC scheduled for Sep 6, 2026 end in a draw?"; `tsc-epl-eve-mnu-2026-09-06-2pt5` "Will the total in EVE vs MNU be more than 2.5?"; `astatc-epl-eve-mnu-…-btts` "Will both teams score in the match between Everton FC and Manchester United FC on 2026-09-06 9:00AM ET?"; `asc-epl-eve-mnu-…-pos-1pt5` "Will the Everton FC cover 1.5 vs the Manchester United FC in EVE vs MNU?"; exact scores "Will EVE vs MNU finish EVE wins 2-1?" |
| `epl-ars-che-2026-09-06-che` / `-ars` ($118k) | `atc-epl-ars-cfc-2026-09-06-cfc` "Will Chelsea FC win against Arsenal FC in the Premier League match scheduled for Sep 6, 2026?" / `-ars`; `tsc-epl-ars-cfc-…-0pt5` … |

League code equal (`epl`), date equal, ONE team code equal in the same position (`eve`, `ars`), the OTHER differs per club (`mun`/`mnu`, `che`/`cfc`). The identity arm (§8) reads `yn:no-row` on every family of both events because `atc-` + his slug names nothing; C3's identifier builders (`_c3_code`) read `btts:no-row` / `spread:line-absent` / `total:line-absent` for the same reason; only the full-game total mapped, through the wording arm (his title's matchup keys fetch the event, the line is unique on the segment). Serie A (`bol`/`sas`) and Ligue 1 (`fl1->lg1`) had matched: this is a per-club code difference, not a league one. Patch C5 against 43b453c (the branch HEAD; 885a8c6 + two render-ops presets); tests in `backend/tests/test_c5_code_translation.py` (the two games' rows embedded verbatim).

### 11.1 The rule (`premap.c5_certify`, `premap.c5_resolve`) — a translation certified by the venue's question, per event, never a table

1. **Trigger.** His slug parses as `<lg>-<a>-<b>-<date>-<rest>` (any family: the moneyline's `rest` is a team code, the draw's `draw`, C3's `total-2pt5` / `btts` / `spread-away-1pt5` / `first-half-total-1pt5`). Among the venue's FULL-GAME per-team moneyline rows on his date (`atc-<LG>-<va>-<vb>-<date>-<t>`, `t ∈ {va, vb}`; half rows, draws, exact scores never count) no event carries BOTH his codes — his own event on the board, mapped or refused, belongs to the identity/alias arms and is never translated around — and EXACTLY ONE event shares exactly one of his codes in the SAME position (home stays home) under his league code; with none under his code, under exactly one other code (the C2 alias, then witnessed on both clubs). Two candidate events (a same-day twin listing) or two league codes: `yn:code-translate:ambiguous`. Zero: today's reason stands, no C5 trace. A lone per-team row (the other never named it): today's reason.
2. **Witness.** Both the shared code's row and the unshared code's row must be the venue's per-team contract — a yes row and a no row with the venue's own intents, one question, no line, no sign (`_c5_sided`) — and each question must fullmatch the measured P4 template on his date through the reading C2 uses (`_yn_team_question`, factored out of `_yn_team_row` unchanged: fold, `pmus._YN_Q_PATTERNS`, `_yn_date_ok`, the club scope screen on subject and opponent, the league rule on the league slot). The two rows must NAME EACH OTHER (`pmus._yn_name_match` both ways). Then HIS OWN WORDS must name the unshared club: his title when this market is that club's moneyline (`_bridge_title_subject`, C2's title witness — the same normalisation: case, `pmus.fold_latin`, the pinned `GENERIC_CLUB_TOKENS`); else his moneyline title on that club as `trades.market_title` / `markets.title` store it for the slug `<lg>-<a>-<b>-<date>-<code>` (one bounded read, `_C5_TITLES_SQL`: trades through `trades_ts_idx` with a 3-day bound, markets through `markets_event_idx` on his event slug — no new index, see §11.3; `LIMIT 9`, and nine distinct rows back is `yn:code-translate:titles-overflow` — a title past the bound could be the one that disagrees; two stored titles naming different clubs witness nothing); else the side of his event title (`markets.event_title` — `trades` stores no event title, `001_init.sql`) that is not the venue's shared club. A total's, a btts's or a draw's matchup title is NOT read as a code witness (the brief's rule: a title that names no one club binds no code). Nothing readable: `yn:code-translate:unwitnessed` (with `why`: `no-event-title`, `event-title-shape`, `event-title-other-side`, `titles-disagree`, `venue-lined`, `venue-intent`, `venue-sides`, `venue-question`, `shared:yn:…`, `unshared:yn:…`). A name that is not the venue's: `yn:code-translate:name-mismatch` (`his_club`, `venue_club`; `why: rows-disagree` when the venue's two rows disagree with each other). Under an alias the shared club must be his words too (his event title's other side, or his stored moneyline title on it).
3. **Rewrite, then the lane as today.** His slug is rewritten to the venue's codes (`epl-eve-mun-…-mun` → `epl-eve-mnu-…-mnu`; the tail is replaced only when it IS the translated code) and `resolve`'s own calls run on the same prefix- and segment-kept rows: `match_side(…, yn_identity=True)` (the yes/no identity arm, the draw's event/title witness) and `c3_pick` (totals by segment, btts, spreads). No family gets a side rule here; a lane refusal on the rewritten slug is named by the lane (`total:line-absent`, `yn:subj` …) with `yn_c5.certified = true`. The hit keeps the lane's own `matched_by` (`premap_identity`, `premap_draw_title`, `premap_fh_total`, `premap_btts`, `premap_spread`) and source `premap`, and carries `code_translated: {"mun": "mnu"}` and `witness: atc-epl-eve-mnu-2026-09-06-mnu` (the venue row that named his club) — `resolve` returns both, `resolve_explain` carries them with `yn_c5` (`label: yn:code-translated`, `witness_src: title|trades|event`, `event`, `shared`, `pair`, `slug`, `his_club`, `venue_club`). The translation lives in the one call: nothing is stored, so another date or opponent derives its own or none (pinned: the 2026-09-13 return fixture certifies from its own rows; 2026-09-07 certifies nothing).

Consulted only after every other arm found nothing (wording, identity/alias/draw, C3, the named lane), inside `PREMAP_YN_IDENTITY` like all of C2/C3: off, every row answers exactly as before. NEVER a static club-code table, never a prefix/similarity match on codes (`mun`/`mnu`, `che`/`cfc` look alike — that is not evidence; pinned: a code with nothing in common, `xyz`, is certified by the question alone, and the near-twin `mnu` naming Manchester City refuses). The one new read (`trades`/`markets` titles) happens only once a single candidate event exists and the market's own title names no club; an unreadable store is unwitnessed.

### 11.2 Expectation table (the verbatim rows, offline, `PREMAP_YN_IDENTITY=on`)

| his slug | outcome / event title | venue identifier | side / intent | matched_by | witness (src) |
|---|---|---|---|---|---|
| `epl-eve-mun-…-mun` | Yes (No) | `atc-epl-eve-mnu-2026-09-06-mnu` | yes / LONG (no / SHORT) | premap_identity | `…-mnu` (title) |
| `epl-eve-mun-…-eve` | Yes, event title | `atc-epl-eve-mnu-…-eve` | yes / LONG | premap_identity | `…-mnu` (event) |
| `epl-eve-mun-…-draw` | Yes, event title / no event title + his `-mun` title stored | `atc-epl-eve-mnu-…-draw` | yes / LONG | premap_identity / premap_draw_title | `…-mnu` (event / trades) |
| `epl-eve-mun-…-total-2pt5` | Under | `tsc-epl-eve-mnu-…-2pt5` | under / SHORT | premap (the wording arm, as before C5) | — |
| `epl-eve-mun-…-first-half-total-1pt5` | Under, his `-mun` title stored | `tsc-epl-eve-mnu-…-fh-1pt5` (identifier attested; the fh line is an expectation) | under / SHORT | premap_fh_total | `…-mnu` (trades) |
| `epl-eve-mun-…-btts` | Yes | `astatc-epl-eve-mnu-…-btts` | yes / LONG | premap_btts | `…-mnu` (event) |
| `epl-eve-mun-…-spread-away-1pt5` | Manchester United FC (−1.5, b) | `asc-epl-eve-mnu-…-pos-1pt5` | no / SHORT | premap_spread | `…-mnu` (event) |
| `epl-ars-che-…-che` / `-ars` / `-draw` / `-btts` | Yes / Yes / Yes / No | `atc-epl-ars-cfc-…-cfc` / `-ars` / `-draw`; `astatc-…-btts` | as above | premap_identity / premap_btts | `…-cfc` |
| a moneyline with no `markets.event_title` | | `no_key_intersection` — his moneyline title builds no matchup key, the rows are never fetched (a lookup fact, unchanged) | | | |
| draw / btts / fh total with no event title and no stored moneyline title | | `no_side_match:yn:code-translate:unwitnessed` (`why: no-event-title`) | | | |
| the venue's rows naming Manchester City on both sides | | `no_side_match:yn:code-translate:name-mismatch` — for EVERY family of the event | | | |
| a same-day twin listing under another club code | | `no_side_match:yn:code-translate:ambiguous` | | | |
| his own `atc-epl-eve-mun-…` on the board and refused (`yn:subj`) | | `yn:subj`, no C5 trace | | | |

### 11.3 The census, and what is left out

`resolve_explain`'s `split` carries the three refusals, so `explain_unmapped` prints `no_side_match:yn:code-translate:…` and MIRRORUNMAP buckets them; a mapped translation is `resolves` with `code_translated` / `witness` / `matched_by` and `yn_c5.label = yn:code-translated`. Two reporting-only changes from the investigation: (i) the one current path that left a yes/no label BARE — picked by `_yn_pick`, vetoed by `match_side`'s line/identity guard (`epl_unmapped.md` §1.3) — is now `no_side_match:yn:picked-vetoed`, so a bare `no_side_match` on a yes/no row can only mean "judged by older code"; (ii) `MIRRORUNMAPMKT` in `engine-diagnostic.yml` prints `at=` (when the `.latest` row was written; `mirror_report.summarize` keeps it), because the shadow re-judges only the 20 newest-touched markets a tick and a market whose fills stopped keeps its last verdict — the stale EPL labels of 2026-09-06 were that (§5.2 of the investigation). Nothing here re-judges a stale row; the rows above are re-judged when the shadow next reads them (or the mapgap endpoint re-runs `resolve_explain`).

**Review fold v2 (2026-09-07).** MEDIUM-1: the club-name EQUALITY rule (`pmus._yn_name_match`, token-set equality, no containment) is pinned in both prefix directions — his "Will Manchester win on 2026-09-06?", an event title "Everton FC vs. Manchester", a stored "Will Manchester win …" and a venue naming "Manchester United FC Salford" all refuse `yn:code-translate:name-mismatch` (`his_club: manchester`); a `startswith` reading either way would have certified. LOW-2: the stored-title read is bounded fail-closed (`LIMIT 9`, nine rows → `titles-overflow`, never eight read as agreement) and both legs read through indexes that exist: `trades` by `trades_ts_idx` (`ts >= now() - interval '3 days'` — his fills on a dated game; an older market falls to the event title), `markets` by `markets_event_idx` (`event_slug = <lg>-<a>-<b>-<date>`, the kindless form the feed stores; a different stored form finds nothing and falls to the event title). No index migration: `scripts/migrate.py` runs every file inside `conn.transaction()`, so `CREATE INDEX CONCURRENTLY` cannot be used (migrations/053's header), and a plain `CREATE INDEX` on `trades` would hold a SHARE lock against every ingestion INSERT for the build, on the API's boot path — not safe live on Render; `markets(slug)` likewise left out. Reading `markets` by `condition_id` does not apply: the witness is ANOTHER market's title (the unshared club's moneyline), not the market being resolved. LOW-3: the shared row alone, the unshared row alone, or neither: no witness, today's reason, no C5 trace (pinned per case). Observation B (the venue's two questions swapped against their identifiers, still naming each other): where his words bind the code — his own title on that moneyline, or his stored moneyline title — the refusal is `name-mismatch` with `why: rows-swapped` (his club is the SHARED row's subject); where only his event title witnesses, nothing of his binds a code to a name, so the pair certifies on the names the rows carry and the record carries `his_club` = `venue_club` (the club the witness matched, the unshared row's subject) beside `shared_club` (the shared row's subject), so the swap is readable in the plan; the family lanes read names on their own rows, so no side is wrong (the moneylines refuse `yn:subj` / `name-mismatch`; btts, spread and draw land on the event's own rows — pinned).

Left out, by design: no club-code table and no similarity of codes; no witness from a total's, a btts's or a draw's own matchup title (per the brief — the event title or a stored moneyline title is required; a moneyline without a stored event title never reaches the rows anyway); no re-judging of stale shadow rows (§5.4 of the investigation, a report-side change); the live worker's memo (`mirror_live`) unchanged; `_norm`, `side_norm`, `match_side`'s branches, `c3_pick`'s side tables and `_yn_team_row`'s refusal names unchanged (`_yn_team_question` is a factoring, byte-identical outcomes); the copy lane's own yes/no lane (`pmus.resolve_team_yesno_exact`) untouched; no separate switch (one switch for C2/C3/C5, as the C3 review settled). The venue's `event_title` for the two EPL events was not printed by `epl-rows`; the fixture assumes the naming its questions carry (the lookup key only — every decision reads the question).

## 12. E2 (2026-09-06) — the latency half: what the census now carries

Owner, ~22:50Z: "I want this firing as frequently as his. Make the latency as low as possible." The build is described in `docs/mirror-to-a-tee-program.md` §5a (E2). The census keys it adds or reads, all in `mirror_live.CENSUS_KEYS` past the served 40-key prefix (raw heartbeat and this module's tests; `integ` is at its ceiling):

- `venue_calls` — every venue REQUEST the tick made, as events: paced reads (quotes, order status, open orders, recent trades, the flatten's bid), cancels, placements (a BUY is a preview and a create: two; a SELL one), closes, every page of the tick's positions walk (`ms.account_positions_pages`) and of the flatten's own reading of the venue's positions (`ml._pm_held`, paced through this lane's gate), the grammar class's market reads and echo, the mapping lane's resolver calls (`map_venue_read`, one for one). Since the review every WRITE of this lane also claims its gaps on the process-wide pacer, so the rate the venue sees is the pacer's (one request per 0.35 s from this process); this counter measures what a tick spends of it.
- `venue_calls_capped` — the soft guard: at `rules.MIRROR_VENUE_CALLS_PER_TICK` (default 80 since review round 2 — 40 candidate reads plus 20 BUYs at two requests each; `capped_env`) the CANDIDATE walk stops for the tick (`capped_tick` beside it). The guard counts the tick's WRITES and its CANDIDATE reads alone — never an open book's read (the tick's fixed cost) — so 40 candidate reads are reachable at any book count; no exit is bounded by it. A candidate a memo or a book already skips is not counted as cut.
- `rate_limited` — every site that read a venue 429 (E2 review round 2; EVERY placement site since round 4): a placement — the post-only rest's create (the adapter's 4xx refusal, `status_code` 429), and since round 4 a BUY's PREVIEW and an IOC take's create too, the two requests the adapter does not wrap (they RAISE the SDK's `RateLimitError` through `_guarded`; read by class/status before the lost-response path, the row is refused `place_refused:rate_limited` with a receipt naming the raise, the room goes back, nothing is frozen and no open-orders search is made: a 429 is the venue refusing the request before it processed it, so nothing was sent and nothing is lost) — the tick abandons `rate_limited` at all three, and skips the backoff while the circuit holds; since round 5 the flatten's two placements too — the sole-holder `close_position` whose adapter caught the SDK's `RateLimitError` into a `close_failed` raw naming it, and the co-held IOC's create raising it — are the same named refusal on their row (never the lost-close freeze, never the open-orders search; a socket reset on either is still lost); a quote read (`_bbo`, never counted as `no_quote` and never a step or a reset of the outage streak); a cancel whose error names 429/RateLimit (each attempt); the tick's positions walk (abandons `positions_unreadable`, and since round 4 skips the 60 s backoff too while the circuit holds). Each trips the process-wide pacer's circuit: the gap is ×2 for 600 s for every lane (`venue_pace.penalize`, `penalty_left`). The match is the SDK's `RateLimitError` class, a `status_code` of 429, or an error text that starts with the name / '429' / 'Too Many Requests' — never '429' inside an id, a slug or a price (round 3); at the placement sites the refusal raw is read by its NAMED fields — `status_code`, `error_type` (the adapter now carries the exception's class name on the 4xx refusal, as `close_position` does), `error`'s head — never as a substring of the whole raw (round 4: a `preview_mismatch` whose `expected_cost` is 429.0 or whose quantity is 429 is a mismatch, and a 400 saying "would cross at 0.429" arms the take and abandons nothing). An int `status_code` is authoritative (round 5): 429 is a rate limit and any other int is not whatever the text's head says — a 400 whose message begins "429 contracts exceeds the maximum order size" is a size refusal; `error_type` and the text are read only when no int status is there (the `close_failed` raw, a cancel's error string).
- `place_refused` — a placement the adapter refused without minting an order (`place_refused:<status>` on the process census: `preview_mismatch`, `preview_unreadable`, the short gate's names, and since round 4 `rate_limited` for a 429 raised by the preview or an IOC create; since round 5 the flatten's sole close and co-held IOC too). The row is `rejected` with the adapter's raw (or, for the raised 429, `{error, error_type, status_code}`) as its receipt.
- `walk_error` — the walk-order streak judge raised after a book (logged, counted; the game's walk goes on).
- `backoff_skipped_circuit` — a 429 abandon — a placement's (`rate_limited`) or the positions walk's (`positions_unreadable`) — that skipped the 60 s backoff because the pacer's circuit already holds (review round 3; the walk since round 4); the next tick runs at the doubled gap. The skip is an explicit flag the 429 sites pass to `_abandon` (`rate_limited=True`, always after `_rate_limited` tripped the circuit), never the reason's name. An outage abandon still backs off, a circuit from an earlier 429 notwithstanding.
- `requotes` (existing) — since round 3 a TTL/replace cancel and its re-rest are ONE op (`ops` counts the pair once); `venue_calls` still counts every request.
- `abandoned_in_flight` — a decided order (an entry or an exit) refused because another book abandoned the tick while this one was in flight under the parallel walk; NO plan is written for that book, so its updated_at stays and E1's walk reads it as unreached and walks its game first on the next tick (`tick_abandoned` counts the abandon itself, once).
- `take_at_his_level` — the bounded take's wait (`MIRROR_TAKE_AFTER_S`, default 20 s since E2, env lengthens only) has run AND the book is at or through his level: one IOC at the same wire follows (`take_placed`, `placed_take` count the placement itself).
- `take_refused_price` — the wait has run and the book is NOT at his level: no IOC, the rest stands (`resting_above_level` beside it on the rest path; on the armed path the arm is cleared `market_away` as before).
- `take_capped` / `replace_capped` — read them knowing an EXIT is never behind them since the review: a reduce, a flatten or a plan on the other side of the rest cancels and goes out whatever the hour's count (`_exit_or_flip`); the two names now count entry churn alone. `take_arm_stale` fires past max(2 × wait, 60 s), never sooner.
- `ops_capped` — read it against the new budget of 20 (was 6); a placement whose refusal came before the venue write no longer spends an op (`_op_slot`).
- `map_reads_capped` — unchanged in meaning; `MIRROR_MAX_MARKETS` is 40 (was 20), so compare the before/after census on the same hour of his flow.
- the candidate read budget the live lane reads is its own: `mirror_live.MAX_MARKETS_PER_TICK` (env `MIRROR_LIVE_MAX_MARKETS`, 40); the shadow's `ms.MAX_MARKETS_PER_TICK` is back at 20.
- `tick_s` — the wall time; the parallel walk (`MIRROR_BOOK_CONCURRENCY`, default 6) takes it toward the pacer's floor (~0.35 s a paced read), not under 10 s on 46 books, which is why `POLL_S` stays 30 s.

## 13. E4 (2026-09-06) — exits at his price, within one cent; entries take first

Owner, ~23:24Z: "we should exit when he exits at his price or within 1c variance (tolerance)"; the addendum, ~23:38Z: "Remove the 20 second wait on entires too". The build is described in `docs/mirror-to-a-tee-program.md` §5a (E4). The census keys it adds, all in `mirror_live.CENSUS_KEYS` past the served 40-key prefix (raw heartbeat and this module's tests), and the plan fields beside them:

- `exit_take` — an exit's IOC SENT (counted at the placement, beside `take_placed`; review LOW-4): the bid at or through the take cent (the lowest cent at or above his exit price less `rules.MIRROR_EXIT_TOL`, 0.01, env tightens only, read at call time), on the tick it is there, a standing rest cancelled first. `take_at_his_level` / `take_first` are the ENTRY's names and never an exit's. A priced short cover's close carries `exit_close_bips` / `exit_close_ask` on the plan: the slippage the ceiling leaves above the ask read just before the close (1 at the ceiling; review MEDIUM-2).
- `exit_out_of_tol` — an exit held outside the cent: a long book's SELL with the bid under the take cent (the rest stands at his cent, ceil(his price), nothing chases), or a short book's cover with the ask over the ceiling (his buy-back plus the tolerance; no close_position, no position read, no freeze, read again next tick). The plan carries `exit_out_of_tol` {bid, ask, floor | ceiling, at}.
- `requote_same_wire` — an exit rest past `MIRROR_REST_TTL_S` at the same side, cent and quantity, left standing: the TTL re-quote it would have been is a no-op (book 29's fourteen). Counted on every tick the rest stands past its TTL; `requotes` does not move. An exit rest at another cent or quantity is replaced as before, exempt from the replace budget (`replace_capped` / `take_capped` never name an exit).
- `take_first` — an ENTRY's IOC sent before any rest (the addendum): the ask at or through HIS CENT (`rules.buy_wire(his)`, the IOC's limit; review HIGH-1 -- the rest's wire sits under the bid and is never "at or through" in a normal book) on the tick the increase is planned, `MIRROR_TAKE_AFTER_S` being 0. A long book's entries only; a short book's add rests as before. The unfilled remainder rests post-only at the same wire (`rest_placed` beside it); the IOC cancels nothing, so it is never a replace. Under a lengthened wait (env) E2's rest-first take runs: a take off a standing rest counts `take_at_his_level` alone, and the armed take with no rest standing (the arm's age is the wait) still counts here, since nothing was cancelled before it.
- plan fields — `exit_px` (his exit price, unrounded), `exit_px_src` (`his_fill`, or `none` for a snapshot-driven reduce, an unpriced vanish or the admin flatten, whose prices are the old ones), `exit_floor` / `exit_rest` / `exit_take` on a long book, `exit_ceiling` / `exit_cover` on a short, `requote_same_wire: true` while a rest stands past its TTL, `take_qty` / `take_filled` on the tick an IOC went out.
- reading a held book — `exit_out_of_tol` rising tick after tick on one book with `requote_same_wire` beside it is the rule working: he exited, the market is more than a cent past his price, and the book waits at his cent. It is not a stuck book; a stuck book names `open_order_pending` with neither.
- meeting E2's re-quote credit (§12; E2 review round 3, LOW-6 — a TTL or replace cancel and the rest that follows it on the same book are ONE op, `_Tick.requote_credit`) — the credit is granted by a TTL or replace cancel alone and spent by a non-IOC placement alone, and a `_Tick` is built per tick, so no credit outlives one. On the same-wire path no cancel is sent, so no credit is ever granted there (nothing to clear, none to go stale). An IOC never spends it: an exit's take after a TTL cancel of an entry rest is the cancel's op plus its own (`ops` 2); the take-first after a TTL cancel is the cancel's op, the IOC's own and the remainder's rest on the credit (`ops` 2); an IOC the budget refused (`ops_capped`) with the credit standing rests the plannable quantity on the credit instead (`_entry_take`), so a TTL cohort's crossing book is not left bare for the tick.
- `ops_capped` never names an exit (E4 review round 3, 2026-09-07, M-1) — EXITS ARE EXEMPT FROM THE PER-TICK OPS BUDGET: every cancel, IOC, rest and close on an exit's path (a reduce, the paired and the vanish flatten, the sign-flip flatten, a short book's cover, the TTL re-quote of a reduce rest) takes its slot whatever the count (`_op_slot(exit=True)`; `_place` reads it off the side's leg action) and is still counted in `ops`, on `venue_calls` and the soft guard; only entries are refused `ops_capped`, exactly as before (E2's take-first fallback on the credit included). Before the fold the exit's take at the budget's last op cancelled the rest and had its IOC refused, leaving the book with no exit order for the tick. Read `ops` above the budget as exits going out.
- `cancel_refused:<reason>` (plan reason, round 3 L-4) — a take's or a replace's cancel of the standing rest was refused before it went out (`ops_capped`: an entry's take or replace at the cap; an exit cannot reach it): the rest stands, nothing was sent, and the plan says so — never `take`. A cancel that went out and left the order unknown reads `cancel_pending`, the freeze's name, on the take paths as on the replace path.
- the TTL of a reduce rest (round 3 D-1/D-2) — step O's TTL clause skips only a PRICED exit rest (the book's last plan says `exit_px_src: 'his_fill'`: the rest is at his cent and `requote_same_wire` keeps it; with no plan on file, a long book's rest whose wire is the cent of its own recorded `his_level`); an UNPRICED reduce rest (`exit_px_src: 'none'`; with no plan on file, a rest the ask lifted or one with no level of his) keeps its `ttl` re-quote — reason `ttl`, `requotes` 1, never `replace`, never counted by `_SQL_REPLACES` against the book's entry budget — and is TTL'd through an abandoned tick as before E4.

## 14. S4 (2026-09-07) — the short cover as a priced order; the read-back proof

Every short close on 2026-09-07 failed within a second. With 4a5da1f's logging the venue's reason reads `pmus: close_position aec-itfwo-wuszhe-jiaqwan-2026-09-07 failed: BadRequestError: Price is required for limit order` (03:30:06Z, 03:50:54Z; the same for aec-itfme-yuatak-kazido at 04:29:56Z and 04:51:16Z and aec-itfme-kosuz-chisat at 04:51:52Z): 11 CLOSE rows, 0 executed. The venue refuses `POST /v1/order/close-position` as an unpriced limit order, so the short cover is a REAL order through the placement machinery the long side uses — a BUY of the long token with the closing intent (`ORDER_INTENT_SELL_SHORT` on the wire: the row's side is BUY_LONG, its intent SELL_SHORT, exactly the CLOSE row's labels) — never `close_position`. The rule is E4's mirrored for a short book (`rules.exit_terms(BUY)`): the cover REST post-only at floor(his buy-back price) to the cent (never above him; under the ask whenever the take did not fire), ONE IOC at the ceiling cent (his price + `MIRROR_EXIT_TOL`, buy_wire'd) whenever the ask is at or under it, on the same tick, no wait, never twice per rest, a partial IOC leaving no remainder resting; outside the tolerance the rest stands (`requote_same_wire`), held and re-checked every tick, no freeze. Exits stay exempt from the ops budget, the replace budget and the loss stop. The census keys:

- `s4_probe_placed` — the 1-share read-back probe went out: on the first tick that holds a LIVE short book with a two-sided quote (bid < ask, the market OPEN) while `mirror_s4_proof` is not proved — ONE post-only BUY of the long token with the closing intent at max(0.01, floor(bid − 0.05)) to the cent (off-market, cannot fill; the bid is the book's own `_bbo` read, the LONG token's space the cover's cents are priced in — S4 v2, F2), read back through `open_orders` on the slug (price, quantity, the venue's OWN side and the `intent` as the venue reports them), then cancelled. No mirror_orders row; its two writes are exit ops and counted venue calls, paced. At most once per hour while unproven, never on a frozen book, never twice in a tick, never on a locked or one-sided book, never when the key is unreadable (a malformed key is unreadable: nothing is placed on a key we cannot record into), never on a book with an order standing (S4 v3: a probe that fills needs the book's open-order slot for its row), and never while the record holds a probe order the venue KEPT (S4 v3, GAP 2, below). A 429 on the probe's placement (S4 v2, F3: the create's post_only_rejected raw with `status_code` 429, or the SDK's RateLimitError raised) is E2's rate-limit path — `rate_limited`, the pacer's circuit, the tick abandoned `rate_limited` with the backoff skipped while the circuit holds — and (S4 v3, GAP 3) writes `{"proved": false, "why": "rate_limited", "at": now, "echo": {raised, error}}` so the hour's hold applies: one abandon, not one per tick for as long as the venue limits creates (v2 wrote no record and the probe was the first write of every tick); the `recent` row says `s4_probe: rate_limited`, the plan `s4: {unproven: rate_limited}`. A 429 is still no verdict about the order: the record is the hold.
- `s4_proved` — the read-back matched: price, quantity 1, the slug, the venue's own side `ORDER_SIDE_BUY` (`ml.S4_VENUE_BUY`: the SDK's Order.side, carried verbatim by `pmus._norm_order` as `venue_side` beside the desk's derived `side` — S4 v2, F1), `intent` `ORDER_INTENT_SELL_SHORT`, the order's `state` one of the venue's STANDING states (`ml.S4_RESTING_STATES`: `new`, `pending_new`, `pending_risk` — the SDK's OrderState enum with the prefix dropped; a filled, cancelled, rejected, expired or replaced echo, or one in no state, proves nothing about a standing cover — S4 v3, INFO 5), and the cancel accepted. The key records `{"proved": true, "at", "slug", "order_id", "price", "bid", "ask", "echo": {order_id, us_market_slug, side, venue_side, intent, price, quantity, state, tif}, "cancel"}` (`bid`/`ask`: the quote the probe was placed against); the cover goes on the same tick and every tick after.
- `s4_unproven` — a flatten cover (paired, vanished, the sign flip) refused because the proof has not passed: the key absent, unreadable/malformed, or a recorded mismatch (`why`: `wrong_price`, `wrong_quantity`, `wrong_slug`, `wrong_side`, `side_missing`, `wrong_intent`, `intent_missing`, `not_found`, `read_back_raised:<Type>`, `place_refused:<status>`, `place_raised:<Type>`, `filled_at_create`, `cancel_failed`, `record_failed:<Type>`, and since S4 v3 `wrong_state`, `state_missing`, `rate_limited`; the echo beside it). A `cancel_failed` record KEEPS the probe's `order_id` with `cancel: {ok: false, error, retries, at}` (S4 v3, GAP 2): the venue kept a 1-share order of ours, so every tick after — before the walk, the tick's first venue call (`_s4_cancel_retry`, paced through `_guarded`, an exit op) — the cancel is retried and NO second probe goes out beside it; the venue accepting the cancel, or a refused cancel followed by the order's own status reading cancelled / expired / rejected / replaced or no record of the id, clears the id (`cancel: {ok: true, order, gone, retries}`) and restarts the hourly clock from then (`at`); a status reading FILLED is a share of ours (`cancel.filled`), booked on the book's next walk as a filled probe (`s4_probe_filled`) and then cleared; a 429 on the cancel is E2's path for a cancel — `rate_limited`, the circuit — the id kept and retried next tick, the tick not abandoned. The `recent` row `s4_probe_cancel` says which (`refused` / `rate_limited` / `filled` / `gone`). `wrong_side` is the venue listing the probe as ORDER_SIDE_SELL — an order in another price space (a venue reading a SELL_SHORT limit in the short token's space rests the probe on a high-priced book with our price and intent, and every cover after it would rest where it can never fill); only the venue's own side tells it apart. The book is held live, nothing sent, no freeze; the plan says `s4: {unproven: <why>}`. A PARTIAL short reduce (his partial buy-back) is refused under the pre-S4 name `short_reduce_unproven` until the proof has passed in production — S4 unlocks it: proved, the partial reduce is the same priced cover for the reduce quantity.
- `s4_probe_filled` (S4 v3, GAP 1) — the probe EXECUTED: the post-only latch tripped and the venue filled our 1-share off-market cover at create (the 23:16Z shape), or a probe the venue kept filled before its cancel got through. The share is ours, so it is BOOKED exactly as a cover fill: an ordinary mirror_orders row of kind `s4_probe` (side BUY, intent SELL_SHORT, qty 1, GTC post-only as sent, wire = the probe price, the fill's price, `taker_at_placement`, maker false, state filled) and the ledger through `_book_delta` — one share toward zero, the realized on it, the standing row's shares — so `_cover_qty` sizes the next cover off the ledger the row moved (a filled probe on a 300 short leaves 299 to cover, never 300 against a venue short of 299). The record says `why: filled_at_create` with `booked: {row, booked, ledger}`; the cover stays gated (`s4_unproven`) for the hour. A row that cannot be written leaves the share on the venue and not on the ledger — inside the one-share tolerance the venue/ledger read never names — so the book is frozen `s4_probe_unbooked` by name with the order and the error (`booked: {row: null, error}`).
- `short_cover_rest` — the cover's rest at floor(his) went out (GTC, post-only, no good-till; the row kind is the plan's: `reduce`, `flatten_paired`, `flatten_vanished`).
- `short_cover_take` — the cover's IOC went out (kind `take` at the ceiling cent; or the unpriced vanish's bounded IOC under the flatten kind). `short_flatten_close` now counts a short book's leg covered to flat by any cover order (the name `close_position`'s fill used to carry).
- `short_cover_out_of_tol` — the cover held outside the cent (beside `exit_out_of_tol`; the plan's `exit_out_of_tol` carries bid/ask/ceiling).
- no price of his (`exit_px_src: 'none'`: a snapshot-driven reduce, a vanish with no fill of his, the admin flatten) — the cover is HELD and named `no_price` with `cover: unpriced_held` on the plan, never rested at the ask; the ONE exception is the confirmed vanish / the sign flip with his position on our side gone: one IOC at the ask bounded by `le.buy_limit_price` (the ask + `rules.MIRROR_FLATTEN_SLIP` 0.02, `sell_limit_price` mirrored) for our quantity, `cover: {ioc_at_ask, limit, why}`. (A sign flip is priced by construction: the fills that carry his net across zero are the cover-side fills.)
- the sole/co-held question is gone from the short side: a cover is a clamped order of OUR quantity; `_flatten_send` (the long slippage leg) refuses a short at its head (`book_error`). `_reconcile_lost_close` stays for the legacy CLOSE rows on file; new covers are ordinary rows (tif GTC / IOC) that `_reconcile_placing` / `_find_lost_placement` cover — the fingerprint matches INTENT to intent whenever the row (the 050 column) and the venue row both name one (S4 v2, F4: the cover's SELL_SHORT, a short add's BUY_SHORT; the plan side BUY and the venue row's derived side 'SELL' never agreed on a cover, so a lost cover response was never adopted), side to side when either lacks one. A lost cover at our cent, our quantity, our intent, inside the window, is adopted in `_lost_response` and in step O; one the search cannot find freezes the book `placement_lost` like any rest (never silently live); a cover adopted on a frozen book is cancelled under the freeze's name, the book thawed and the cover rested again. Plan fields: `exit_px`, `exit_ceiling`, `exit_cover`, `exit_rest`, `exit_px_src`, `s4`, `cover`.

## 15. W1 (2026-09-07) — the two lanes stop spending their ticks on finished markets; the shadow names the venue's word

Investigation `gap_book_unreadable.md` (13:50Z): of the 64 markets / $388k whose LATEST shadow verdict read 'no mark: book unreadable', 99.3% of the rows inside his own trading window read the venue OPEN with a two-sided book; the first terminal read (MARKET_STATE_EXPIRED) lands 2-14 min AFTER his last fill on 60/60; 0 halts, 0 raised reads in 8,740 rows. The label was the census reading the NEWEST row of a market the shadow kept re-judging for LOOKBACK_H after the venue expired it. On the live lane the same shape cost the tick: at 13:44Z 26 of 27 book reads were on EXPIRED markets whose `markets` row still read closed=false (two paced calls each, >= 18 s of a 20.4 s tick). Four changes, measurement first, one live change, no order path touched:

- R1 `mirror_shadow.no_mark_reason` — the shadow's `no mark: book unreadable` is split by the reading the row already holds, the venue's string as read and never normalised: `no mark: venue state <STATE>` (any non-OPEN state, terminal or a halt), `no mark: read failed <Exc>` (bbo_error), `no mark: no state, empty` (state None, no error), `no mark: bid only <px>` (OPEN, a bid, no ask — a decided market's 0.99), `no mark: quote off ladder <bid>/<ask>` (OPEN, a quote the mark rule refused), `no mark: empty open book` (OPEN, neither). Nothing else on the row moves (target 0, would_side None, the exit leg's `exit_reason` carries the same string). Every consumer reads the PREFIX `no mark:` (`ms.NO_MARK_PREFIX`; render-ops `unread-rows` reads `LIKE 'no mark:%'`, so the older rows still match). `mirror_report.summarize` gains `no_mark_by` — per class (`mirror_report.no_mark_class`: the figured classes `bid only` / `quote off ladder` collapse to the class name), the markets whose newest row carries it and the rows in the window. Pinned on the six verbatim readings of `hard2/unread_repro.py` (`test_no_mark_names_the_venue_state`). Not in scope: the shadow still marks a quoted non-OPEN book (CLOSED with stale rests, bid 0.01 / ask 0.20) off its quotes — only the live lane's `_bbo` refuses that quote.
- R2 `mirror_shadow._terminal_until` — the shadow's own D1: a market whose read carried a state in `STATE_TERMINAL` is remembered per (whale, condition_id) for `UNMAPPED_TTL_S` and skipped in the tick loop beside `_unmapped_until`, counted `skipped_terminal` on the heartbeat. One `no mark: venue state …` row, then silence, so the newest row per market is the last reading that could plan and the 20-slot tick stops spending slots on finished matches. NEVER for HALTED / SUSPENDED / PREOPEN (they reopen), never when the state was unread; the miss-streak rule is unchanged. Pinned: `test_a_terminal_read_leaves_the_read_set_for_the_ttl`, `test_a_halted_read_never_leaves_the_read_set`, `test_an_unread_state_never_leaves_the_read_set`.
- R3 render-ops `coverage-gap` — `sh` is now, per market, the newest shadow row written at or before his last fill in the window whose `detail->>'state'` is not one of the four terminal strings, falling back to the newest row; the new `judged` column prints when that row was written. Read-only; the second statement stays self-contained.
- R4 `mirror_live._terminal_book_until` (+ `_terminal_book_state`, the state string for the plan) — the live lane's one change. In `_tick_book`, AFTER step M (the markets row and `_maybe_close_episode`, which still run every tick) and before the quote read: a book inside the memo skips the quote read AND the per-market position read and writes the plan `{"kind": "no_plan", "venue_terminal": <state>, "at": now}` under the existing `no_mark` name — census `no_mark` as today plus the new `book_terminal_skipped` (inserted before the pinned last key `cand_terminal_skipped`; past the served prefix, read off the raw heartbeat). The memo's ONE writer is `_memo_terminal_book`, off the book's own read (`_bbo`, book=True): a state in `ms.STATE_TERMINAL`, and nothing open on the book (`t.open_by_book` / `t.nonterminal` — the cancel the first terminal read sends under `no_mark` must have landed; the memo is written on the next terminal read). NEVER on HALTED / SUSPENDED / PREOPEN (read every tick as before), never on an unread state, never from a candidate read (that stays D1's `_terminal_until`). Fail-closed: a terminal market accepts no order; a book with a resting order is read until the rest is gone; the close lands the tick the row reads closed, inside the memo, with no read spent. Effect at 13:44Z's shape: 26 x 2 paced reads leave the tick. Pinned: `test_an_expired_book_is_read_once_per_ttl_and_still_closes_on_the_markets_row`, `test_a_halted_book_is_read_every_tick`, `test_a_book_with_an_open_order_is_never_memo_skipped`, `test_the_book_memo_never_sets_from_a_candidate_read` (which also re-runs `test_the_shadow_never_touches_an_order`).
- Reading the census after W1: `no_mark` on the live heartbeat now counts memo-skipped books too (one per book per tick, as before the read was spent); `book_terminal_skipped` beside it says how many spent no read; `snap_market_unreadable` stops counting expired markets. On the shadow, `skipped_terminal` beside `skipped_unmapped`; `no_mark_by` on the report. The R5 halt cadence and the R6 loss stop of the investigation are unchanged (owner decisions, not rules).
## 16. R1 (2026-09-07) — the league-stripped pair key; the census names an unlisted league

Owner standing orders as §11. The gap, read from the venue's own rows (render-ops `league-census` / `league-rows` 13:51Z, the 19:09Z dump; `scratchpad/hard2/gap_league_codes.md`): his feed and the venue code the same game under DIFFERENT league codes with the team codes and the date byte-identical — `nor-kbk-tro-2026-09-06-kbk` against `atc-els-kbk-tro-2026-09-06-kbk` ("Will Kristiansund BK win against Tromso IL in the Eliteserien match scheduled for Sep 6, 2026?"), itsb/srb, den/sld, sui/swsl likewise — so the slug keys (`nor-kbk-tro-…` vs `els-kbk-tro-…`) never met and the ONLY bridge was the matchup key from `markets.event_title`, absent on a moneyline judged before the title is stored (production `no_key_intersection`, keys=2) and unequal wherever the venue spells a club otherwise. On the 09-06 read every LISTED same-code game's keys meet once the event title is stored (the production labels were stale, §0.1 of the investigation), so R1 recovers little on that day and is kept MINIMAL: a lookup, and the census that reads it.

### 15.1 The key (`premap.event_keys_for`, `_pair_key`; the sweep through `keys_for_row`)

Both sides emit `<a>-<b>-<date>` beside the slug key: his `nor-kbk-tro-2026-09-06-kbk` also keys `kbk-tro-2026-09-06`; the venue's `atc-els-kbk-tro-2026-09-06` likewise. It ends in the date, so `_dated_admissible` admits it by the rule it already had (a pair key without a date, `kbk-tro`, is never admitted). EXACTLY three tokens must stand before the date (`<lg>-<a>-<b>`): a two-token slug has no league to strip, a four-token one is not the grammar, and a bare date is never a key — the `league_alias_probe`'s own lesson (`gvs-2026-08-25` strips to nothing). Keys are a lookup, never a decision: the rows it fetches answer to the arms exactly as before — the identity arm, the alias arm's single-code and opponent witness (`yn:alias-unwitnessed` with no event title, `yn:league-ambiguous` on two codes carrying the pair), the draw witness, C3's `_c3_code`, C5. Rows re-key at the next sweep: the UPSERT rewrites `event_keys` on conflict, the full sweep rewrites every live row inside 30 minutes and the fast lane the imminent window inside 3, so no backfill statement is needed (rows the sweep no longer sees age out at `PRUNE_HOURS` and are not tradeable anyway).

| his slug (event title) | before | after |
|---|---|---|
| `nor-kbk-tro-…-kbk` "Will Kristiansund BK win on 2026-09-06?" (none) | `no_key_intersection` keys 2 rows 0 | rows 6, `no_side_match:yn:alias-unwitnessed` — the witness rule survives the lookup |
| the same, "Kristiansund BK vs. Tromsø IL" | `atc-els-kbk-tro-…-kbk` LONG `premap_alias nor->els` (the matchup key) | the same, reached through the pair key too |
| the same, the venue's title spelled "Kristiansund vs. Tromsø" | `no_key_intersection` | `atc-els-kbk-tro-…-kbk` `premap_alias nor->els`; the draw likewise |
| `els` and `xx` both carrying `kbk-tro-2026-09-06` | — | `no_side_match:yn:league-ambiguous` `[els, xx]` |
| `nor-kbk-tro-…-first-half-total-1pt5` "…: 1st Half O/U 1.5", the venue's title otherwise | `no_key_intersection` | `tsc-els-kbk-tro-…-fh-1pt5` `premap_fh_total nor->els` (C3's `_c3_code`, unchanged) |
| `nor-sar-vif-2026-09-07-vif` against the venue's `els-sar-vif-2026-09-06` | `no_key_intersection` | the same: the pair key carries HIS date |
| `fl1-olm-pfc-…-spread-home-1pt5` with no event title (the C3 pin) | `no_key_intersection` | `asc-lg1-olm-pfc-…-neg-1pt5` `premap_spread fl1->lg1` |

Tests: `backend/tests/test_r1_pair_key.py` (the els rows verbatim from the 19:09Z dump). Three pins moved with the lookup, each to the class the key recovers: `test_c2_yesno_alias.test_the_alias_needs_his_event_title_as_the_witness` (`no_key_intersection` → `no_side_match:yn:alias-unwitnessed`), `test_c3_derivatives.test_the_feeds_own_rows` (the spread without an event title maps), `test_premap_key_bridge.test_a_slug_with_no_kind_prefix_is_unchanged` (the key set names the pair key).

### 15.2 The census (`resolve_explain`, R3): `venue:league-unlisted`

The `league_alias_probe` stripped the league from HIS slug key only and the venue never emitted the stripped key, so it read `would_have_hit: false` on every row for two weeks and measured this class as zero. With the pair key in the fetch that question has no answer left, so the probe is RE-POINTED: on `no_key_intersection`, for a slug that parses `<lg>-<a>-<b>-<date>…`, it asks whether ANY atc row on his date carries either of his club codes in its position (`identifier ~ '^atc-[a-z0-9]+-(<a>-[a-z0-9]+|[a-z0-9]+-<b>)-<date>-'`, LIMIT 25, the diagnostic path only — never `resolve`); his club NAMES (§16's `his_name_keys`) were in the fetch already and met nothing. Nothing: the venue does not list his league that day — `split = venue:league-unlisted` (`explain_unmapped` prints `no_key_intersection:venue:league-unlisted`; gre1, bel1, ere, bra3 on 2026-09-06, $33,868) and the row leaves the mapper's bucket. Something: listed, keys unmet — `would_have_hit`, counted by `api_unmapped_census` as before (`alias_hits`, `alias_examples`). A slug outside the grammar (tennis, an undated future) asks nothing. The probe's fields: `name_keys`, `code_pattern`, `rows_it_would_find`, `would_have_hit`, `sample`.

## 17. C6-N (2026-09-07) — the names-witnessed per-team row when NO code is shared

Owner standing orders as §11. The gap, read from the venue's own rows (render-ops `esports-chi-rows` 13:52Z, `scratchpad/hard2/gap_esports_chi.md` §1.1-1.2; the col1 rows in `gap_league_codes.md` §a): his league code AND both club codes differ from the venue's —

| his feed (`trades` / `markets`) | the venue (`us_premap`) |
|---|---|
| `chi1-cdh-csc-2026-09-06-csc` "Will CSD Colo-Colo win on 2026-09-06?", event "CD Huachipato vs. CSD Colo-Colo" (No $24,372 / Yes $1,604; his +30,419 win, `no_side_match:yn:no-row` at 22:58Z) | `atc-pdc-hua-col-2026-09-06-col` yes/LONG no/SHORT, line empty — "Will CSD Colo-Colo win against CD Huachipato in the Primera Chile match scheduled for Sep 6, 2026?" |
| `…-cdh` "Will CD Huachipato win on 2026-09-06?"; `…-draw` "Will CD Huachipato vs. CSD Colo-Colo end in a draw?" | `…-hua` "Will CD Huachipato win against CSD Colo-Colo in the Primera Chile match …"; `…-draw` "Will the Primera Chile match CD Huachipato vs CSD Colo-Colo scheduled for Sep 6, 2026 end in a draw?" |
| `chi1-cdp-cu1-…-cu1` "Will CD Universidad de Concepción win on 2026-09-06?" | `atc-pdc-pal-uco-…-uco` "Will CD Universidad de Concepcion win against CD Palestino in the Primera Chile match …" |
| `col1-dep-mif-…-mif` "Will Millonarios FC win on 2026-09-06?", `markets.event_title` NULL ($24,576, `no_key_intersection`); `…-dep`, `…-draw` | `atc-lco-per-mil-…-mil` "Will Millonarios FC win against Deportivo Pereira in the Liga Colombia match scheduled for Sep 6, 2026?", `-per`, `-draw` |
| `col1-cle-gdr-…-gdr` "Will Águilas Doradas Rionegro win on 2026-09-06?", `…-cle`, `…-draw` (event title NULL) | `atc-lco-leq-agu-…-agu` / `-leq` "Will Internacional de Bogota win against Aguilas Doradas Rionegro in the Liga Colombia match …" / `-draw` |

The alias arm (§8) refuses `yn:no-row` (no row under any league code carries his `<a>-<b>-<date>-<t>`) and C5 (§11) has no candidate (it needs exactly one shared code): $27k chi1 + $41k col1 in 24 h. The venue's own per-team question names his club as his title states it and his opponent as his event title states it, on his date — C2's alias witness (§8.1 step iii) minus the suffix equality — and that, and nothing else, admits the row.

### 16.1 The rule (`premap._yn_names_pick`, `_yn_names_draw_pick`; `names_resolve` for the stored-title witness)

Consulted inside `_yn_pick` only after the alias arm's `yn:no-row`, and only where C5 has no candidate (`_c5_owns`: his own event on the board under some code, or an event sharing exactly one of his codes in position, is C5's shape — the names arm refuses `yn:no-row`, today's reason, and C5 runs after as before). Then, ALL of:

1. **His title is his dated win question** (`_bridge_title_subject`: the anchor X; the C2 gate `_yn_pick` already passed).
2. **His event title names exactly two sides** (`_yn_event_sides`), X matches exactly ONE (`yn:event-shear`), and that side's POSITION is t's position in his slug (t == a → 0) else `yn:names-shear` (a title asking about the other club, a reversed event title). With no event title stored, his moneyline title on the OTHER code as `trades.market_title` / `markets.title` store it (`names_resolve`: the C5 read `_C5_TITLES_SQL`, bounded — `LIMIT 9`, nine rows `titles-overflow`, two anchors `titles-disagree`, none `no-stored-title`) names the opponent; read only once the pure arm said `yn:names-unwitnessed`, and the identity lane's yes/no call then runs again with that one name as `names_witness`. Nothing: `yn:names-unwitnessed` (`yn_c6.why`).
3. **Among the fetched rows, the FULL-GAME per-team atc rows on his date whose suffix is the code at the SAME position** (`_yn_names_rows`; never fh/sh/draw/exact) whose P4 question passes `_yn_team_row(d, anchor=X, opp_witness=other)` — subject X, opponent the other side, his date, the club scope screens, the league-slot rule (a women's twin, 'Primera Chile Femenina', is screened here and is no candidate) — form EXACTLY ONE identifier. Two (a same-day twin under `pdcx` with the same league phrase and clubs): `yn:names-ambiguous` (`candidates`). None, but a row naming X against another club, his opponent against another club, or — the other position's row read in mirror — his opponent as subject against a club that is not his: `yn:names-mismatch` (`venue_rows`): 'Gil Vicente Barcelos' is not 'Gil Vicente FC', 'Universidad Catolica' is not 'Universidad Católica de Chile', 'Junior FC' is not 'CDP Junior FC' under token-set equality — refused by design. None at all: `yn:no-row` stands (a wrong date, a terse question).
4. **`_yn_gate`**: the venue's own side and intent for his Yes/No (`yn:side`, `yn:intent`); then `match_side`'s `_yn_line_ok` and identity veto as for every arm (a lined venue row: `yn:picked-vetoed`).

**The draw** (`_yn_draw_pick` → `_yn_names_draw_pick` when no `-draw` row under any code carries his codes): his sides from his event title or, with none, his draw title (`_yn_draw_title_sides`, §9.3); the per-team rows naming sides[0] at position 0 against sides[1] (or sides[1] at position 1 against sides[0]) witness the event — exactly one event; a pair read only in the swapped positions is `yn:names-shear`, two events `yn:names-ambiguous`, a per-team row naming one of his sides at its position against another club `yn:names-mismatch` (as for the moneyline); then that event's `-draw` row through `_yn_draw_row` on his sides.

The hit is `matched_by premap_names`, source `premap` (the class the quarantine admits), and carries `code_pair {"cdh": "hua", "csc": "col"}`, `witness` (the row that named him), `league_alias chi1->pdc` where the league code differs; `resolve` returns `code_pair` / `witness`, `resolve_explain` carries them with `yn_c2` (the pure arm's trace) and `yn_c6` (`witness_src: trades`, `other`, the refusal with `why`). Read in this one call: never stored, never reused for another date or opponent (the 2026-09-13 return certifies from its own rows), never a table (pinned: no code or club literal in the arm's source). Behind `PREMAP_YN_IDENTITY` like all of C2/C3/C5: off, every row answers as before (`yn:would-resolve` in the dark census).

### 16.2 The lookup: the per-club dated name key (`venue_name_keys`, `keys_for_row`, `his_name_keys`)

R1's pair key does not fetch these rows (both codes differ), and the matchup key fails whenever `markets.event_title` is NULL (`col1-dep-mif-…-mif`) or the venue spells the event otherwise. So the sweep keys each FULL-GAME per-team atc row by the P4 subject its own question names (`<club>@<date>`, folded and normalized as the arms read names; the question's date must be the identifier's) and the draw row by the two clubs its draw template names; his side keys his title's anchor, his event title's two sides and his draw title's two sides. The sweep writes `keys_for_row(event_keys, r)` on every row (both paths); rows gain the keys at their next sweep exactly as §15.1. A lookup only: what the fetched rows answer to is the witness above. Consequence for §11: a moneyline with no event title now reaches the venue's row for HIS club alone through his anchor's key; C5 then sees a lone per-team row and witnesses nothing (`yn:no-row`, today's reason — before, `no_key_intersection`); with the event titles meeting, C5 reads as before.

### 16.3 Expectation table (the verbatim rows, offline, `PREMAP_YN_IDENTITY=on`; `backend/tests/test_c6_names.py`)

| his slug | outcome / witness | venue identifier | side / intent | matched_by | code_pair |
|---|---|---|---|---|---|
| `chi1-cdh-csc-…-csc` | No / Yes, event title | `atc-pdc-hua-col-…-col` | no SHORT / yes LONG | premap_names | {cdh: hua, csc: col} |
| `…-cdh` | Yes / No | `…-hua` | yes LONG / no SHORT | premap_names | the same |
| `…-draw` | Yes / No, event title or his draw title | `…-draw` | yes LONG / no SHORT | premap_names (draw_witness event / title) | the same |
| `chi1-cdp-cu1-…-cu1` (typed moneyline: M1's parser fix) | Yes | `atc-pdc-pal-uco-…-uco` | yes LONG | premap_names | {cdp: pal, cu1: uco} |
| `col1-dep-mif-…-mif` | Yes, event title / NULL event title + his `-dep` title stored / neither | `atc-lco-per-mil-…-mil` / the same via `trades` / `no_side_match:yn:names-unwitnessed` | yes LONG | premap_names | {dep: per, mif: mil} |
| `col1-cle-gdr-…-gdr`, `-cle`, `-draw` (NULL event title) | No / Yes / No | `atc-lco-leq-agu-…-agu` / `-leq` / `-draw` | as traded | premap_names | {cle: leq, gdr: agu} |
| slug `-csc`, title Huachipato; a reversed event title | | `no_side_match:yn:names-shear` | | | |
| a `pdcx` twin, the same clubs and league phrase | | `no_side_match:yn:names-ambiguous` (moneyline and draw) | | | |
| `por-gil-acv-…-gil` "Gil Vicente FC" against ligpor "Gil Vicente Barcelos" (codes assumed `gvb-adv`) | | `no_side_match:yn:names-mismatch` from the `-adv` row his opponent's key fetched | | | |
| Liga Portugal, the venue's own rows (`league_rows_por_1415.log`): `atc-ligpor-vit-cas-2026-09-05-*` "Will Casa Pia Lisbon win against Vitoria SC Guimaraes in the Liga Portugal match scheduled for Sep 5, 2026?", `atc-ligpor-scl-rav-2026-09-05-*` "Santa Clara Azores vs. Rio Ave FC", `atc-ligpor-gil-acv-2026-09-05-*` "Gil Vicente Barcelos vs. Academico de Viseu FC" against his `por-gui-cas-2026-09-06` "Vitória SC vs. Casa Pia AC", `por-cds-rio-2026-09-06` "CD Santa Clara vs. Rio Ave FC", `por-gil-acv-2026-09-06` | | the venue DATES the games 2026-09-05 where his slugs say 2026-09-06: no key of his meets (his name and pair keys carry HIS date), `no_key_intersection` + `venue:league-unlisted` (no atc row on HIS date carries either code), and handed the rows every arm reads `yn:no-row` — never a ±1 day tolerance. With the date forced equal in the fixture: cds-rio (no shared code) `yn:names-mismatch` on all three — 'Santa Clara Azores' is not 'CD Santa Clara'; gil-acv (his own codes) the alias arm's `yn:subj` / `yn:opp-witness` / `yn:draw-names` — 'Gil Vicente Barcelos' is not 'Gil Vicente FC'; gui-cas (one shared code, both clubs named otherwise) `no_key_intersection`, listed — nothing of his keys it. 'Rio Ave FC' and 'Académico de Viseu FC' alone pass and a pick needs both sides: por ($80k/24 h) is NOT mappable identity-only on today's rows | | | |
| a lined / intent-swapped / terse / wrong-date venue row | | `yn:picked-vetoed` / `yn:intent` / `yn:no-row` / `yn:no-row` | | | |
| the venue coding Huachipato `cdh` (one shared code) | | C5: `atc-pdc-cdh-col-…-col` `premap_identity`, `code_translated {csc: col}` — the names arm stood aside | | | |

Left out, by design: a witness from the opponent's row alone admits nothing (it names the mismatch only); no key on the venue's opponent slot (the brief's `<subject>@<date>`), so a names hit needs the row for HIS club fetched — his anchor's key does that on every moneyline; no relaxation of the club screen or of token-set equality; the market-group suffix on non-moneyline event titles (" - More Markets") is M1's; nothing stored, no separate switch.

## 18. E1 (2026-09-07) — the esports event-title key: his cs2 series meet the venue's board

Read 13:40–14:10Z (`gap_esports_chi.md` §1.3/§3.3; the venue rows `esports_chi_rows_1352.txt` Q4, his rows Q1, verbatim). The venue lists every cs2 series he traded as an `aec-cs2-<a>-<b>-<date>` moneyline whose SIDES are the team names (`fokus` LONG / `nemiga` SHORT, `ex rustec` / `fortress`, `1win` / `astral` …), the question `Who will win in the upcoming esports event <A> vs <B> scheduled for <date> at <clock> UTC?` (the clock stamps line `00`/`30`, erased as a clock artifact) and the event title `<A> vs. <B>`. With EQUAL codes the wording arm already maps a series by the venue's own side description EQUALLING his outcome (`match_side`'s named branch, unique; `cs2-fnc-nip-…` [NIP] → `aec-cs2-fnc-nip-…` nip BUY_SHORT, book 39). Five series refused `no_key_intersection` for the keys alone: his feed digit-suffixes or shortens a code (`fokus-nemi1`, `k271-sin2`, `bbl1-sin2`, `rustec-for10`, `fokus-ast`, `1win-ast` against the venue's `fokus-nemi`, `k27-sin`, `bbc-sin`, `erx-for`, `fokus-astr`, `1win-astr`), so the slug keys never meet, and his event title `Counter-Strike: <A> vs <B> (BO<n>) - <tournament>` keyed `fokus vs nemiga stake ranked episode 4@…` where the venue's title keys `fokus vs nemiga@…`. Patch M4 against f1f2fb2; tests `backend/tests/test_m4_esports.py` (beside `test_c3_derivatives.py`'s key-builder pins; a separate file so another builder's edit of `event_keys_for` merges).

**The rule (`premap._esports_matchup`, one branch of `event_keys_for`; a lookup, never a decision).** His title is read through exactly the template `^counter-strike: (?P<a>.+?) vs (?P<b>.+?) \(bo\d\)(?: - .*)?$` — on the raw title, case-folded, because `_key_norm` erases the very punctuation the template names (the C3 draw template reads the raw title the same way) — down to `<a> vs <b>`, and that matchup then keys exactly as any title does: `<a> vs <b>@date` and `<b> vs <a>@date` (`fokus vs nemiga@2026-09-06`, `nemiga vs fokus@…`; `ex rustec vs fortress@2026-09-07`; `1win vs astral@…`), which the venue's `FOKUS vs. Nemiga` already keys. A title outside the template (`… - Map 1 Winner`, `Map Handicap: …`, a draw title, another game) keys as it always did. The decision is unchanged: prefix {aec, atc}, then `match_side`'s named branch — the venue's own side description must EQUAL his outcome after `_norm`, unique; anything else refuses `no_side_match` (a venue side `nemiga` beside his outcome `Nemiga Gaming` refuses, pinned). The same keys carry his `-game<N>` map markets to the venue's `astatc-` rows (§17).

| his slug (Q1) | outcome | venue identifier | side / intent | matched_by |
|---|---|---|---|---|
| `cs2-fokus-nemi1-2026-09-06` ($2,862 + $344) | Nemiga / FOKUS | `aec-cs2-fokus-nemi-2026-09-06` | nemiga BUY_SHORT / fokus BUY_LONG | premap |
| `cs2-k271-sin2-2026-09-06` ($1,489) | K27 | `aec-cs2-k27-sin-2026-09-06` | k27 BUY_LONG | premap |
| `cs2-rustec-for10-2026-09-07` ($2,301) | ex-RUSTEC | `aec-cs2-erx-for-2026-09-07` | ex rustec BUY_LONG | premap |
| `cs2-fokus-ast-2026-09-06` ($2,429) | ASTRAL | `aec-cs2-fokus-astr-2026-09-06` | astral BUY_SHORT | premap |
| `cs2-bbl1-sin2-2026-09-06` ($2) | Sinners | `aec-cs2-bbc-sin-2026-09-06` | sinners BUY_SHORT | premap |
| `cs2-sin2-cs1-2026-09-06` ($496) | Sinners | `no_key_intersection` — the venue's listing unread (§5 of the gap note); the keys are built (`sinners vs cybershoke esports@…`) | — | — |
| `cs2-1win-ast-2026-09-06` series (not in his 30 h; its `-game2` is §17's) | 1WIN | `no_side_match` with `his_lines=['1']` — the wording arm's line reader takes the `1` of `1WIN` right after `Counter-Strike:` as a line (`_LINE_CTX`'s `:` context) and the unlined aec row fails the line guard. The decision arm, not the lookup: pinned as it stands, not widened here | — | — |

The k27-sin aec row's sides were not printed by any read (the Q4 listing was cut at `hero-1win`); the test builds them as every printed aec-cs2 row builds them — the side is the team name exactly as the venue's event title `K27 vs. Sinners` states it. Behind no switch: keys are built the same way at sweep time and copy time, and the venue's rows land under these keys at the next sweep.


## 19. E2 (2026-09-07) — the map-winner family (`map_winner`): his `-game<N>` maps to the venue's per-team map row, subject side only

Read 13:50Z / 14:03Z (`nf-venue_1350.log` lines 311–318, `esports_chi_rows_1403.log` lines 280–326, verbatim). His `cs2-<a>-<b>-<date>-game<N>` with title `Counter-Strike: <A> vs <B> - Map <N> Winner` and a TEAM as his outcome read `unknown_market_type:unparsed` (`game1` is neither alpha, a line nor a team code) — $24.5k + $5.2k in 30 h. The venue lists ONE per-team yes/no row per map: `astatc-cs2-<a>-<b>-<date>-map<N>`, sides yes BUY_LONG / no BUY_SHORT, line `''`, question `Will <X> win Map <N> vs <Y>?` — `fnc-nip` map1/map2 name NIP, `hero-1win` map2 Heroic (no map1 listed at 13:50Z), `1win-astr` map2 1WIN, `fokus-nemi` map1/map2 FOKUS, `k27-sin` map2 K27, `k27-npg` map2, `sin-pha` map1/map2 Sinners.

**The readers.** `copy_sports.family_of` reads a lone `game<N>` as family `map_winner`, `segment_of` as `map<N>`; `market_type_of` stays `unknown` (the copy lane never reads it, as before); `mirror_family_of` files it `map_winner` and `MIRROR_FAMILIES` carries it. `premap.map_his` is the slug reader ({lg, a, b, date, n}); `_want_prefixes` answers {astatc} for it inside the identity switch.

**The rule (`premap._c3_pick_map`, matched_by `premap_map_winner`, source `premap`, behind `PREMAP_YN_IDENTITY` with C3; the wording arm never sees its rows).** In order: his title must fullmatch `Counter-Strike: <A> vs <B> - Map <N> Winner` with N equal to his slug's (`map:title-shear`); among the rows his keys fetched (his event title's matchup, §16 — so his codes need not equal the venue's: `1win-ast` reaches `1win-astr`), an `astatc-` identifier on his date whose suffix is exactly `map<N>` (`map:segment-absent` when the venue lists no map N on the event — `hero-1win -game1`); its question fullmatches `^will (?P<x>.+?) win map (?P<n>\d+) vs (?P<y>.+?)\??$` with n == N (`map:question-shape`); his outcome is exactly one side of his own title's matchup and `<x>` `_yn_name_match`es it while `<y>` matches the other side (`map:names`); his BUY of X is the row's `yes` side with the venue's own intent (`map:side` when no yes side is listed, `map:intent` when its intent is not BUY_LONG); two identifiers passing is `map:ambiguous`. His fill on Y — the venue lists no row with Y as its subject — refuses `map:subject-absent`: `no` on `NIP wins Map 1` is `fnatic wins Map 1` only if a listed map is never voided, which no row states, so the complement is NOT assumed (admit it only after a settled `no` row on a lost map is read). The same fact bears on the subject `yes` row itself: a voided map (a 2-0 series never plays map 3) settles the venue row and his market by rules no row states — accepted here as for the other derivatives (decision 17's ledger), said rather than assumed. **The event witness (review HIGH-1 / MEDIUM-1):** the matchup keys reach every astatc row titled `<A> vs. <B>` on his date whatever game code or team codes its identifier carries (the pair fields rosters in several games; a bracket's same-day rematch lists them under reversed codes), so the identifier's league code must equal his (the venue's esports codes read `cs2` on every row), and when ANY row on his date carries his own stem `astatc-<lg>-<a>-<b>-<date>-` (his codes byte for byte) an identifier outside it is another event — refused `map:segment-absent`, never a candidate, never an ambiguity; the digit-suffixed case (`1win-ast`) carries no stem on the board and is unchanged. Never a position, never a code similarity, never a nickname; the scope screen (`_yn_slot_bad`) is not applied to the two names — `_yn_name_match`'s token-set equality already refuses a qualifier on either side. `resolve_explain` carries `family`, `map` (the trace) and the refusal as the step's `split`, so MIRRORUNMAP prints `no_side_match:map:subject-absent`; it is set last so a C5 trace on a digit-suffixed code never relabels a family C5 has no lane for.

| his slug (Q1) | outcome | venue identifier / refusal | side / intent | matched_by |
|---|---|---|---|---|
| `cs2-fnc-nip-…-game1` ($6,885) | NIP | `astatc-cs2-fnc-nip-2026-09-06-map1` | yes / BUY_LONG | premap_map_winner |
| `cs2-fnc-nip-…-game1` ($1,107) | fnatic | `no_side_match:map:subject-absent` | — | — |
| `cs2-fnc-nip-…-game2` ($4,648 / $2,339) | NIP / fnatic | `…-map2` yes / `map:subject-absent` | | |
| `cs2-hero-1win-…-game2` ($1,991 / $3,198) | Heroic / 1WIN | `astatc-cs2-hero-1win-2026-09-06-map2` yes / `map:subject-absent` | | |
| `cs2-hero-1win-…-game1` ($358 / $3) | Heroic / 1WIN | `map:segment-absent` (no map1 listed) | | |
| `cs2-1win-ast-…-game2` ($383 / $244) | 1WIN / ASTRAL | `astatc-cs2-1win-astr-2026-09-06-map2` yes (through §16's keys; `no_key_intersection` without his event title) / `map:subject-absent` | | |
| `cs2-fokus-nemi1-…-game1` ($5,873 / $3,687), `-game2` ($2,396 / $25) | FOKUS / Nemiga | `astatc-cs2-fokus-nemi-2026-09-06-map1` / `-map2` yes / `map:subject-absent` | | |
| `cs2-k271-sin2-…-game2` ($10,293 / $1,789) | K27 / Sinners | `astatc-cs2-k27-sin-2026-09-06-map2` yes / `map:subject-absent` | | |
| `cs2-k271-sin2-…-game1`, `cs2-bbl1-sin2-…-game2`, `cs2-rustec-for10-…-game2` | | unverified: no `astatc-…-map<N>` row read for k27-sin map1, bbc-sin, erx-for (the 1403 read lists none) — `map:segment-absent` by fact if the venue lists none | | |

**The wrong-family route closed.** His `cs2-<a>-<b>-<date>-tot-<L>` (total maps) fell to the bare-line fallback and typed SPREAD, into the asc rows. The venue lists `tsc-cs2-<a>-<b>-<date>-tot-2pt5` in the full C3 total grammar (over BUY_LONG / under BUY_SHORT, line 2.5, `Will the total in FOKUS vs. Nemiga be more than 2.5?`). `family_of` now reads it as the `total` family on the segment `maps` (`c3_his` line 2.5, seg `maps`), which no venue identifier carries as a segment token, so `c3_same_segment` empties the pool and the pick refuses `total:segment-absent` by name — a C3 total step, never the spread lane, never the wording arm. `market_type_of` types it `prop` (no venue prefix: with the switch off it refuses `unknown_market_type:family_not_listed`), exactly as a first-half total is — NOT `total`, because a `total` typing would let the wording arm map it onto that tsc row on line equality alone with the switch OFF, and the env may only lower rails. A `tot` that IS one of his slug's own team codes (a Tottenham pick side, its compact spread) is that code as before. The total-maps lane, the map handicap (`asc-…-hcap-(neg|pos)-<L>`, mostly still `1 50` sides in the 1403 read, sin-pha yes/no), the map-N round handicap and total rounds are NOT built (§3.5 of the gap note): questions read but no rule written.

**Left out, by design.** The map winner's other side (~$7k read); `sin2-cs1` (listing unread); the `1WIN`-first series phantom line (§16, the decision arm); an orientation rule for the complement until a settled map row is read. Every C1–C5 pin is green with the patch (`python -m pytest -q -x -p no:cacheprovider tests`).

## 20. C6-ES (2026-09-07) — the exact score by identity

Owner standing orders: "capture and mirror RN1's whole book"; identity-only, fail closed. `C3_ABSENT` (§9) refused `exact_score` by name because `c3_rows_2041.log` listed none — an artefact of that preset's `NOT LIKE 'atc-%'` filter. The venue lists exact scores UNDER THE MONEYLINE PREFIX (render-ops `epl-rows`, `scratchpad/hard2/epl_rows_1338.log:241-314`; `premap_rows_1909.log:62-97` for La Liga; the investigation in `gap_new_families.md` §1.1, §2, §3.1): ~$86k of his book in 24 h (epl 50.8k, lal 18.7k, sea 9.1k, fl1 4.5k, bun 2.9k) died at `unknown_market_type:family_not_listed`.

| his feed (`trades`, nf_his_1350.log) | the venue (`us_premap`, epl_rows_1338.log) |
|---|---|
| `epl-eve-mun-2026-09-06-exact-score-2-1` — "Exact Score: Everton FC 2 - 1 Manchester United FC?" No/Yes ($12.5k) | `atc-epl-eve-mnu-2026-09-06-exact-score-2-1` yes LONG / no SHORT, line `1` (a phantom) — "Will EVE vs MNU finish EVE wins 2-1?" (:301-302) |
| `…-exact-score-1-2` / `-3-0` / `-1-1` / `-0-0` | `…-exact-score-1-2` "Will EVE vs MNU finish MNU wins 2-1?" (:295); `-0-3` "… MNU wins 3-0?"; `-1-1` "… Draw 1-1?" (:293); `-other` "… finish Other?" (:313) — 17 identifiers a game, 34 rows of both EPL games read so |
| `epl-ars-che-…-exact-score-2-1` "Exact Score: Arsenal FC 2 - 1 Chelsea FC?" | `atc-epl-ars-cfc-…-exact-score-2-1` "Will ARS vs CFC finish ARS wins 2-1?" (:265-266) |
| `lal-val-bar-…-exact-score-1-2`, `sea-juv-mil-…-2-0`, `fl1-ang-ren-…-1-2`, `bun-hsv-mai-…-0-1` | listed by the atc census (34 identifiers a game; `atc-lal-esp-sev-…-exact-score-*` verbatim in premap_rows_1909.log) |
| `bra-bot-pal-…-exact-score-3-0`, `ecu1-`, `bol1-`/`lpb-`, `arg-`/`lpa-` | NOT listed: 6 atc rows a game (a, b, draw) — `exact:family-absent` stands, $25.5k/24 h cannot be mapped |

### 20.1 The rule (`premap.c6_his`, `premap.c6_pick`, `premap._c6_pick_exact`; matched_by `premap_exact_score`, source `premap`)

1. **Trigger.** `family_of(slug) == 'exact_score'` with a full-game post-date rest of exactly `exact-score-X-Y` (digits); `_want_prefixes` answers `{atc}` (switch on). The older `-es-X-Y` form, a segmented exact score (`first-half-exact-score-…`) or any other tail: `exact:slug-shape`; `-exact-score-other`: `exact:other` ('Other' is every score the venue does not list — not a proposition his title states). Outcome Yes/No (`exact:outcome`). His title must fullmatch, after `fold_latin` and lower case, `^exact score: (?P<A>.+?) (?P<X>\d+) - (?P<Y>\d+) (?P<B>.+?)\??$` with A and B past the club scope screen (`exact:title-shape`) and X, Y equal to the slug's (`exact:title-score`).
2. **The row.** Among the atc- rows on his codes and date whose rest begins `exact-score-` (never the `-fh-exact-score-` twin, never a full-game moneyline row), his own league code when any row carries it, else the ONE other code (the C2 alias, `league_alias`; two: `exact:league-ambiguous`); none at all: `exact:family-absent` (today's name — step `unknown_market_type`, split `family_not_listed`, so the census bucket is unchanged); the family listed but not this score: `exact:no-row`. The identifier is then `atc-<code>-<a>-<b>-<date>-exact-score-X-Y` byte for byte — his slug under `atc-` (the C5 code translation rewrites `mun→mnu` / `che→cfc` first, as for every family: `c5_resolve` step 3 runs `c6_pick` on the rewritten slug). The row on his side: `side_norm` == his Yes/No, `intent` the venue's own for it (`exact:side`, `exact:ambiguous`, `exact:intent`). NEVER `_yn_line_ok`: every exact-score row in the dump carries a phantom line (`1` out of "wins 2-1", `0` out of "3-0"; `_question_line`), and `_market_rows` now stamps NO line on a `-exact-score-` identifier so the phantom reaches no other arm (the stored rows keep theirs until re-swept; the lane never reads it — pinned on rows carrying it).
3. **The question certifies the digit order.** Fullmatch `^will (?P<va>[a-z0-9]+) vs (?P<vb>[a-z0-9]+) finish (?:(?P<w>[a-z0-9]+) wins (?P<m>\d+)-(?P<n>\d+)|draw (?P<d1>\d+)-(?P<d2>\d+)|other)\??$` (`exact:question-shape`), `va`/`vb` the identifier's codes in its order (`exact:codes`); then X>Y ⇒ `w == va` and (m, n) == (X, Y); X<Y ⇒ `w == vb` and (m, n) == (Y, X); X==Y ⇒ `draw` with d1 == d2 == X; `other` on a scored identifier ⇒ `exact:other`; anything else `exact:order-uncertified` (the row's question in the trace). Never assumed from the identifier.
4. **The club witness.** His title binds X to his FIRST club A; A is the venue's `va` only through the event's own P4 rows: `atc-<code>-<a>-<b>-<date>-<a>` and `-<b>` must both be the venue's per-team contract (`_c5_sided`) whose question is the measured template on his date (`_yn_team_question`) with subject `_yn_name_match`-ing A and B respectively (`exact:club-unwitnessed`, `why`: `p4-row-absent:<code>`, `subject:<code>`, `yn:shape:<code>` …). A title with the clubs swapped ("Exact Score: Manchester United FC 2 - 1 Everton FC?" on `-2-1`) refuses here.

Like a segmented C3 pick, an exact score is read by the family's pick alone: the wording arm never sees the atc- rows the prefix admits (the wrong-market trap `_yn_title_is_his_game` names). Behind `PREMAP_YN_IDENTITY` with C2–C5: off, `_want_prefixes` carries no prefix and the family reads `unknown_market_type:family_not_listed` / `exact:family-absent` byte for byte as before. `resolve` returns the hit with `matched_by: premap_exact_score` (and `code_translated` / `witness` under C5, `league_alias` under the alias); `resolve_explain` carries the lane's trace as `c6` (or inside `yn_c5.lane` when C5 translated), and its refusal as the step's `split` (`no_side_match:exact:order-uncertified` …). `mirror_family_of` already files the slug `exact_score` (in `MIRROR_FAMILIES`).

### 20.2 Expectation table (the verbatim rows, offline, `PREMAP_YN_IDENTITY=on`; `backend/tests/test_c6_exact_halftime.py`)

| his slug | title / outcome | venue identifier | side / intent | matched_by | note |
|---|---|---|---|---|---|
| `epl-eve-mun-…-exact-score-2-1` | "Exact Score: Everton FC 2 - 1 Manchester United FC?" Yes | `atc-epl-eve-mnu-…-exact-score-2-1` | yes / LONG | premap_exact_score | `code_translated {mun: mnu}`, witness `…-mnu` (event title) |
| `…-exact-score-1-2` | No | `…-exact-score-1-2` ("MNU wins 2-1") | no / SHORT | premap_exact_score | |
| `…-exact-score-1-1` / `-0-0` | Yes / No | `…-1-1` ("Draw 1-1") / `…-0-0` | yes / LONG, no / SHORT | premap_exact_score | every listed score of both games, both sides, pinned |
| the `-1-2` row reworded "EVE wins 2-1" | | | | `no_side_match:exact:order-uncertified` | also "MNU wins 2-1" / "EVE wins 1-2" / "Draw 2-1" on the `-2-1` row |
| "Exact Score: Manchester United FC 2 - 1 Everton FC?" on `-2-1` | | | | `no_side_match:exact:club-unwitnessed` (`why: subject:eve`) | a P4 row missing or reworded likewise |
| `sea-juv-mil-…-exact-score-2-0` on an event listing no exact score | | | | `unknown_market_type:family_not_listed`, refusal `exact:family-absent` | today's names, `c6` trace beside them |
| `-exact-score-1-2` with only the `-fh-exact-score-1-2` twin listed | | | | `no_side_match:exact:no-row` | the twin is never a candidate |
| `…-exact-score-other`; a `-2-1` row worded "finish Other?" | | | | `no_side_match:exact:other` | |
| `…-es-2-1`, `…-first-half-exact-score-2-1` | | | | `no_side_match:exact:slug-shape` | |
| the rows as us_premap stores them tonight (line `1`) | | `…-exact-score-2-1` | yes / LONG | premap_exact_score | the phantom never vetoes; the sweep stamps `''` from now on |
| switch off | | | | `unknown_market_type:family_not_listed`, `exact:family-absent` | no `c6`, no `yn_c5` |

Left out, by design: the halftime exact score (`-fh-exact-score-`; his feed shows no segmented exact score); 'Other'; unlisted leagues (bra / ecu1 / lpa / lpb — `exact:family-absent` is the true verdict); por / scop (no venue league identified); no code table, no similarity, no positional reading of the digits. The stored `markets.event_title` suffix (" - Exact Score") is the key grammar M1 builds (`event_keys_for`, `_yn_event_sides`), not this lane: the fixture keys on the matchup title as the C5 fixture did.

## 21. C6-HT (2026-09-07) — the halftime result by identity

The same gap one family over (`gap_new_families.md` §1.4, §3.3): ~$15k/24 h ($48k/30 h: bra 8.6k — NOT listed, 6 atc rows a game — fl1 7.2k, epl 5.0k, ere 3.5k, sea 2.2k) read `halftime:family-absent` while the venue lists `atc-<lg>-<a>-<b>-<date>-fh-<t>`:

| his feed (nf_his_1350.log) | the venue (nf_venue_1350.log:273-278, 297-300; premap_rows_1909.log:98-101) |
|---|---|
| `epl-ars-che-2026-09-06-halftime-result-away` — "Chelsea FC leading at halftime?" No/Yes ($2.4k); `-home` "Arsenal FC leading at halftime?" | `atc-epl-ars-cfc-2026-09-06-fh-cfc` yes LONG / no SHORT, line empty — "Will Chelsea FC lead Arsenal FC at halftime?"; `-fh-ars` "Will Arsenal FC lead Chelsea FC at halftime?" |
| `fl1-olm-pfc-…-halftime-result-draw` — "Olympique de Marseille vs. Paris FC: Draw at halftime?" ($6.3k) | `-fh-draw` "Will ARS vs CFC be tied at halftime?"; `atc-lal-esp-sev-…-fh-draw` "Will ESP vs SEV be tied at halftime?"; `-fh-esp` "Will RCD Espanyol de Barcelona lead Sevilla FC at halftime?" |
| `epl-eve-mun-…-halftime-result-away` "Manchester United FC leading at halftime?" | `atc-epl-eve-mnu-…-fh-eve` "Will Everton FC lead Manchester United FC at halftime?" (the `-fh-mnu` row was cut by the preset's LIMIT); `-sh-mnu` "Will Manchester United FC win the second half against Everton FC?" — never a candidate |

### 21.1 The rule (`premap._c6_pick_halftime`; matched_by `premap_halftime`, source `premap`)

1. **Trigger.** `family_of(slug) == 'halftime_result'`: `…-halftime-result-(home|away|draw)` (`halftime:slug-shape` otherwise), outcome Yes/No (`halftime:outcome`), prefix `{atc}`. Candidates are the atc- rows on his codes and date whose rest is exactly `fh-<t>`, t ∈ {a, b, draw} — never `-sh-`, never `-fh-exact-score-`, never a full-game row; league as §20.1 step 2 (`halftime:league-ambiguous`; none: `halftime:family-absent` under today's step/split; no per-team `-fh-` row for a home/away pick: `halftime:no-row`).
2. **Draw.** His title is the matchup and the qualifier `'<A> vs. <B>: Draw at halftime?'` (`_c3_title_parts`, qualifier exactly `draw at halftime`; else `halftime:title-shape`); the `-fh-draw` row on his side (`halftime:no-row` / `halftime:side` / `halftime:ambiguous` / `halftime:intent`); its question fullmatches `^will (?P<va>[a-z0-9]+) vs (?P<vb>[a-z0-9]+) be tied at halftime\??$` (`halftime:question-shape`) with the identifier's codes (`halftime:codes`); the P4 rows bind A/B to a/b as §15.1 step 4 (`halftime:subject-unwitnessed`, `why`).
3. **Home / away — NEVER a position.** His title fullmatches `^(?P<S>.+?) leading at halftime\??$` naming ONE club S past the club scope screen (`halftime:title-shape`); his other club O is the side of his event title that is not S (`_yn_event_sides`; none: `halftime:event-unwitnessed`; S not a side of it: `halftime:event-shear`). Among the `-fh-<a>` / `-fh-<b>` rows, the one whose question fullmatches `^will (?P<s>.+?) lead (?P<o>.+?) at halftime\??$` with `_yn_name_match(s, S)` and `_yn_name_match(o, O)` is his — none parses: `halftime:question-shape`; none names S: `halftime:subject-unwitnessed` (the subjects seen in the trace); two: `halftime:ambiguous`. The side row he is placed on must carry the same question (review LOW-1: `why: side-row-question`). The code t is then read from THAT row's identifier (`fh_code` in the trace) and HELD AGAINST THE CLUB (review MEDIUM-1): the event's P4 row `-<t>` must be the venue's per-team contract whose subject is S, and the other P4 row's subject O (`_c6_clubs_witnessed`; else `halftime:subject-unwitnessed` with `why: code-shear:<t>` and the P4 reason in `p4`) — a venue event whose two `-fh-` questions are swapped against their identifiers (the shape `c5_certify` names `rows-swapped`) refuses instead of trading the question. His slug's `home` / `away` token is never read — his `-home` slug with a title naming Chelsea lands on `-fh-cfc`.

Behind `PREMAP_YN_IDENTITY`; C5 translates the codes first (`che→cfc`, witnessed by the event's P4 rows and his event title); the C2 alias carries the league (`fl1->lg1`). The mirror files the slug under `market_type_of`'s `prop` (`mirror_family_of` unchanged), which `MIRROR_FAMILIES` admits.

### 21.2 Expectation table (offline, `PREMAP_YN_IDENTITY=on`)

| his slug | title / outcome / event title | venue identifier | side / intent | matched_by |
|---|---|---|---|---|
| `epl-ars-che-…-halftime-result-away` | "Chelsea FC leading at halftime?" Yes (No), `Arsenal FC vs. Chelsea FC` | `atc-epl-ars-cfc-…-fh-cfc` | yes / LONG (no / SHORT) | premap_halftime, `code_translated {che: cfc}` |
| `…-halftime-result-home` | "Arsenal FC leading at halftime?" Yes | `…-fh-ars` | yes / LONG | premap_halftime |
| `…-halftime-result-home` | "Chelsea FC leading at halftime?" | `…-fh-cfc` | yes / LONG | premap_halftime — the title, never the token |
| `fl1-olm-pfc-…-halftime-result-draw` | "Olympique de Marseille vs. Paris FC: Draw at halftime?" Yes | `atc-lg1-olm-pfc-…-fh-draw` | yes / LONG | premap_halftime, `league_alias fl1->lg1` |
| `lal-esp-sev-…-halftime-result-home` | "RCD Espanyol de Barcelona leading at halftime?" | `atc-lal-esp-sev-…-fh-esp` | yes / LONG | premap_halftime |
| the `-fh-cfc` row reworded "Will Arsenal FC lead Chelsea FC at halftime?" | | | | `no_side_match:halftime:subject-unwitnessed` |
| `-fh-ars` worded as Chelsea leading and `-fh-cfc` as Arsenal leading (rows swapped) | | | | `no_side_match:halftime:subject-unwitnessed` (`why: code-shear:ars`, `p4: subject:ars`) — v1 traded `-fh-ars` |
| the `-fh-cfc` NO row alone reworded as Arsenal leading | | | | his No: `halftime:subject-unwitnessed` (`why: side-row-question`); his Yes maps |
| both rows naming Chelsea | | | | `no_side_match:halftime:ambiguous` |
| `epl-eve-mun-…-halftime-result-away` "Manchester United FC leading at halftime?" with `-fh-eve` and `-sh-mnu` listed | | | | `no_side_match:halftime:subject-unwitnessed` — `-sh-` never a candidate; no `-fh-` per-team row: `halftime:no-row` |
| `-fh-draw` worded "Will ESP vs SEV be tied …" on the lg1 identifier | | | | `no_side_match:halftime:codes` |
| a P4 row missing / a title naming another club / a qualified title | | | | `halftime:subject-unwitnessed` / `halftime:title-shape` |
| `sea-juv-mil-…-halftime-result-draw` on an event listing no `-fh-` row; switch off | | | | `unknown_market_type:family_not_listed`, `halftime:family-absent` |

Left out: bra / ven1 / bol1 halftime rows (not read; bra inferred absent); the second-half result (`-sh-`, no feed row); a title with no event title stored (`no_key_intersection` before the lane, and `halftime:event-unwitnessed` inside it); the ' - Halftime Result' event-title suffix (M1's key grammar).

## 22. W2 (2026-09-07) — the live lane names every candidate refusal, reads the long token when his fills sit on the short-side token, and rotates the candidate walk

Investigation `gap_planned_unopened.md` (mirror_live.py at 1c1e1d7 / f1f2fb2): 58 mapped tennis and football markets ($218k in 24 h) had a shadow plan on a two-sided OPEN book while he traded and NEVER had a mirror_books row, and the lane's own verdict on each survived nowhere — the census counts a name once per tick, the heartbeat keeps the last tick's ~20 `recent` events, and four exits of `_tick_candidate` returned silently. Three changes, built on W1 (the same files); one live change on the candidate path, no order path touched.

- P1 `long_token_unknown` (mirror_live `_tick_candidate`, after the grammar certification and before the book check and the quote read) — class B, $26.9k proven and up to $79k: every fill of his sits on the venue's SHORT-side token (the aec tennis family's `<his side>:SHORT`), so `_choose_long` named the long side `other_of(a)` over HIS tokens and found none, the mapping came back `long_asset: None`, and `if not la: return` left silently every tick until he bought the other token (14 of 22 mapped markets on 2026-09-07 opened that way, 15-127 min each). The lane now reads the long token off the catalogue exactly as it reads the OTHER token for a long-only fill — the condition's sibling of the token in hand, `_SQL_SIBLING_TOKEN` (one text for both lanes since W2, `ms.SIBLING_TOKEN_SQL`; identity only, never a name or a position guess) — and the EXISTING short road runs unchanged from there: `rules.mirror_target`'s signed target, `_his_level(reducing=True, short=True)` = his other-token BUY at round(1 − p, 6), BUY_SHORT behind `short_model_confirmed` / `_short_gate` / S4. A catalogue that does not name it (absent, unreadable, or the mapper naming neither token) refuses `long_token_unknown` and memoises the market for `UNMAPPED_TTL_S` (900 s): no venue read, no book, never a token the catalogue does not name. The gates are not widened: the same fills are refused `short_side_refused` / `short_model_disarmed` / `short_gate_refused` exactly as a short whose long token the mapper named, with the same row. The shadow's row agrees: `ms.map_market` fills a `long_asset: None` mapping from the same catalogue row (`_long_from_catalogue`, `detail.long_from = catalogue`), so `long_asset` is set on the mapped row and `his_long` reads 0 against `his_other`; a catalogue that names nothing leaves the row's `long_asset` NULL and marks `detail.long_token_unknown` (the shadow measures; it still plans as before). Pinned: `test_w2_fills_only_on_the_short_side_token_open_a_short_book_through_the_existing_gates`, `test_w2_a_short_side_only_candidate_is_refused_by_the_gates_exactly_as_one_whose_long_token_was_named`, `test_w2_an_unnamed_long_token_refuses_long_token_unknown_before_any_read`, `test_w2_the_long_path_is_unchanged_when_the_mapper_names_the_long_token`; on the shadow `test_w2_the_mapper_fills_the_long_token_from_the_catalogue_when_his_fills_sit_on_the_short_side`, `test_w2_the_shadow_row_carries_the_catalogue_long_token_and_names_an_unnamed_one`.
- P2 every exit of `_tick_candidate` returns a NAME (the census counters `_mirror_stop` already had are unchanged); the four silent ones are `long_token_unknown`, `target_zero` (ratio × net rounding to nothing), `book_row_unreadable` (the book opened and its row could not be read back; it is walked from its row next tick), and `cand_unread_capped` (the walk's cap — the candidates' budget or the soft guard — left the candidate unread this tick; never one a memo or a book would have skipped anyway). `book_seen` (a market with a book) is the one name counted and written nowhere. One row per (whale, condition_id) TRANSITION lands on `mirror_candidate_refusals` (migration 054: whale, condition_id, us_slug, refusal, at, his_net, target, mark, his_px, ask, band, long_asset, books_live, opened_today, active_conditions, cand_reads, tick_s): a row when the name differs from the last one written for the pair, and again when the same name has stood for `CAND_REFUSAL_RESTAMP_S` (900 s, the `_unmapped_until` memo class), so the writes are bounded by his active conditions over 900 s; a book opening writes no row and sets the pair's memo to `opened`, so a refusal after the book closes is a transition again. The row carries only what the tick already held when the candidate left (NULL for what it never reached: "not reached", never 0), and no venue call is made for it. The tick's rows go out as ONE statement after the walk (`_SQL_CAND_REFUSALS`, `jsonb_to_recordset`); a write failure never blocks the tick — `refusal_write_failed` on the census every tick it fails, logged once per process, and the memo never takes the failed write, so the row lands on the next tick that can write it (the table is absent until 054 is applied; the workers never run migrations). Census keys added before the pinned last key: `long_token_unknown`, `target_zero`, `book_row_unreadable`, `cand_unread_capped`, `refusal_write_failed` (past the served prefix; `integ` is at its ceiling). Render-ops: the read-only `cand-refusals` preset prints the last 24 h by refusal with his dollars joined (the latest refusal per market and its no-book part; every name's rows with the tick's wall time, candidate reads and his active conditions; the 40 largest no-book markets with their refusal path through the day), and `coverage-gap` prefers this table's refusal (`live:<name>`, the newest row at or before his last fill, else the newest) over the shadow's verdict for a market with no book. Pinned: `test_w2_every_exit_of_the_candidate_returns_a_name_and_every_name_is_a_census_key`, `test_w2_one_row_per_transition_and_a_restamp_after_900_s`, `test_w2_a_refusal_write_failure_never_blocks_the_tick_and_is_logged_once`, `test_w2_a_book_row_that_cannot_be_read_back_is_named_and_the_book_is_walked_next_tick`, `test_w2_target_zero_is_named`, `test_w2_the_walks_cap_names_every_candidate_it_left_unread`; the 054 pins in `test_mirror_live_migration.py`.
- P3 the candidate walk rotates (`_candidate_order`, `_cand_cursor`, `_shadow_planned`): the woken markets first as before, then the candidates whose NEWEST shadow row carries a plan (a `would_side`, or a target that is not 0; one `mirror_shadow` read per whale per tick, `_SQL_SHADOW_PLANNED`; unreadable = no plan known, never a stop) ahead of the rest, each group newest-fill-first; a tick that broke on the cap resumes AFTER the last candidate it walked next tick, so every readable candidate is walked within ceil(readable / `MAX_MARKETS_PER_TICK`) ticks (100 candidates at the cap of 40: three ticks); a walk that reached the end of its list clears the cursor, so an uncapped tick keeps the order it always had. The cap is unchanged (`MIRROR_LIVE_MAX_MARKETS`, env may only lower it) and the memos (`_unmapped_until`, `_terminal_until`, the full-game memo, a market with a book) still skip BEFORE any read, spending no slot. Pinned: `test_w2_the_candidate_walk_rotates_planned_first_woken_first_and_the_memos_still_skip`.
- Reading the census after W2: `cand_unread_capped` on the heartbeat is how many candidates the cap left unread (0 on an uncapped tick); `refusal_write_failed` rising every tick means 054 is not applied (or the write is failing) and the rows are being retried; `long_token_unknown` is a mapped market whose long token neither his fills nor the catalogue name — a catalogue gap to read, never a mapping to guess. Names kept as they are: `max_books` (lifted 5df3975), `side_band` (named on the row, the rule kept), `short_side_refused` (lifted), `mirror_loss_stop` (owner).
